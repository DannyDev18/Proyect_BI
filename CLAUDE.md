# CLAUDE.md — Contexto del Proyecto

> **Proyecto:** Plataforma Inteligente de Analítica Empresarial y Predicción de Ventas para Empresas Multisucursal (proyecto de tesis).
> **Última actualización de este documento:** 2026-08-04 (Módulo de Gestión de Inventario y Reabastecimiento (MGIR), `docs/features/plan_modulo_inventario_reabastecimiento.md`, auditoría `docs/auditoria/52_modulo_inventario_centralizado.md` -- petición explícita del usuario: reestructurar el Dashboard de Bodega + Simulador What-If + Generador de Propuestas (ya implementados en la sesión previa como el sistema de Reabastecimiento Inteligente) en un módulo independiente con lógica centralizada, presentada originalmente como una propuesta genérica de microservicio con Kafka/RabbitMQ/Redis. **Tres rechazos explícitos, documentados con motivo en el plan:** microservicio separado (duplicaría JWT y la RLS por almacén RN-B10 sin necesidad -- el volumen de ~8.153 artículos no lo justifica), broker de mensajes (las alertas ya funcionan por polling de 60s, sin consumidor real para un broker), Redis (el proyecto ya tiene caché en proceso; el problema real era de pasadas redundantes, no de almacenamiento) -- la independencia se logró con un paquete Python de frontera explícita (`backend/app/inventory/`), no con infraestructura nueva; tampoco se renombraron los 15 endpoints ya en producción bajo `/analytics/bodega/reabastecimiento` a `/api/inventory/*`, que habría roto el frontend sin ganar nada. **Fase 0 (auditoría previa obligatoria, solo `SELECT` contra `bi_postgres_edw` real):** cuantificó 6 duplicidades reales (D-1..D-6) que el requerimiento intuía pero no localizaba -- **D-1/D-2 (hallazgo central, severidad ALTA):** dos motores de punto de reorden coexisten a propósito desde la sesión anterior (determinista de `WarehouseService`, RN-B1/B2, vs. estocástico de `replenishment_engine`, `SS=z·σ·√LT`) con vocabularios de estado distintos (`Crítico/Cerca/Seguro/Exceso` vs. `critico/alto/medio/bajo/sin_demanda`); medido contra el catálogo real (8.153 artículos, sin restricción de almacén): **4.045 artículos (49,7% del catálogo) que el motor determinista clasifica "Seguro" el motor estocástico los clasifica riesgo "crítico"** -- amplía cuantitativamente el hallazgo A-0.7 de la auditoría 50 (54,2% de cambio en el ROP) confirmando que ese cambio de magnitud cruza el umbral de clasificación en la mitad del catálogo; **decisión de negocio para gerencia, no resuelta en esta sesión** (Fase 5 del plan, explícitamente diferida con la evidencia ya cuantificada). D-3 (dos consultas SQL sobre el mismo universo de 8.153 filas), D-5 (`/necesidad-compra` ordena por volumen de ventas, `/reabastecimiento/lista` por riesgo -- ambas activas, respuestas opuestas a la misma pregunta) confirmados sin corregir en esta sesión (decisiones de producto, no de código). **Aplicado (Fases 1, 2, 4, 7 del plan, corte de valor mínimo recomendado por el propio plan -- Fase 3 sí se aplicó, ver abajo):** **Fase 1** -- paquete nuevo `backend/app/inventory/` (`engine.py`/`service.py`/`config_service.py`/`repository.py`/`proposals.py`/`schemas.py`, movidos desde `app/services/replenishment_*`/`app/repositories/replenishment_*`/`app/schemas/replenishment.py` sin cambiar una sola línea de lógica), `__init__.py` como API pública (`dependencies.py`/`routes/replenishment.py`/`notification_service.py` actualizados para importar de ahí); sin cambio de contrato HTTP, los 15 endpoints existentes intactos. **Fase 2** -- las 4 fórmulas deterministas de `WarehouseService` (`_salida_diaria`/`_punto_reorden_efectivo`/`_dias_inventario`/`_estado_stock`) pasan a delegar en funciones puras nuevas del motor (`demanda_diaria_simple`/`punto_reorden_determinista`/`dias_inventario`/`estado_stock`, reciben los umbrales `settings.BODEGA_*` por parámetro -- el motor sigue sin importar `settings`), mismo resultado exacto -- los 363 tests existentes (incluidos los de `_estado_stock`) pasaron sin modificarse una sola línea, más 12 tests nuevos directos sobre las funciones absorbidas. **Fase 3 (D-4, unificación de `coeficiente_variacion`)** -- `WarehouseService._justificacion_transferencia` pasa a llamar la misma función del motor (`inventory_engine.coeficiente_variacion`) en vez de calcular `std/media` inline; los CORTES de clasificación (`BODEGA_CV_ALTA/MEDIA` para transferencias, `CORTE_XYZ_X/Y` para ABC/XYZ) se mantuvieron como dos juegos de constantes separados a propósito (auditoría 50 A-0.4: reutilizarlos entre sí dejaba al 96,6% del catálogo en una sola clase); único efecto real del cambio -- exigir `MESES_MINIMOS_CV`=3 meses de historia para calcular un CV, antes solo exigía demanda media > 0 -- **verificado en vivo contra `GET /transferencias-sugeridas` (212 sugerencias reales): 0 de 212 afectadas.** **Fase 4 (D-6, rendimiento)** -- `ReplenishmentService` gana `_evaluar_catalogo` (privado, memoizado por instancia con clave = filtros de alcance + overrides del simulador -- seguro porque el servicio se construye una instancia nueva por request vía `Depends` y la RLS por almacén ya viaja inyectada en `warehouse_repo`); `get_lista_reabastecimiento` pasa a ser un wrapper barato que post-filtra y ordena sobre una COPIA de la lista cacheada (nunca la muta in situ, verificado con un test dedicado). **Verificado en vivo contra `bi_backend` con datos reales del EDW** (hot-reload, sin reconstrucción de imagen): el flujo `resumen+alertas+simular` pasó de **4 llamadas/1,64s** (línea base medida en la Fase 0) a **2 llamadas/0,87s** (las 2 restantes son irreducibles por diseño: actual vs. simulado, cálculos genuinamente distintos) -- **~47% más rápido**, con los mismos números exactos que la línea base (`productos_riesgo_critico=4110`, `costo_total_compra_sugerida=$601.812,60`, `total_articulos_evaluados=8153`): cero cambio de comportamiento, solo de costo. **Fase 7 (guarda de frontera)** -- `test_inventory_module_boundary.py` nuevo, verifica por AST que `engine.py` no importa `app.repositories`/`app.models`/`app.database`/`app.core.config`/`app.ml`/`sqlalchemy` (motor puro real, no solo por convención) y que ninguna de sus funciones recibe un parámetro `db`/`session`; **ajuste sobre el texto original del plan** -- la regla de frontera "nadie importa `engine` directamente" resultó incompatible con la Fase 2 (que exige que `WarehouseService` importe `engine`), corregida en el plan y en la auditoría a "el motor es puro", la invariante que realmente importa. **Continuación en la misma sesión (petición explícita del usuario: "continua"), Fase 6 (frontend) + validación de reconstrucción completa de imágenes:** `BodegaReabastecimiento.tsx` (545 líneas, 5 componentes internos sin exportar) reescrito como contenedor delgado (~55 líneas) con `Tabs` (mismo patrón ya establecido en `DashboardMetas.tsx` para Operación/Config/Simulación/Bitácora -- preferido sobre rutas independientes nuevas, que el requerimiento original del usuario proponía genéricamente, porque las 4 vistas comparten el mismo filtro global de Bodega vía `BodegaFilterBar`; una ruta por vista habría exigido mover ese filtro a un store compartido entre rutas sin ganar nada que las pestañas no dieran ya); 4 componentes nuevos en `frontend/src/components/bodega/`: `ReplenishmentListaInteligente.tsx` (Centro de Decisiones + Lista, se mantienen juntos porque comparten `horizonteDias`/filtros de riesgo-ABC-XYZ), `ReplenishmentSimuladorPanel.tsx`, `ReplenishmentPropuestasPanel.tsx` (ambos ganan su propio horizonte local, antes heredado del padre) y `ReplenishmentPoliticaPanel.tsx` (pestaña "Configuración", gerencia/administrador) -- sin cambio de contrato HTTP, mismos hooks/endpoints de antes. **Reconstrucción completa de ambas imágenes** (`docker compose build backend`/`frontend`, no solo hot-reload/HMR como el resto de la sesión): `bi_backend` arrancó limpio con 4/4 modelos ML y, en el camino, **aplicó en vivo la migración `0018_quitar_anomalias`** que la actualización anterior de este documento dejó explícitamente pendiente -- cierra ese pendiente sin acción adicional, era exactamente la "próxima reconstrucción de `bi_backend`" que se esperaba; `bi_frontend` arrancó limpio. Validado extremo a extremo: `pytest backend/tests/unit` 382 passed (363 base + 19 nuevos, sin regresiones); `tsc --noEmit` limpio, `oxlint` sin advertencias nuevas (las 5 preexistentes son de archivos no tocados), `npm run build` limpio (bundle 1.225,34 kB, en línea con sesiones previas). **Con esto, Fases 0-4, 6 y 7 del plan de 7 fases quedan completas; solo la Fase 5 (decisión de negocio sobre unificar los dos motores de punto de reorden, con la evidencia cuantitativa del 49,7% de discrepancia ya lista para gerencia) queda explícitamente diferida, no por descuido.** Actualización previa de la misma sesión (Módulo de Gestión de Inventario y Reabastecimiento (MGIR), Fases 0-4/7 -- ver el párrafo anterior, que ya incluye este detalle completo. Actualización previa 2026-08-04 (Decomisión del modelo `anomaly` y `churn_rf` a un solo algoritmo, `docs/auditoria/51_decomision_anomaly_y_churn_unico.md` -- dos peticiones explícitas del usuario en la misma sesión, confirmadas con preguntas de alcance antes de tocar código. **(1) Decomisión completa de `anomaly` (Isolation Forest):** mismo criterio que `sales_rf`/`goals_rf` (auditorías 49/20) -- sin reemplazo funcional, no solo ocultado en el frontend. **Modelos ML del proyecto: de 5 a 4** (`demand_rf`, `churn_rf`, `segmentation`, `association`). Retirado de punta a punta: entrenamiento (`ml/src/training/train_anomaly_detection.py` borrado, su llamada en `ml/main.py::run_ml_pipeline` y su entrada en `ml/retrain_all.py::ENTRENADORES` retiradas, `fetch_transactions_for_anomalies`/`MUESTRA_ANOMALIAS` borrados de `ml/src/data/make_dataset.py`, `ml/contracts/models/anomalies.json` borrado, `ml/models/{anomalies.pkl,anomalies.meta.json}` y su versión archivada borrados, entrada `anomaly` de `ml/models/registry.json` retirada); serving (`_MODEL_FILES`/`MODEL_DISPLAY_NAMES` de `model_loader.py`, función `detect_anomalies` de `inference.py`, `PredictionService.get_anomaly_status`, `AnomalyFeatures`/`get_transaction_features` de `prediction_repository.py`); API (el subsistema completo de triage del panel de Administrador retirado -- endpoints `GET/PATCH /admin/anomalies*`, modelo ORM `AnomaliaRevision`, su repositorio/servicio dedicados, schemas Pydantic, wiring en `dependencies.py`, y **migración `0018_quitar_anomalias`** que hace `DROP TABLE public.anomalias_revisiones`, creada en la baseline `0001` -- pendiente de aplicar contra `bi_postgres_edw` real en la próxima reconstrucción de `bi_backend`, no aplicada en esta sesión de edición); `training_service.py` pierde `"anomaly"` de `CLAVES_VALIDAS` (el panel MLOps de administrador ya no lo ofrece para reentrenar). **Consumidor no anticipado en el pedido original, retirado también por depender 100% de `anomaly`** (documentado como hallazgo, no solo cosmético, mismo patrón que la decomisión de `sales_rf`): `GoalMLService._detectar_meses_atipicos` corría el modelo sobre transacciones del vendedor para pesar menos (no excluir) los meses con comportamiento atípico dentro del motor de metas v2 -- eliminado junto con `GoalRepository.get_vendor_transactions_history`/`VendorTransactionFeatures` (exclusivos de esa señal); el parámetro genérico `meses_atipicos_ml`/`PESO_MES_ATIPICO_ML` de `goal_calculation_engine.py` se **conserva** (mecanismo del motor v2, no exclusivo de este modelo -- retirarlo habría sido un cambio de contrato más amplio que decomisionar el modelo en sí), documentado en el código como inerte hasta que otra fuente lo alimente; el campo `meses_atipicos_ml_detectados` de la API queda siempre en `0`. Frontend: `DashboardAdmin.tsx` pierde la tarjeta "Detector de Anomalías Transaccionales" y la sección "Triage de Anomalías", sin reemplazo (`hooks/admin.ts`, `services/admin.ts`, `types/admin.ts` limpiados en paralelo); bundle de producción bajó de 1.232 kB a 1.224 kB. **(2) `churn_rf` a un solo algoritmo fijo:** antes `train_churn_model` (`ml/src/training/train_churn_prediction.py`) llamaba a la función compartida `find_best_classification_model` (`ml/src/training/model_selector.py`), que hacía competir 4 familias (RandomForest/XGBoost/LightGBM/CatBoost) vía `RandomizedSearchCV` en cada reentrenamiento -- el campeón real y consistente ya era `CatBoostClassifier` (confirmado en `ml/models/churn.meta.json`/`ml/REPORTE_MEJORA_MODELOS.md`). Se escribió un entrenamiento dedicado directamente en `train_churn_prediction.py` (un solo `RandomizedSearchCV` sobre `CatBoostClassifier`, mismo espacio de hiperparámetros y `cv` que tenía dentro de la competencia) en vez de modificar `find_best_classification_model`, que se **conserva intacta** porque también la usa `train_cross_sell_ranker.py` (7º modelo del proyecto, contrato `draft`, sin campeón fijo que justifique el mismo cambio). Validado extremo a extremo: `pytest backend/tests/unit` 363 passed (0 fallas, sin regresiones -- tests de `_detectar_meses_atipicos`/`detect_anomalies`/triage de anomalías eliminados junto con el código que probaban; `conftest.py`/`test_system_service.py` actualizados para no depender de la clave `anomaly`); `tsc --noEmit`/`oxlint`/`npm run build` del frontend limpios. **Pendiente, explícitamente diferido (no un descuido):** la migración `0018` no se aplicó contra el EDW real en esta sesión (requiere reconstruir `bi_backend`, no realizado); no se corrió un reentrenamiento real de `churn_rf` contra el EDW/contenedor `ml` (sin conectividad en este entorno de edición) -- el cambio queda validado por lectura de código y la suite de tests existente, el próximo `docker run ... python retrain_all.py --model churn_rf` ejercitará el nuevo camino por primera vez. Actualización previa 2026-08-04 (Módulo de Reabastecimiento Inteligente, `docs/features/plan_reabastecimiento_inteligente.md`, auditoría `docs/auditoria/50_reabastecimiento_inteligente.md` -- petición explícita del usuario para auditar y rediseñar la reposición de inventario de Bodega bajo un enfoque de Supply Chain (stock de seguridad, punto de reorden, clasificación ABC/XYZ, riesgo de quiebre), redactado primero como plan completo de 12 fases (F0-F11) y luego ejecutado en esta sesión con el corte de valor mínimo recomendado por el propio plan, F0→F4, dejando F5-F11 explícitamente diferidas (cada una es una sesión propia). **F0 (auditoría obligatoria, solo SELECT contra `bi_postgres_edw` real, sin escribir código):** confirmó que ni `Fact_Compras` (solo `fecha_sk` de factura recibida) ni `Fact_Transferencias` (un solo `fecha_sk`) permiten derivar lead time real -- intentado también verificar `encabezadoordcom`/`renglonesordcom` de SAP como fuente futura, sin conectividad real disponible en este entorno (`ValueError: cannot find context for 'fork'` en el host Windows, `Adaptive Server is unavailable` dentro del contenedor `etl`, documentado como limitación de la sesión, no un hallazgo negativo); distribución ABC real del catálogo (excluyendo `Z-9001`/chatarra, mismo criterio que `demand_rf`): 58 SKUs clase A (2,6%), 294 B (13,2%), 1.875 C (84,2%); los umbrales de coeficiente de variación ya existentes (`BODEGA_CV_ALTA`/`BODEGA_CV_MEDIA`, calibrados para decidir transferencias) dejaban al 96,6% del catálogo en una sola clase XYZ al reutilizarlos para este propósito distinto -- se calcularon cortes propios por terciles reales (`CORTE_XYZ_X=0.39`/`CORTE_XYZ_Y=0.61`); solo el 32,1% del catálogo tiene ≥6 meses de historia en la ventana de 36 meses (confirma que la mayoría del catálogo necesita degradación explícita del método); confirmado por lectura de código que el parámetro `dias_salidas` de `WarehouseRepository.get_inventario_productos` es decorativo -- ninguno de sus 7 llamadores lo sobreescribe; cuantificado el impacto real de cambiar la fórmula de punto de reorden sobre 779 SKUs con venta en 30 días: 54,2% cambia más del 30% respecto al valor actual, confirmando que es una decisión de política real, no un cambio cosmético. **F1 (motor puro, sin I/O, mismo patrón que `commission_variable_engine.py`):** `backend/app/services/replenishment_engine.py` -- `estadistica_demanda` (degrada `estocastico`/`determinista`/`sin_historia` según meses con venta), `stock_seguridad` (`SS = z(nivel_servicio) × σ_demanda × √lead_time`, Silver/Pyke/Peterson; Chopra & Meindl), `punto_reorden`, `evaluar_riesgo` (`critico`/`alto`/`medio`/`bajo`/`sin_demanda`, nunca deja un artículo sin señal de demanda clasificado silenciosamente como seguro -- mismo defecto ya corregido para "Inmovilizado" en la auditoría 42), `clasificar_abc`/`clasificar_xyz`, `resolver_lead_time` (especificidad producto > categoría > proveedor > default, con el origen declarado explícitamente en la salida), `cantidad_sugerida`, `construir_desglose` (arma `razones: list[str]` en lenguaje natural para explicabilidad, regla transversal del plan: "ninguna predicción sin volverse explicable"). 50 tests nuevos (`test_replenishment_engine.py`), suite completa de `pytest backend/tests/unit` en 343 passed sin regresiones. **F2 (configuración editable):** migración `0016_reabastecimiento` -- `public.reabastecimiento_politica` (nivel de servicio por clase ABC, semilla A=97,5%/B=95%/C=90%) y `public.reabastecimiento_lead_time` (producto/categoría/proveedor, con 3 índices únicos parciales de Postgres en vez de una `UniqueConstraint` compuesta rota -- NULL≠NULL en Postgres habría permitido duplicar filas con columnas nulas distintas, defecto real detectado y corregido antes de aplicar la migración); repos/servicios/endpoints CRUD bajo `/analytics/bodega/reabastecimiento/{politica,lead-times}` (solo gerencia/administrador). **F3 (lista priorizada):** `ReplenishmentService.get_lista_reabastecimiento`/`get_resumen`, nueva consulta `WarehouseRepository.get_metricas_reabastecimiento` (reutiliza `_filtros_snapshot`, misma RLS por almacén de RN-B10) que agrega demanda mensual real por SKU, convertida a diaria (`media_diaria = media_mensual / (365,25/12)`, `σ_diaria = σ_mensual / √(365,25/12)`) para alimentar la fórmula de stock de seguridad, que asume unidades diarias. Endpoints `GET /reabastecimiento/{lista,resumen}` (bodega/gerencia/administrador). **F4 (frontend):** página nueva `/bodega/reabastecimiento` (`BodegaReabastecimiento.tsx`) -- Centro de Decisiones (KPIs de riesgo, no de censo: crítico/alto/con recomendación/costo sugerido, regla explícita del plan de no duplicar KPIs financieros ya oficiales del Dashboard Ejecutivo), Lista Inteligente priorizada por RIESGO real (no por volumen de ventas, distinción explícita frente a `/bodega/almacenes`) con fila expandible (`DataTable.renderExpanded`, mismo patrón que el desglose de comisiones) mostrando `razones[]` en lenguaje natural más el desglose numérico completo, filtros de horizonte/riesgo/ABC/XYZ, y panel de configuración de política visible solo a gerencia/administrador. Convive con `/bodega/almacenes` (`/necesidad-compra`, motor determinista de RN-B1/RN-B4) sin reemplazarlo. Validado extremo a extremo: `tsc --noEmit`/`oxlint`/`npm run build` limpios; `bi_backend`/`bi_frontend` reconstruidos; `GET /reabastecimiento/resumen` responde con datos reales contra `bi_postgres_edw` (8.153 artículos evaluados, 4.110 en riesgo crítico, $601.812,60 de compra sugerida, cobertura mediana 42,6 días); `GET /reabastecimiento/lista` confirma el contrato completo (`razones[]`, `lead_time_origen`, `metodo_stock_seguridad`) contra artículos reales (ej. `602432` BATERIA 24 HP S4, cobertura 0,3 días, riesgo crítico). Reglas nuevas RN-RB1..RN-RB5 en `docs/auditoria/02_reglas_negocio_validadas.md` §25. **Continuación en la misma sesión, F5 (explicabilidad por artículo):** nuevo `GET /reabastecimiento/lista/{codart}/explicacion` (`ReplenishmentService.get_explicacion`) -- reutiliza `get_lista_reabastecimiento` con los filtros de alcance pero deliberadamente SIN propagar los post-filtros de riesgo/ABC/XYZ, porque la clase ABC de un artículo depende de la curva de Pareto de TODO el catálogo filtrado: calcularla en aislamiento para un solo `codart` le habría dado una clase distinta a la que ese mismo artículo tiene en la lista completa, rompiendo la invariante de que lista y detalle cuentan la misma historia. `404` explícito si el `codart` no existe en el alcance de filtros actual. Regla nueva RN-RB6 en `docs/auditoria/02_reglas_negocio_validadas.md` §25. 4 tests nuevos (`test_replenishment_service.py`, incluido uno que verifica explícitamente que la clase ABC del detalle coincide con la de la lista completa) -- suite completa en 347 passed, sin regresiones; validado en vivo contra `bi_backend` reconstruido: `GET /lista/602432/explicacion` reproduce exactamente la misma fila que la lista (riesgo crítico, cobertura 0,3 días), `GET /lista/NO-EXISTE-XYZ/explicacion` responde `404`. **Continuación en la misma sesión, F7-F10 (petición explícita del usuario: "continua hasta terminar la implementación"):** **F7 (alertas inteligentes, extienden sin duplicar):** `replenishment_engine.py` gana `detectar_cambio_brusco` (demanda del último mes fuera de `media ± 2σ` del histórico, control estadístico de proceso clásico, exige ≥3 meses de historia y `σ>0`) y `detectar_tendencia_decreciente` (3 meses consecutivos estrictamente decrecientes); `GET /reabastecimiento/alertas` y `ReplenishmentService.get_alertas` las exponen. **Decisión explícita de diseño, documentada en código:** el riesgo de quiebre (🔴 del plan) y el sobrestock (🟠) NO se agregan a la campana -- son el mismo evento real que `stock_critico`/`prediccion_agotamiento`/"Inmovilizado" del generador de Bodega ya existente, solo calculado con una fórmula distinta; duplicarlos habría mostrado 2 avisos por el mismo artículo sin información nueva. `NotificationService._generar_bodega` se extiende (nueva dependencia `ReplenishmentService` inyectada), sin tocar el generador existente. **F8 (simulador what-if, solo lectura):** `POST /reabastecimiento/simular` recalcula el resumen bajo `niveles_servicio`/`lead_time_default_dias` hipotéticos -- refactor de `get_lista_reabastecimiento`/`get_resumen` para aceptar overrides opcionales sin tocar la config persistida; test dedicado que aserta que ningún método de escritura del repositorio de configuración se invoca durante la simulación. Validado en vivo: lead time 7→30 días sube el riesgo crítico de 4.110 a 4.241 artículos y el costo de compra sugerida de $601.812 a $1.162.619; nivel de servicio 97,5-90%→99% en las 3 clases sube el costo a $631.476 sin tocar el riesgo crítico (más stock de seguridad, no más urgencia); en ambos casos `GET /politica` confirmó que la configuración real seguía en la semilla original. **F9 (propuestas de compra persistidas):** migración `0017_propuesta_compra` (`public.propuesta_compra`/`propuesta_compra_linea`) -- `POST /propuestas` congela un snapshot de la Lista Inteligente vigente (solo líneas con cantidad sugerida > 0) con la justificación completa por línea, mismo criterio que `comision_liquidaciones` (no se recalcula al mirarla); máquina de estados cerrada `borrador→aprobada`/`borrador→rechazada` únicamente, una propuesta ya decidida rechaza una segunda decisión (`ValidationError` explícito, confirmado en vivo con 400). **`transferencia_decision` (cierre de H-5, la aprobación de transferencias en `/bodega/almacenes` sigue en `useState` efímero) se deja fuera de esta migración a propósito** -- pertenece a un router/página distintos y se documenta como trabajo de seguimiento explícito, no una omisión silenciosa. **F10 (limpieza del dashboard de Bodega):** `DashboardBodega.tsx` retira G2 (Matriz Rotación×Margen, análisis financiero puro que duplicaba el Dashboard Ejecutivo, H-10 -- reemplazado por un enlace directo a la clasificación ABC/XYZ real de `/bodega/reabastecimiento`), G3 (Top 20 Productos con Mayor Salida, ordenaba por ventas, lo opuesto de priorizar por riesgo de quiebre) y G4 (Distribución de Salidas por Categoría, descriptivo puro sin ninguna acción derivada); el hook `useTopProductos` se conserva solo porque alimenta el selector de producto de G1. **Deliberadamente NO se decomisiona** ningún endpoint backend (`/rotacion-matriz`/`/salidas-categoria`/`/top-productos` siguen sirviendo el reporte de Gerencia y el selector de G1; `/necesidad-compra` sigue activo porque A-0.7 de la auditoría 50 ya había cuantificado que el cambio de fórmula altera más de la mitad del catálogo, decisión de negocio que le corresponde a gerencia, no a un refactor de UI). Reglas nuevas RN-RB6..RN-RB10 en `docs/auditoria/02_reglas_negocio_validadas.md` §25. Validado extremo a extremo: `pytest backend/tests/unit` 366 passed (0 fallas, sin regresiones -- 22 tests nuevos: 10 de los detectores de alertas en `test_replenishment_engine.py`, 12 de servicio -- alertas/simulador/propuestas -- en `test_replenishment_service.py`); `tsc --noEmit`/`oxlint`/`npm run build` del frontend limpios en cada fase (el bundle de producción incluso se redujo tras retirar G2/G3/G4, de 1.245 a 1.232 kB); `bi_backend` reconstruido 3 veces (F7, F8/F9, sin cambios de F10) con arranque limpio y la migración `0017` aplicada en vivo; `bi_frontend` reconstruido 2 veces; flujo completo de propuestas probado en vivo contra `bi_postgres_edw` real (crear con 22 líneas/$70.926,01 → listar → aprobar → re-decidir rechazada correctamente con 400 → `GET` de una propuesta inexistente con 404), artefacto de prueba limpiado de la base real tras validar. **Con esto, F0-F10 del plan de 12 fases quedan completos.** **Pendiente, explícitamente diferido (solo F6, no descuido):** conectar `demand_rf` como fuente de `metodo_demanda` exigiría primero el backtest por SKU (A-0.8 de la auditoría 50, nunca completado por falta de conectividad al contenedor `ml`/SAP en este entorno) que demuestre que el modelo gana al baseline estadístico en el grano `(producto, almacén)` -- y aun si lo hiciera, correr walk-forward de `demand_rf` para las ~8.150 filas del catálogo en cada request de `/lista` sería computacionalmente impracticable (miles de inferencias por request); forzarlo sin esa evidencia repetiría el error que ya llevó a decomisionar `goals_rf`/`sales_rf`. F11 (esta actualización de CLAUDE.md) queda aplicada con este mismo párrafo. Actualización previa de la misma sesión (Decomisión completa de `sales_rf`, `docs/auditoria/49_decomision_sales_rf.md` -- petición explícita del usuario: quitar el gráfico "Histórico y Predicción de Ventas (ML)" del dashboard de Gerencia y reemplazarlo por un gráfico de barras+líneas de ventas reales mes a mes, y "quitar este ML de todo rastro ya que no es muy eficaz... y no aporta mucho al análisis". Confirmado explícitamente por el usuario (pregunta directa): decomisión completa, mismo criterio que `goals_rf` (auditoría 20), no solo ocultar la UI. **Modelos ML del proyecto: de 6 a 5** (`demand_rf`, `churn_rf`, `segmentation`, `association`, `anomaly`). Retirado de punta a punta: entrenamiento (`ml/main.py::train_general_sales_prediction` y su llamada en `run_ml_pipeline`, `ml/retrain_all.py::ENTRENADORES["sales_rf"]`, `ml/src/training/train_sales_prediction.py` borrado, `SalesTimeSerieExtractor.fetch_daily_sales` borrado -- la clase se conserva, sigue sirviendo a los otros 5 modelos --, `ml/contracts/models/sales.json` borrado, `ml/models/{sales.pkl,sales.meta.json}` y su versión archivada borrados, entrada `sales_rf` de `ml/models/registry.json` retirada); serving (`_MODEL_FILES`/`MODEL_DISPLAY_NAMES` de `model_loader.py`, función `predict_sales` de `inference.py` -- **`app/ml/forecasting.py::walk_forward_forecast` se verificó y CONSERVÓ intacto**, `WarehouseService` también lo usa para el pronóstico de demanda de Bodega, no era sales_rf-exclusivo pese a que la mayoría de sus consumidores sí lo eran); API (`GET /gerencia/sales-prediction` y `GET /goals/forecast-cierre` eliminados, `PrediccionVentasResponse`/`MetricasPrediccion`/`ForecastCierreResponse` eliminados de `schemas/analytics.py`, `CLAVES_VALIDAS` de `training_service.py` pierde `"sales_rf"` -- el panel MLOps de administrador ya no lo ofrece para reentrenar). **Dos consumidores dependientes no anticipados en el pedido original, retirados también por ser 100% sales_rf** (documentados en la auditoría 49 como hallazgo, no solo "cosmético"): el panel "Pronóstico de cierre del mes" del dashboard del vendedor (`GoalMLService.forecast_cierre`/`ForecastCierre` dataclass/`_probabilidad_alcanzar_meta` eliminados; `VendorGoalDashboard.tsx` pierde la tarjeta, sin reemplazo -- no se inventó una alternativa no solicitada) y la notificación calculada "Desvío del forecast semanal" de Gerencia (`NotificationService._generar_gerencia` simplificado a solo la divergencia plano-vs-variable de comisiones, que sí sigue siendo real; `NOTIF_DESVIO_FORECAST_PCT` retirado de `config.py`; `PredictionService` retirado por completo de la composición de `NotificationService`, ya no tenía ningún otro uso ahí). **Reemplazo del panel retirado, 100% real, sin ningún modelo:** `AnalyticsRepository.get_evolucion_mensual_ventas` (Venta Neta real -- venta bruta menos devoluciones, mismo criterio G-02 del resto del dashboard -- agregada por mes sobre los últimos N meses, respeta los filtros de vendedor/almacén ya existentes) expuesto en `GET /gerencia/evolucion-mensual`; `DashboardGerencia.tsx` renderiza un `ComposedChart` (barras = venta neta mensual real, línea = promedio móvil de 3 meses, aritmética simple calculada en el frontend sobre esa misma serie, no un modelo) con selector 12/24 meses, reemplazando el `AreaChart` de histórico+predicción, el badge "Modelo ML activo" y el panel lateral "Inteligencia Comercial" (insights/métricas 100% derivados de `sales_rf`). Validado en vivo contra `bi_postgres_edw` real: `pytest backend/tests/unit` 292 passed (0 fallas, sin regresiones -- 8 tests de `forecast_cierre`/`_probabilidad_alcanzar_meta`/`predict_sales` eliminados junto con el código que probaban, 3 fixtures/tests de notificaciones y 1 de `SystemService` actualizados para no listar `sales_rf`); `tsc --noEmit`/`oxlint`/`npm run build` del frontend limpios; `bi_backend` reconstruido, arranque limpio con **5/5 modelos** (`demand_rf, churn_rf, segmentation, association, anomaly`), sin `sales_rf` y sin warnings/errores; `GET /gerencia/evolucion-mensual` responde con Venta Neta real mes a mes (confirmado contra el EDW real: $242K-$371K/mes, feb-jul 2026); `GET /gerencia/sales-prediction` y `GET /ventas/goals/forecast-cierre` confirmados `404`; `GET /system/provenance` confirma exactamente 5 modelos activos, ninguno "Random Forest (Ventas)". **Pendiente/fuera de este alcance, documentado explícitamente en la auditoría 49 (no un descuido):** los notebooks de EDA históricos sobre `sales_rf` (`ml/notebooks/`) se dejan intactos, son artefactos exploratorios que no corren en ningún pipeline; los documentos de auditoría/plan previos sobre el modelo (`docs/auditoria/21_...md`, `docs/features/plan_mejora_modelo_ventas.md`) se conservan como historia, mismo criterio que la decomisión de `goals_rf`. Actualización previa de la misma sesión (Metas y Comisiones -- petición explícita del usuario tras revisar el panel "Fórmula de metas": quitar "Comisión (%)" de la consola de propuestas (sin efecto real -- solo alimentaba `calcular_comision` plano, retirado de toda ruta de servicio real desde la Fase 1 de la actualización previa; se conserva el campo en el backend/BD porque `CommissionSimulationService.simular` -- comparación plano-vs-variable -- todavía lo usa, pero el frontend deja de editarlo/enviarlo); reemplazar el editor de JSON crudo de "Fórmula de metas" por un formulario dinámico (`ETAPAS_CATALOGO` gana `parametros: [{clave,label,tipo,min,max,paso,default}]` por etapa, misma fuente que valida `MetaConfigModuloService.RANGOS_PARAMETROS` -- test nuevo que verifica que ambos coincidan); quitar el botón "Generar Plan con Inteligencia ML" (mal etiquetado -- el motor es estadístico puro, `goals_rf` fue decomisionado hace sesiones) y hacer que la fórmula se aplique sola, en tiempo real, sobre la propuesta que luego se aprueba (`GoalsConsole.tsx` dispara `generate_proposals` con debounce de 600ms al mover el slider de presión comercial o cambiar de período -- `generate_proposals` ya era idempotente sobre filas `PROPUESTA`, nunca toca `APROBADA`/`RECHAZADA`, así que automatizarlo no tenía riesgo nuevo); fusionar la bitácora embebida del panel de fórmula con la pantalla "Bitácora de cambios" (ambas ya leían la MISMA tabla `comision_config_auditoria` con `tabla='metas_config_modulos'` -- el panel embebido era una duplicación exacta, no una fuente de datos distinta; se retiró y se dejó que `useUpdateMetaConfigModulo` invalide la query de `BitacoraPanel.tsx`); mostrar si el vendedor sigue activo (`edw.dim_vendedor.activo`, `CatalogRepository.get_vendedores_activo_bulk`, nuevo, lote sin N+1) junto a cada propuesta. **El hallazgo real de esta sesión, no solo cosmético:** de las 16 etapas del pipeline v3, E9 (capacidad instalada)/E13 (cartera activa)/E15 (cumplimiento histórico)/E16 (volatilidad) tenían un método marcado `implementado=True` en el catálogo -- así que la UI dejaba "activarlas" sin advertencia -- pero JAMÁS estaban conectadas dentro de `GoalMLService._ajustar_meta_por_tipo`: E9 se llamaba siempre con `clientes_activos=None, ticket_promedio=None, frecuencia=None` (un no-op garantizado por diseño de la propia función), y E13/E15/E16 no se invocaban en absoluto pese a que sus funciones puras ya existían y estaban probadas -- activar cualquiera de las 4 en el panel no cambiaba ningún número real, exactamente el "mockup" que el usuario reportó. Corregido de punta a punta: `GoalRepository` gana `get_metricas_cartera_vendedor` (clientes activos del mes más reciente + promedio de la ventana de 6 meses + ticket promedio + frecuencia de compra, agregando `fact_ventas_detalle` real por `(vendedor, mes)`, insumo compartido de E9 y E13, una sola consulta por vendedor aunque ambas etapas estén activas) y `get_cumplimiento_historico_promedio` (promedio real de venta/meta de los últimos N meses del propio vendedor sobre `metas_comerciales_operativas` + `get_vendor_net_sales_period`, insumo de E15); `_ajustar_meta_por_tipo` pasa a recibir la `SugerenciaMeta` completa (antes solo `meses_historico_usados` suelto) para alimentar E16 con `factor_tendencia_aplicado`/`coeficiente_variacion` ya calculados por el motor v2 (E16 reescala la meta por la razón entre el factor de crecimiento penalizado y el original, nunca recalcula la tendencia desde cero). Validado en vivo activando E13 contra `bi_postgres_edw` real: `VEN01`/agosto-2026 pasa de $59.700 a $70.000 al activar el factor de cartera con un rango amplio (0.5-2.0) y vuelve a $65.700 al desactivarlo -- la bitácora (`GET /commission-config/auditoria`) registra ambos cambios correctamente. **E12 (distribución corporativa) y E14 (potencial de mercado) se RETIRARON del catálogo por completo** (migración `0015_metas_pipeline_ajustes`, no solo se ocultaron): ninguna tuvo nunca un método `implementado=True` -- E12 exige `public.metas_corporativas` (inexistente) y E14 exige una fuente de mercado total que ningún ERP transaccional propio puede proveer -- dejarlas en el catálogo era una tarjeta de configuración que el usuario podía abrir pero JAMÁS activar, el mismo patrón de mockup que motivó todo este pedido; el pipeline pasa de "16 etapas" (13 con matemática real + 2 permanentemente inertes + E11) a **14 etapas, todas con al menos un método real**. `pytest backend/tests/unit`: 300 passed (0 fallas -- la única falla preexistente dependiente de la fecha del sistema, documentada en sesiones previas, dejó de reproducirse tras el avance natural del reloj); `tsc --noEmit`/`oxlint`/`npm run build` del frontend limpios; migración `0015` aplicada en vivo contra `bi_postgres_edw` real (`bi_backend` reconstruido, arranque limpio, 6/6 modelos); `GET /meta-config/catalogo` confirmado con 14 etapas exactas, sin E12/E14. **Pendiente, explícitamente diferido (no bloqueante):** las etapas E9/E13/E15/E16 siguen desactivadas en la semilla (igual que antes -- activarlas ahora sí tiene efecto real, pero sigue siendo una decisión de gerencia, no un cambio de comportamiento por defecto de esta sesión); no se tocó el resto de la Fase 8/9/10 del plan (previsualización en vivo del editor más allá del slider ya dinámico, backtest formal, factores opcionales de días hábiles). Actualización previa de la misma sesión: Fase 9.A del plan -- `backend/scripts/backtest_motor_metas.py` (nuevo, herramienta de auditoría, 100% solo lectura) ejecutado en vivo contra `bi_postgres_edw` real, `docs/auditoria/48_backtest_motor_metas.md`. Walk-forward mes a mes sobre los últimos 12 meses cerrados (2025-08..2026-07), población = misma regla de elegibilidad de producción (12 meses, hallazgo H-2 de la actualización anterior) -- 129 observaciones vendedor-mes. Bug real corregido durante la ejecución: el script (proceso standalone, no la app FastAPI) lanzaba `InvalidRequestError: ... 'Role'` al primer query ORM -- `MetaConfigModulo.usuario` referencia `User` por nombre de clase (string), que solo resuelve si `app.models.user` ya se importó alguna vez en el proceso; agregado `import app.database.base` explícito al inicio del script. **Resultado real:** v2 (estadístico puro) mediana 99,5% de cumplimiento, 48,8% de vendedores ≥100%, costo de comisión agregado $109.022,44; v3 (pipeline modular vigente, con E6 madurez/E8 tipo/E11 redondeo activos) mediana 93,2%, 37,2% ≥100%, costo $99.230,67 (-9,0%) -- calculado con `calcular_comision_variable_completa`, el mismo motor real de liquidación, no una aproximación. Ninguno de los dos motores alcanza el rango objetivo 60-70% de vendedores ≥100% sobre esta muestra, pero el resultado agregado está dominado por un solo caso real (`VEN23`, muy poca historia propia): v2 le asignaba metas de apenas $26,80-$294,40 (sin sentido de negocio, cualquier venta mínima disparaba miles de % de "sobrecumplimiento"), v3 las corrige a $8.600-$22.600 mezclando con la mediana del equipo -- confirma que E6 corrige un defecto real de v2, pero con solo 10-11 vendedores por mes ese único caso pesa ~10% de cada muestra mensual, arrastrando la métrica agregada hacia abajo pese a ser una mejora individual real. **No se recalibró la semilla v3 con este resultado** -- documentado explícitamente que el tamaño de muestra es la limitación real, se recomienda repetir el backtest con más meses/vendedores antes de tocar parámetros. Fase 9.B (backtest contra metas aprobadas) sigue bloqueada, solo 2 meses `APROBADA` en este entorno, muy por debajo del umbral de 6-12 que exige el plan para no fabricar evidencia. Actualización previa de la misma sesión: 2 hallazgos reales reportados por el usuario en producción ("las comisiones variables aun no estan funcionando bien para todos los vendedores... revisar si las formulas para las metas y las comisiones son funcionales para todos") y corregidos. **H-1 (traza de comisión inconsistente con el gate):** `calcular_comision_variable_completa` ya forzaba `comision_final=0.0` bajo el umbral de cumplimiento (Fase 2), pero la traza expuesta como "componentes" en el desglose expandible seguía sumando/restando devoluciones y bonos sobre el acumulado sin la compuerta, terminando en un total distinto de cero -- confirmado en vivo con ALMACEN ATAHUALPA/julio-2026: `comisión=$0,00` pero la traza terminaba en `$4.689,34`. Corregido reescribiendo `acumulado_tras_paso` a `$0` desde el paso `multiplicador_cumplimiento` en adelante cuando el gate aplica; test de regresión nuevo. **H-2 (11 de 24 vendedores activos sin ninguna meta):** `get_vendors_with_recent_sales` exigía venta en el mes INMEDIATO anterior -- sin meta, ningún vendedor puede tener comisión. De los 11 sin meta, 9 son genuinamente inactivos (última venta entre 2018 y 2024, correctamente excluidos) pero 2 (`VEN22` oct-2025, `VEN23` dic-2025) tenían actividad real reciente injustamente excluida. Decisión explícita del usuario: ventana de elegibilidad de 12 meses (vendedor activo + venta en los últimos 12 meses respecto al objetivo, no solo el mes anterior). Corregido: la consulta gana `meses_ventana=12` y filtra `v.activo=TRUE` por primera vez. Validado en vivo: regenerando agosto-2026, `registros_creados` pasa de 13 a 15 (exactamente VEN22/VEN23), los 9 genuinamente inactivos siguen sin meta. `pytest backend/tests/unit`: 291 passed (1 falla preexistente ajena); `bi_backend` reconstruido dos veces, arranque limpio, sin errores en logs. Actualización previa de la misma sesión, Fase 5/6 de `docs/features/plan_motor_metas_v3_y_comisiones_unificadas.md` -- motor de metas v3 modular de 16 etapas, petición explícita del usuario tras el cierre de R-8. **Decisión de ingeniería central:** en vez de reescribir la matemática ya probada de `IQRGoalCalculationEngine` (E1 limpieza/E2 nivel/E3 estacionalidad/E4 tendencia/E5 estabilidad/E10 banda) en funciones independientes -- alto riesgo de introducir un defecto sutil en un motor que ya mueve dinero real y metas validadas en producción -- se le agregaron interruptores (`aplicar_limpieza`/`aplicar_estacionalidad`/`aplicar_tendencia`/`aplicar_estabilidad`/`aplicar_banda`, todos `True` por defecto = comportamiento v2 exacto) y sus parámetros pasan a leerse de la config modular nueva en vez de la plana; las etapas genuinamente nuevas (E7 estrategia/E8 tipo vendedor -- absorbe `COMISION_META_FACTOR_*`, evitando el defecto de doble aplicación ya documentado en la auditoría 46 H-10/E9 capacidad/E11 redondeo/E13 cartera/E15 cumplimiento/E16 volatilidad) se implementaron como funciones puras nuevas en `app/services/goal_pipeline_stages.py`. **Migración `0014_metas_config_modular`** (aplicada en vivo): tabla `public.metas_config_modulos` (una fila por etapa, catálogo cerrado), sembrada exactamente con la configuración recomendada del plan -- E1/E2/E3/E4/E5/E6/E8/E10/E11 activas, E7/E9/E12/E13/E14/E15/E16 desactivadas con su razón visible; `metas_config_parametros` (13 constantes planas, migración `0013`) se conserva sin cambios pero el motor real ya no la lee. **Catálogo cerrado** (`ETAPAS_CATALOGO`) documenta TODOS los métodos que describe el plan, marcando cuáles están realmente `implementado` -- los declarados-pero-no-implementados (`isolation_forest`, `holt_winters`, agrupador `sucursal`/`region`, etc.) exponen su motivo real y `MetaConfigModuloService` **rechaza activarlos** explícitamente, nunca se puede prender en la UI algo sin código real detrás. **E11 (redondeo, múltiplo 100) queda activo en la semilla** -- mejora deliberada, el propio plan excluye a E11 (junto con E6) de la exigencia de equivalencia numérica exacta con v2; las metas de agosto-2026 pasaron a ser múltiplos de 100 tras regenerar en vivo. **Bug real encontrado y corregido durante la validación en vivo:** el primer intento de la migración serializaba `parametros` con `json.dumps()` antes de insertarlo en una columna `JSONB` -- SQLAlchemy ya serializa dicts automáticamente, así que el resultado era una cadena JSON citada dentro del JSONB, no un objeto; `GET /meta-config/modulos` fallaba 500 al validar contra Pydantic; corregido pasando dicts crudos y reaplicando la migración (downgrade+upgrade) para sanear los datos ya insertados. **API nueva:** `GET /meta-config/catalogo`, `GET/PUT /meta-config/modulos/{etapa}`. **Frontend:** `MetaConfigPanel.tsx` reescrito de una tabla plana de 13 parámetros a 16 tarjetas expandibles por etapa (activo/método/parámetros JSON). Validado en vivo: `pytest backend/tests/unit` 290 passed (1 falla preexistente ajena, +39 tests nuevos); migración aplicada y saneada contra `bi_postgres_edw` real; `bi_backend`/`bi_frontend` reconstruidos, arranque limpio; regenerando agosto-2026 -- 13 registros (24 vendedores activos, incluidos los 4 de 0 meses de historia de la Fase 7), todas las metas ahora múltiplos de 100, `trazabilidad_calculo.version='v3'` y `config_snapshot` poblados y confirmados con `SELECT` directo; `tsc`/`oxlint`/`npm run build` limpios. **Pendiente de esta fase, explícitamente diferido:** previsualización en vivo del editor (Fase 8 punto 3); formato de trazabilidad granular `etapas: []` de §20 (hoy solo `config_snapshot`, sin el detalle paso a paso -- refactorizar el motor para emitirlo se evaluó como riesgo innecesario); E12 distribución corporativa (requiere `public.metas_corporativas`, que el plan mismo documenta como inexistente); wiring de datos reales de Cartera360 para que E9/E13 tengan algo que ajustar (las funciones están listas y probadas, pero `GoalMLService` no consulta hoy clientes activos/ticket/frecuencia); reordenamiento §18.bis (banda debería ir DESPUÉS de madurez/tipo/capacidad según el plan -- se mantuvo el orden heredado del v2, banda dentro del motor antes de E6-E9, para no arriesgar los números de E6 ya validados en producción la sesión anterior); Fase 9 (backtest formal, script `backend/scripts/backtest_motor_metas.py`). Actualización previa de la misma sesión, cierre del gap de 0 meses de historia dejado explícitamente pendiente en la actualización anterior: `GoalRepository.get_active_vendors_without_history` (nueva) trae los vendedores activos que jamás registraron una venta -- confirmados en vivo los 4 exactos de la auditoría 47 A-0.4, `VEN05`/`VEN06`/`VEN08`/`VEN20` -- y `GoalMLService.generate_proposals` los incorpora al lote (antes `get_vendors_with_recent_sales`, que exige venta el mes anterior, nunca los traía) asignándoles el benchmark puro del equipo vía `ajustar_meta_por_madurez(meses_antiguedad=0)` sin invocar el motor IQR (que exige histórico y lanzaría `ValidationError`); nuevo método estadístico `sin_historico` en la trazabilidad persistida. Validado en vivo regenerando agosto-2026 contra `bi_backend` reconstruido: `registros_creados` pasa de 9 a **13** -- exactamente los 4 vendedores nuevos, cada uno con `monto_meta=$35.490,94` (mediana real del equipo) y `metodo_estadistico=sin_historico`/`meses_historico_usados=0` -- sin errores en logs. 2 tests nuevos en `test_goal_ml_service.py` (benchmark asignado correctamente, y un vendedor que apareciera en ambas listas a la vez no se duplica); `pytest backend/tests/unit`: 253 passed (1 falla preexistente ajena, ya documentada). **Con esto, los 24 vendedores activos reciben una meta sin excepción -- cierra R-8 por completo dentro del alcance de esta sesión** (la madurez gradual de la Fase 7/E6 ya cubría 1-23 meses; este cambio cubre el único caso que quedaba, 0 meses). Actualización previa de la misma sesión: Fase 4 y Fase 7-parcial de `docs/features/plan_motor_metas_v3_y_comisiones_unificadas.md`. **Fase 4 (R-4, simulador):** `CommissionSimulationService.reconstruir_mes_especifico` gana el parámetro `usar_configuracion_de_hoy` (default `true`, compatibilidad) -- `false` = "reconstrucción fiel", usa la configuración vigente AL CIERRE del período (idéntica a `CommissionService`), en vez de siempre "hoy" sin decirlo (causa real de que "el simulador no concuerda con las comisiones"); la respuesta ahora expone `modo` (`reconstruccion_fiel`/`config_actual`/`proyeccion`) explícito. Nuevo test de paridad (`test_reconstruccion_fiel_coincide_con_el_calculo_real`) y validación en vivo: `VEN01`/julio-2026 da **$1.613,03** en ambos caminos, centavo a centavo. `CommissionSimulationPanel.tsx` gana el selector "Configuración a usar" y los textos explicativos se separan por modo real (antes asumían siempre "vigente hoy"). **Fase 7, solo la etapa E6 (madurez, R-8) -- el resto del pipeline v3 de 16 etapas NO se implementó, ver "Pendiente" abajo:** nueva `ajustar_meta_por_madurez` (`goal_calculation_engine.py`) reemplaza el escalón único que tenía `GoalMLService._ajustar_meta_por_tipo` (basado en `fecha_ingreso`, cliff binario a los `COMISION_VENDEDOR_NUEVO_MESES`=3) por una transición GRADUAL de 4 tramos (0 meses=benchmark puro / 1-5=mezcla lineal `w=meses/6` / 6-23=mezcla fija 80/20 / ≥24=propio puro), usando `meses_historico_usados` del propio motor v2 como señal de madurez (más confiable que `fecha_ingreso`, que ni vive en `edw.dim_vendedor`). Nuevos settings `COMISION_VENDEDOR_NUEVO_MESES_MADUREZ`(6)/`_MADURO_MESES`(24)/`_PESO_PROPIO_INTERMEDIO`(0.80). Validado en vivo: regenerando agosto-2026, `VEN24` (10 meses de historia real, confirmado en la auditoría 47 A-0.4) pasa de $32.379,10 a $30.646,62 -- antes el escalón no aplicaba ningún benchmark a un vendedor con 10 meses. `pytest backend/tests/unit`: 250 passed (1 falla preexistente ajena); `bi_backend` reconstruido, arranque limpio, sin errores en logs durante las pruebas en vivo. **Pendiente, explícitamente diferido por alcance de esta sesión (no por descuido):** el resto de la Fase 5/6 (pipeline v3 modular completo de 16 etapas + migración `0014_metas_config_modular` + tabla `metas_config_modulos` + registro/catálogo/validación + endpoints CRUD -- el bloque de mayor tamaño del plan, un proyecto multi-sesión en sí mismo); el resto de la Fase 7 (agrupador de benchmark configurable más allá de "equipo completo", capacidad instalada E9); resto de Fase 8 (pestaña "Fórmula de metas" reescrita sobre el modelo modular con previsualización -- depende de la Fase 6, que no se aplicó); Fase 9 (script de backtest formal); Fase 10 (factores opcionales de cartera activa/meses no representativos/tope de variación intermensual). **Gap real identificado y NO resuelto en esta sesión:** los 4 vendedores con 0 meses de historia (`VEN20`/`VEN05`/`VEN06`/`VEN08`, auditoría 47 A-0.4) siguen sin recibir ninguna meta -- `GoalRepository.get_vendors_with_recent_sales` solo trae vendedores con venta en el mes anterior al período objetivo, así que nunca entran al lote de `generate_proposals`, y `GoalMLService.suggest_goal` lanza `ValidationError` ante histórico vacío incluso si entraran. La etapa E6 nueva SÍ resuelve el caso de vendedores con 1-23 meses (la mayoría del hallazgo de R-8), pero el caso de 0 meses requiere ampliar la consulta de vendedores elegibles y un camino de fallback que nunca llame al motor IQR -- documentado como trabajo futuro concreto, no una vaguedad. Actualización previa de la misma sesión (Fases 0-3): `docs/features/plan_motor_metas_v3_y_comisiones_unificadas.md` -- plan consolidado a partir de 14 requisitos explícitos del usuario (R-1..R-14) sobre Metas y Comisiones: la comisión debía dejar de ser plana y salir del motor variable (R-1), solo comisionar si se alcanza la meta (R-2), mostrar la variable en "Cumplimiento real y comisión por vendedor" (R-3), corregir el simulador (R-4), corregir los tramos de cumplimiento (R-5), investigar comisiones desproporcionadas -- caso real `VEN01`/julio-2026, $26.110,43 de los cuales $25.444,91 (97,4%) eran bonos (R-6), quitar los factores de crédito (R-7), tratar vendedores/almacenes sin histórico (R-8), una fórmula de metas "probada en otras empresas" tipo motor modular de 18 etapas (R-9/R-12), bitácora en pestaña propia y de solo lectura (R-11), más el backtest y factores opcionales ya planificados (R-13/R-14, `docs/features/plan_backtest_y_factores_opcionales_metas.md`, ahora marcado como superado y absorbido). **Fase 0 (auditoría previa obligatoria, solo `SELECT` contra `bi_postgres_edw` real, `docs/auditoria/47_metas_v3_y_comisiones_unificadas.md`):** cumplimiento real de julio-2026 con mediana 92,9% y solo 33% de vendedores ≥100% (confirma R-9, pero matiza la lectura inicial: 6 de 9 vendedores caen justo bajo el 100% pero SOBRE el 90%, así que el gate de R-2 no habría dejado a la mayoría en $0 -- solo a `VEN24`, 61,3%); 42% de los vendedores activos (10 de 24) tiene menos de 6 meses de historia, 4 con 0 meses (confirma R-8, más severo de lo esperado); causa raíz de `VEN01` ya documentada en la auditoría 45 (bono de cliente nuevo sin techo, $50/cliente, mostrador con 55% de compra única) -- confirmada, no nueva. **Aplicado de punta a punta (Fases 1-3, backend + frontend, sin tocar el motor de metas todavía -- eso es la Fase 5/6, deliberadamente diferida a una sesión con más margen dado que exige una migración de esquema nueva y 16 etapas nuevas):** **Fase 2 (R-2/R-5/R-6, el cambio de mayor impacto económico):** `commission_variable_engine.calcular_comision_variable_completa` gana una compuerta de cumplimiento sobre la comisión COMPLETA -- antes `bonos` era un paso `sumar` posterior al `multiplicar` de `multiplicador_cumplimiento`, así que un vendedor bajo el tramo "Sin comisión" (0.0×) igual cobraba el 100% de sus bonos (RN-CM16 era falsa en la práctica para ese componente); ahora, si el tramo resuelto tiene `multiplicador<=0`, la comisión final se fuerza a $0 bonos incluidos. Nuevo techo relativo de bonos (`COMISION_BONO_TOPE_PCT_SOBRE_BASE`, semilla 100%, aplicado en `commission_bonus.calcular_bonos_periodo`) acota la suma de los 3 bonos como % de la comisión ya afectada por tipo/cumplimiento -- sin este techo, el bono de cliente nuevo (sin límite, `$50/cliente`) podía superar en varias veces la base real de un vendedor de mostrador con alta rotación de compradores ocasionales. **No implementado en esta pasada** (documentado como decisión de alcance, no como olvido): discriminar el bono de cliente nuevo por perfil mostrador/agencia habría requerido una categoría nueva en el catálogo cerrado `tipo` de `comision_config_vendedor` (hoy solo `externo`/`interno`/`jefe_agencia`) y tocar 3 `CHECK` constraints -- el techo relativo ya resuelve estructuralmente el caso reportado sin esa migración. **Fase 3 (R-7):** `commission_engine._factor_credito` es ahora neutro (1.0) siempre -- la auditoría 30 (H4) ya había confirmado que solo hay datos reales de plazo de crédito para 0 y 30 días en el EDW, sin discriminar nada real; `CommissionService`/`CommissionSimulationService`/`commission_variable_engine` dejan de consultar `comision_factores_credito` (una consulta menos por período); pestaña "Factores de crédito" retirada de `CommissionConfigPanel.tsx` junto con sus hooks/tipos/llamadas de servicio -- los endpoints `GET/PUT /commission-config/credito` y la tabla se CONSERVAN por decisión de alcance (integridad de snapshots congelados que puedan referenciarla), sin nuevo consumidor. **Fase 1 (R-1/R-3, decisión de negocio ya tomada por el usuario en este mismo pedido, no diferida a gerencia):** el esquema plano (`commission_engine.calcular_comision`) queda retirado de toda ruta de servicio -- `VendorCommissionRow`/`MiComision` pierden los campos duplicados `comision_variable`/`nivel_variable`/`pct_cumplimiento_variable`/`tramo_variable`/`modo_comision`; `comision_devengada` es ahora SIEMPRE la comisión variable (con la compuerta y el techo de la Fase 2 ya aplicados), y `nivel` es el tramo real configurable (auditoría 45), no el enum plano de 4 niveles fijos. `COMISION_MODO` pasa de 3 valores (`plana`/`sombra`/`variable`) a 2 (`sombra` default/`variable`) -- el rollback de emergencia deja de ser "volver al esquema plano" y pasa a ser "sombra" (calcula y muestra en todos los paneles, no persiste snapshot oficial); un valor `plana` ahora lanza `KeyError` explícito en vez de degradar en silencio. `get_my_commission` (panel del vendedor) calcula la variable SIEMPRE, alineado con `get_commission_tracking` desde la auditoría 46 (antes solo la calculaba en modo "sombra"/"variable", dejando al vendedor sin cifra real en modo "plana"). **Frontend:** `CommissionTracker.tsx` reescrito con una sola columna de comisión (antes dos, "plana" y "variable" condicionada) y fila expandible con el desglose de 7 componentes (`renderExpanded`, mismo patrón que `CommissionSimulationPanel`); `VendorGoalDashboard.tsx` pierde el panel dual "esquema plano vs. variable" (`PANEL_VARIABLE_CONFIG`/`NIVEL_CONFIG`), fusionado en una sola tarjeta "Comisión" con el desglose "ver cómo se calculó" integrado; la bitácora de cambios (antes pestaña `auditoria` escondida DENTRO de `CommissionConfigPanel`) se extrae a `BitacoraPanel.tsx`, componente nuevo, promovido a pestaña de primer nivel "Bitácora de cambios" en `DashboardMetas.tsx` junto a Operación/Config/Simulación -- de solo lectura (R-11), ahora rotulada para cubrir ambos dominios (comisiones y metas, aunque metas todavía no escribe en esa bitácora porque su config modular es la Fase 6, no aplicada). Bug real corregido en el camino: `cv.pct_cumplimiento` puede ser `None` en un snapshot congelado ANTES de la auditoría 45 (campo nuevo con default `None`) -- `get_my_commission` comparaba `None >= 100` y reventaba con `TypeError` en cuanto se ejercitó con datos de prueba reales; corregido derivando `pct_cumplimiento` de venta/meta como respaldo. Validado extremo a extremo: `pytest backend/tests/unit` (240 passed, 1 falla preexistente ajena ya documentada -- 15 tests reescritos/actualizados en `test_commission_engine.py`/`test_commission_service.py`/`test_commission_simulation_service.py` para reflejar el diseño nuevo); `tsc --noEmit`/`oxlint` del frontend limpios; `bi_backend` reconstruido con arranque limpio (6/6 modelos ML); probado en vivo contra datos reales de julio-2026 con el usuario `gerencia@empresa.com` -- `VEN13` (82,25% cumplimiento) pasa de cobrar sus bonos íntegros a `comision_devengada=$0,00` (la compuerta actuando de verdad); `VEN01`/ALMACEN EL REY (el caso reportado, 99,01% cumplimiento) pasa de $26.110,43 (97,4% bonos) a **$1.613,03** (bonos acotados a $929,33 por el techo), sin errores en logs. **Pendiente de este plan, explícitamente diferido por alcance de esta sesión (no por descuido, cada uno documentado en el plan):** Fase 4 (corrección del simulador -- causa aislada por lectura de código en A-0.6 pero no ejercitada con una comparación en vivo completa); Fase 5/6 (motor de metas v3 modular de 16 etapas + migración `0014_metas_config_modular` -- el cambio de mayor tamaño del plan, requiere su propia sesión); Fase 7 (madurez/benchmark, depende de la Fase 5); Fase 8 restante (pestaña "Fórmula de metas" reescrita sobre el modelo modular, con previsualización -- depende de la Fase 6); Fase 9 (backtest formal, script `backend/scripts/backtest_motor_metas.py`); Fase 10 (factores opcionales de cartera activa/meses no representativos/tope de variación intermensual). El motor de metas (`goal_calculation_engine.py`, v2) NO se tocó en esta sesión -- las metas generadas hoy siguen calculándose exactamente igual; solo cambió cómo se LIQUIDA la comisión sobre el cumplimiento de esas metas. Actualización previa 2026-07-31 (Motor de Metas Comerciales v2 -- fórmula configurable, estacional y con fundamentación de negocio, `docs/features/plan_motor_metas_configurable.md`, auditoría 46, `docs/auditoria/46_motor_metas_configurable.md`. Pedido del usuario, 9 partes sobre el panel de Metas: (1) revisar exactamente la fórmula de la meta -- qué valores toma, bajo qué criterios; (2) hacerla editable/manejable por el gerente, como ya lo es la de comisiones; (3) usar meses anteriores del mismo año + el mismo mes de años anteriores, y degradar explícitamente el método cuando no hay suficiente información; (4) las metas eran demasiado elevadas -- valores que un vendedor no alcanza ni en un mes bajo ni en uno normal; (5) adoptar una fórmula usada en otras empresas, con fundamentación documentada (requisito de tesis); (6) que un mes atípico (excepcionalmente bueno) no eleve la meta de los meses siguientes; (7) considerar otros factores para un buen sistema de metas; (8) que "ver cómo se calculó" muestre los valores REALES con los que se calculó la meta del vendedor elegido; (9) que "Comisiones devengadas" incluya también la comisión variable. Auditoría previa obligatoria (Fase 0, solo `SELECT` + ejecución en modo lectura del motor real dentro de `bi_backend`): confirmó H-1 estructuralmente -- `GoalMLService.suggest_goal` no recibía `anio`/`mes` en absoluto, el motor v1 siempre inferí­a "el mes siguiente al último dato disponible" en vez del período que Gerencia realmente pedía; cuantificó con un caso real (no una hipótesis) el síntoma reportado por el usuario -- recalculando hoy la meta de agosto-2026 de `VEN13` con el motor v1 el resultado es $76.941,13, un 58% menos que la meta persistida ($121.359,95) para el mismo vendedor/período, sin forma de reconstruir la diferencia porque el motor v1 no guardaba ninguna traza del cálculo (H-5); la mediana de cumplimiento del único mes cerrado disponible (julio-2026, 9 vendedores) es 95,7% -- no una ruptura agregada catastrófica, pero sí confirma mecanismos concretos de sobre-ajuste (H-3: ningún techo posterior a los factores de negocio -- rango teórico hasta 1,79× la tendencia reciente; H-4: un mes atípico reciente se filtraba sin atenuar a la tendencia, que además era el ancla del único guardarraíl existente); el índice estacional agregado de la empresa (9 años de histórico) resultó moderado (rango 0,913-1,081) -- la estacionalidad real del negocio NO es la causa principal de metas infladas, confirmando que el diseño debía priorizar un techo/piso final sobre la meta, no solo el ajuste estacional; viabilidad de estacionalidad multi-anual por vendedor confirmada para la mayoría (81,1% de los pares vendedor×mes-calendario ya tienen ≥2 años de historia con ventana de 24-36 meses, mejor que la hipótesis inicial del plan). Aplicado de punta a punta: **motor v2** (`goal_calculation_engine.py`, evolucionado en el mismo archivo en vez de mantener dos motores paralelos -- decisión de simplicidad de esta sesión, documentada como tal) -- reemplaza "promedio del mismo mes de años previos + promedio de últimos 4 meses combinados 50/50 sin ningún tope posterior" por la descomposición clásica de series de tiempo (nivel robusto desestacionalizado × índice estacional ratio-to-moving-average normalizado, propio del vendedor con respaldo del índice de toda la empresa mes a mes cuando falta historia × factor de tendencia reciente sobre la serie YA desestacionalizada) acotada por una **banda de alcanzabilidad sobre la meta FINAL** (después de presión comercial y factor de tipo de vendedor, no antes); fundamentación bibliográfica -- quota setting bottom-up (Zoltners, Sinha & Lorimer, *The Complete Guide to Sales Force Incentive Compensation*) + descomposición nivel-estacionalidad-tendencia (Hyndman & Athanasopoulos, *Forecasting: Principles and Practice*); período objetivo explícito (`anio_objetivo`/`mes_objetivo` pasados por el llamador, nunca inferidos, corrige H-1); escalera de degradación de 4 peldaños etiquetada en `metodo` (`estacional_propio_v2`/`estacional_empresa_v2`/`tendencia_robusta_v2`/`equipo_prorrateado_v2`, este último usando la MEDIANA del equipo, no el promedio que un vendedor grande distorsiona); `unidades_meta` pasa por el mismo motor robusto (antes "unidades del mes anterior × presión" sin ningún filtro, H-7). **Trazabilidad real** (H-5) -- `metas_comerciales_operativas` gana `trazabilidad_calculo` (JSONB, migración `0013_motor_metas_v2`); `GET /gerencia/goals/meta-sugerida?anio=&mes=` devuelve la traza EXACTA persistida junto a la meta ya generada (`es_trazabilidad_persistida=true`), nunca un recálculo con configuración de hoy -- solo cae a cálculo en vivo (etiquetado) para metas legado o períodos sin generar; `generate_proposals` siempre fuerza recálculo fresco (nunca reutiliza una traza vieja al regenerar). **Configuración editable** (requisito 2) -- `public.metas_config_parametros` (13 parámetros, fila viva por clave sin vigencia histórica -- decisión deliberada distinta de `comision_tramos_cumplimiento`: una meta ya generada guarda su propia trazabilidad completa, así que un cambio de parámetro solo afecta metas futuras, nunca necesita reconstruir "la config de hace 3 meses"), sembrada con la semilla recomendada por la auditoría 46 (ventana 36 meses, banda 0.85-1.20, etc.); `MetaConfigRepository`/`MetaConfigService` con validación de rangos (nunca un `banda_max=5.0` que anule el guardarraíl) y bitácora reutilizando `comision_config_auditoria`; endpoints `GET/PUT /gerencia/goals/meta-config/parametros`; pestaña nueva "Fórmula de metas" en el panel de Gerencia (`MetaConfigPanel.tsx`) con edición inline y bitácora de cambios visible. **Comisión variable siempre visible** (requisito 9, H-8, independiente del resto -- confirmado en A-0.6 que el gap era 100% real y activo: `COMISION_MODO=plana` en este entorno hacía que la columna nunca se renderizara) -- `CommissionService.get_commission_tracking` calcula la comisión variable de TODOS los vendedores SIEMPRE, sin importar `COMISION_MODO` (que pasa a controlar solo si el cálculo se persiste como snapshot oficial, nunca si se muestra), resolviendo fórmula/matriz/crédito/tramos de cumplimiento UNA vez por período (mismo patrón de pre-resolución que `CommissionSimulationService`, no una consulta por vendedor); la tabla "Comisiones devengadas" gana `% cumplimiento`/tramo real (los tramos configurables de la auditoría 45, no los 4 fijos del esquema legacy). **Bug corregido en el camino, no reportado por el usuario:** el slider de "Presión Comercial" del panel (H-10) recalculaba en el frontend `(monto/1.1) × (1+presión/100)`, asumiendo que la meta mostrada siempre traía un 10% incorporado -- falso para vendedores internos (×0.95) o nuevos (mediana del equipo); corregido mostrando siempre el monto que el backend ya calculó, sin previsualización falsa (para ver otra presión hay que regenerar). Reglas nuevas RN-MT1..RN-MT6 en `docs/auditoria/02_reglas_negocio_validadas.md` §24. Validado: `pytest backend/tests/unit` (motor v2 reescrito con 35 tests nuevos/actualizados en `test_goal_calculation_engine.py` -- período objetivo explícito en cada mes calendario incluido diciembre→enero, índice estacional normalizado, banda de alcanzabilidad actuando como techo Y como piso, cada peldaño de la escalera de degradación, un pico de un solo mes no domina la meta; 238 passed, 1 falla preexistente ajena y dependiente de la fecha del sistema, ya documentada en sesiones previas); `pytest backend/tests/unit/test_commission_service.py`/`test_commission_engine.py`/`test_commission_simulation_service.py`/`test_commission_variable_engine.py` sin regresiones (63 passed). Pendiente de esta sesión (no bloqueante, documentado explícitamente): aplicar la migración `0013` contra `bi_postgres_edw` real y validar en vivo contra `bi_backend` reconstruido; `tsc --noEmit`/`oxlint`/`npm run build` del frontend; `pytest backend/tests/integration -k "meta or goal or commission"`; backtest formal de 12 meses cerrados (bloqueado por el mismo hallazgo ya documentado en la auditoría 45: este entorno solo tiene metas aprobadas de 2026-07/08, sin historial previo con el que comparar agregadamente -- el backtest queda pendiente de datos reales acumulados). Los factores opcionales de la Fase 6 (días hábiles del mes, cartera activa del vendedor, exclusión de meses no representativos, tope de variación intermensual) quedan sin implementar en esta sesión -- documentados como parámetros futuros, no activados por decisión de alcance. Actualización previa 2026-07-31 (Sobrecumplimiento, umbral de pago al 90% y desglose de la comisión en el simulador -- `docs/features/plan_comisiones_sobrecumplimiento_umbral_y_desglose.md`, auditoría 45, `docs/auditoria/45_sobrecumplimiento_umbral_y_desglose.md`. Pedido del usuario, 3 partes: (1) definir cómo comisiona un vendedor que excede su meta y qué beneficios recibe -- antes un escalón plano único de 1.2× para todo lo que pasara de 100%, sin distinguir 101% de 250%; (2) exponer en la tabla del simulador "cómo se construye la comisión, cuánto gané de cada cosa" (venta, cobranza, etc.), tanto para la reconstrucción de un mes específico como para la proyección al próximo mes, porque había vendedores con valores demasiado elevados; (3) un vendedor comisiona solo desde el 90% de cumplimiento de su meta (antes el tramo 80-89% pagaba con multiplicador 0.7×). Auditoría previa obligatoria (Fase 0, solo `SELECT` + ejecución en modo lectura del motor real dentro de `bi_backend`) antes de tocar código de dinero real: el "valor demasiado elevado" reportado por el usuario **no** se explicaba por ninguna hipótesis inicial sobre cobranza/contado de agencia/denominador de tasa -- el hallazgo real (H-5, no anticipado) es que el bono de "cliente nuevo/reactivado" ($50/cliente, `COMISION_BONO_CLIENTE_NUEVO`) no distingue vendedores de cartera de códigos de venta de mostrador con alta rotación de compradores ocasionales: `VEN01` ("ALMACEN EL REY", un mostrador, no un vendedor de cartera -- 31.107 clientes distintos de por vida, 55% de compra única) generó **$25.600** de este bono en un solo mes (512 de 677 clientes calificados "nuevos"), muy por encima de su propia base comisionable de líneas+cobranza ($514,57). Presentado al usuario, decisión explícita: **no tocar el bono de cliente nuevo** (queda documentado y visible en el desglose, sin corrección de código en esta sesión) y **proceder** con la semilla de tramos propuesta pese a que este entorno de desarrollo no tiene historial de metas aprobadas de meses anteriores (`public.metas_comerciales_operativas` solo tiene filas para 2026-07/08) para medir el impacto agregado con 12 meses reales -- el mecanismo es correcto por diseño (tabla con vigencia, no retroactivo, sin snapshots `oficial` que arriesgar hoy) y el costo real se mide en vivo una vez activo. Aplicado: **motor** -- `commission_engine.py` gana `TramoCumplimiento`/`resolver_tramo_cumplimiento` (función pura) y `ETIQUETAS_COMPONENTES_FORMULA` (fuente única de etiquetas); reemplaza los 4 tramos fijos (`UMBRAL_*`/`COMISION_MULT_*`) como fuente del multiplicador del componente `multiplicador_cumplimiento` -- el esquema plano legacy (`calcular_comision`, `COMISION_MODO='plana'`) no se toca, sigue con sus 4 tramos fijos. **Persistencia** -- migración `0012_tramos_cumplimiento` (tabla `public.comision_tramos_cumplimiento`, mismo patrón de vigencia que `comision_tramos_cobranza`; `perfil` admite `NULL`=genérico o un perfil específico, la semilla usa un solo juego de tramos para todos, diferenciación por perfil descartada explícitamente); semilla exacta del pedido -- `[0,90)→0.0×` ("Sin comisión", RN-CM16/umbral 90%), `[90,100)→1.0×` ("Meta"), `[100,110)→1.2×`, `[110,125)→1.35×`, `[125,∞)→1.5×` (escala de sobrecumplimiento, RN-CM16); cada tramo tiene un `bono_fijo` opcional (sembrado en $0, decisión de gerencia activarlo) que se suma al componente `bonos` ya existente, sin ampliar el catálogo cerrado (`COMPONENTES_FORMULA`) -- el beneficio adicional al sobrecumplimiento que pedía el usuario. `CommissionConfigRepository`/`CommissionConfigService` ganan CRUD con vigencia y validación de cobertura `[0,∞)` sin huecos/solapes (mismo patrón que tramos de cobranza); `CommissionSimulationService` pre-resuelve los tramos una vez por período/proyección (mismo patrón de optimización que matriz/crédito, no una consulta por vendedor). **Desglose** -- `ProyeccionVendedor`/`ProyeccionVendedorResponse` ganan `pct_cumplimiento`/`nivel`/`multiplicador_cumplimiento`/`comisiona`/`componentes` (la traza completa de `evaluar_formula` con etiquetas legibles); en la proyección (promedio 3/6 meses) los pasos `sumar`/`restar` se promedian y los `multiplicar` se mantienen constantes (son literalmente iguales entre los meses históricos simulados, por diseño del método -- verificado con test). **Frontend** -- pestaña nueva "Tramos de cumplimiento" en `CommissionConfigPanel.tsx`; `DataTable.tsx` gana `renderExpanded` opcional (fila expandible, sin romper consumidores existentes); `CommissionSimulationPanel.tsx` muestra la tubería completa paso a paso al expandir cada fila, columnas nuevas `% cumplimiento`/`Tramo`, corrección de presentación de la tasa "% comisión/margen" (headerTitle explícito + indicador visual cuando cobranza/contado de agencia superan el 50% de la base, para no comparar tasas de vendedores de línea vs. de cobranza como si fueran lo mismo). **Corrección colateral real, no anticipada:** `app/database/base.py` (modelos que Alembic usa para `Base.metadata`) no importaba `ComisionTramoCobranza`/`ComisionFormula`/`ComisionFormulaComponente` desde la auditoría 44 (brecha preexistente nunca cerrada) -- corregido junto con el registro de `ComisionTramoCumplimiento`, para no heredar el mismo defecto en el modelo nuevo. **Incidente real durante la aplicación de la migración** (no "funcionó a la primera"): `bi_backend` entró en un ciclo de reinicio silencioso (exit 1 sin traceback en los logs) porque `entrypoint.sh` siempre corre `apply_migrations.py` ANTES de cualquier comando, incluso overrides de diagnóstico -- aislado con `docker run --entrypoint python` (bypass real del entrypoint), se encontraron 2 defectos reales encadenados: `etiqueta VARCHAR(30)` insuficiente para `"Sin comisión (< 90% de la meta)"` (31 caracteres, ampliado a `VARCHAR(50)`), y el nombre de revisión `'0012_comision_tramos_cumplimiento'` (33 caracteres) excedía `alembic_version.version_num VARCHAR(32)` por 1 carácter -- fallaba silenciosamente en el `UPDATE` final de la transacción (tabla creada, versión sin avanzar, reintento infinito en cada arranque); migración renombrada a `0012_tramos_cumplimiento`. Validado extremo a extremo: `pytest backend/tests/unit` (224 tests, 223 passed -- 1 falla preexistente ajena y dependiente de la fecha del sistema; tests nuevos: `resolver_tramo_cumplimiento`, comisión final $0 con bases positivas bajo el umbral, escala de sobrecumplimiento, guardas de rendimiento `call_count==1`, promedio del desglose reproduce el total proyectado); `pytest backend/tests/integration -m integration -k "commission or metas_actualizacion"` (13 tests, 12 passed -- mismo fallo preexistente ya documentado, vendedor 102); `tsc --noEmit`/`oxlint`/`npm run build` limpios; probado en vivo contra `bi_backend` reconstruido con la migración aplicada -- `GET /commission-config/tramos-cumplimiento` devuelve los 5 tramos sembrados; `POST /commission-simulation` con datos reales de julio 2026 confirma R-3 (`VEN13` 82,06% y `VEN24` 73,70% quedan en "Sin comisión", `multiplicador=0.0`) y R-1 (`VEN17` 118,23% y `VEN21` 115,19% reciben "Sobrecumplimiento alto", `multiplicador=1.35×`, no el 1.2× plano anterior) con el desglose completo de 7 componentes visible por vendedor; `PUT` con hueco rechazado (400), con la semilla completa aceptado (200) y registrado en la bitácora; `COMISION_MODO` permaneció en `plana` durante toda la prueba y `public.comision_liquidaciones` en 0 filas antes y después -- sin riesgo sobre dinero real ni artefactos de prueba. Actualización previa 2026-07-30 (Corrección de diseño de Comisión sobre Cobros, sobre el trabajo del mismo día -- petición explícita del usuario tras revisar la Fase 4: "las comisiones variables es un todo... deben sumarse las 2 comisiones para sacar una sola comisión por vendedor y la fórmula solo debe ser una opción no 2". La primera versión modelaba margen/categoría y cobranza como DOS fórmulas alternativas (`actual`/`cobranza`) entre las que gerencia "activaba" una u otra -- diseño conceptualmente erróneo: no son esquemas que se reemplazan, es un solo total que se suma. Corregido con migración `0009_formula_unica_unificada`: se borra la fórmula `cobranza` y se consolidan sus componentes dentro de la única fórmula restante (renombrada `clave='variable'`), que ahora tiene los 7 componentes combinados -- `base_lineas_venta`+`base_cobranza`+`contado_agencia` (sumados) × `factor_tipo_vendedor` × `multiplicador_cumplimiento` − `devoluciones` + `bonos`; se retira por completo el concepto de "activar" (endpoint `/formula/{id}/activar`, `CommissionConfigRepository.activar_formula`, `CommissionConfigService.activar_formula`, hook `useActivarFormula`/`activarFormula` del frontend, botón "Activar" y selector de fórmulas del panel) -- ahora hay UNA fórmula, siempre visible, solo editable en su estructura. Segundo hallazgo del mismo mensaje del usuario ("hay repetido 2 configuraciones, factores de crédito y comisión sobre cobros, estas configuraciones también deben ser adaptadas en el simulador"): confirmado en el código -- `CommissionSimulationService` (`simular`/`reconstruir_mes_especifico`/`proyectar_comision_variable`) llamaba directo a `commission_engine.calcular_comision_variable` (solo líneas de venta con su factor de crédito) y NUNCA incluía la cobranza ni el contado de agencia, aunque ambas configuraciones ya existieran en el panel -- el simulador subestimaba sistemáticamente lo que gerencia usaría como "argumento decisivo". Corregido extrayendo el cálculo a un módulo compartido nuevo, `app/services/commission_variable_engine.py::calcular_comision_variable_completa`, que resuelve el monto de cada componente ACTIVO de la fórmula (líneas+matriz+crédito, cobranza+tramos, contado de agencia) y evalúa la tubería declarativa -- usado ahora tanto por `CommissionService._calcular_variable` (cálculo real) como por las 3 rutas del simulador, garantizando que ambos calculen exactamente lo mismo. Corrección técnica encontrada en el camino (no reportada por el usuario, pero real): el bono de "cobranza sana" se calculaba sobre "el acumulado de la tubería justo antes de bonos", que para la fórmula unificada incluye la resta de devoluciones -- distinto del comportamiento legacy real (`comision_post_cumplimiento`, ANTES de restar devoluciones); corregido para que la referencia del bono sea genéricamente "el acumulado justo antes del paso 'devoluciones'" (o antes de 'bonos' si no hay paso de devoluciones), preservando el comportamiento histórico para cualquier estructura de fórmula. Optimización de rendimiento aplicada en el mismo cambio: el simulador itera muchos vendedores por período — antes cada vendedor repetía su propia consulta de matriz/factores de crédito/fórmula; ahora `calcular_comision_variable_completa` acepta `matriz`/`rangos_credito`/`formula_componentes` pre-resueltos (`resolver_componentes_formula`, nueva), y los 3 métodos del simulador los resuelven UNA VEZ por período (o una vez por llamada completa cuando la fecha de configuración no cambia, como en la proyección) en vez de una vez por vendedor -- detectado por 2 tests unitarios preexistentes que verificaban `call_count == 1` y que inicialmente fallaron con `call_count == 3`/`6` tras el primer intento de la corrección. Validado extremo a extremo: nuevo `test_commission_variable_engine.py` (3 tests: la comisión se paga aunque no haya líneas de venta con tal de que haya cobranza real, líneas+cobranza se suman en una sola comisión, fallback defensivo sin fórmula configurada); `pytest backend/tests/unit` (217 passed -- 3 nuevos de esta corrección más los 214 de la fase anterior -- 1 falla preexistente ajena y dependiente de la fecha del sistema); `pytest backend/tests/integration -k "commission or metas_actualizacion"` (12 passed, 1 failed -- mismo fallo preexistente ya documentado, vendedor 102); probado en vivo contra el EDW real activando temporalmente `COMISION_MODO=sombra` -- VEN13/feb-2026 pasa de `comision_base=200,82` (solo líneas, motor viejo) a `comision_base=1.163,42` (líneas $200,82 + cobranza $962,998 sumadas, motor nuevo), y el simulador (`reconstruir_mes_especifico`) reproduce el mismo total combinado ($6.809,39, la pequeña diferencia frente al cálculo real es por diseño -- el simulador usa la configuración vigente HOY, no la vigente al cierre del período); artefactos de la prueba en vivo (una fila de `comision_liquidaciones` modo sombra) limpiados de la base real tras validar; `tsc --noEmit`/`oxlint`/`npm run build` de producción limpios. Regla nueva RN-CM14 y corrección de RN-CM13 en `docs/auditoria/02_reglas_negocio_validadas.md` §18. Actualización previa 2026-07-30 (Comisión sobre Cobros, `docs/auditoria/44_comisiones_sobre_cobros.md` + `docs/features/plan_comisiones_sobre_cobros.md` -- petición explícita del usuario: adaptar la regla de comisión REALMENTE vigente en la empresa hoy -- comisión sobre COBRANZA efectiva por tramo de días de cobro, no sobre venta facturada -- como configuración adicional del esquema de Comisiones Variables, con la fórmula editable sin tocar código. Auditoría previa contra Producción vía un arnés de solo lectura (SAP SQL Anywhere real, `fp_cxc_cuotas`/`cuentasporcobrar`/`movimientos_caja`/`encabezadofacturas`, guarda que bloquea cualquier verbo de escritura) confirmó y cuantificó 10 hallazgos (H-1..H-10): la tabla que sustenta la regla (`fp_cxc_cuotas`) nunca se extraía al EDW (H-1); `edw.dim_formapago` es un catálogo estático de 3 filas que deja el 100% de las 214.108 filas de `fact_cobros_cxc` en el centinela `-1` (H-2); el 47,4% de los cobros con cheque postfechado cruzan de mes entre `fectra` (recepción) y `banfec` (efectivización) -- $366.688 de $839.412 en ene-2025..jul-2026, desfase promedio 35,5 días -- confirmando en datos reales la advertencia del usuario ("hay el caso de que varían los meses"), H-3; `fact_cobros_cxc` mezcla facturas emitidas con cobros reales bajo `cuentasporcobrar` sin filtrar por `tiporg`/`tipdoc` (H-4); el motor variable actual (`GoalRepository.get_commission_lines`) solo lee `fact_ventas_detalle`, sin ninguna ruta hacia cobranza (H-5); `conpag='E'` es CONTADO y `'C'` es CRÉDITO, contraintuitivo -- verificado 786/786 facturas `'C'` generan CxC, 0/1493 `'E'` (H-7, RN-CM11). Aplicado de punta a punta: **ETL/EDW** -- extractor nuevo `cobros_cuotas_extractor.sql` sobre `fp_cxc_cuotas`, tabla de hechos nueva `edw.fact_cobros_cuotas` (grano cobro-cuota-instrumento, `tipo_instrumento` como dimensión DEGENERADA a propósito -- no repetir el defecto de `dim_formapago` -- `dias_cobro` materializado con piso en 0 por 12 filas históricas con `banfec` anterior a `fecemi`), registrada en `PIPELINE_CONFIG`; bloque nuevo en `resolver_llaves_hecho` (`orchestrator.py`) para resolver `fecha_registro_sk`/`fecha_emision_sk` además de la `fecha_sk` principal (=`banfec`, el devengo -- crítico: el resolutor genérico habría tomado `fecemi` primero de su lista fija de columnas, invirtiendo la regla). Corrida real del ETL contra SAP (imagen `proyect_bi-etl` reconstruida): 140.312 filas cargadas; verificado en el EDW contra la verdad de campo de feb-2026 (VEN13 $963,00, VEN02 $781,78 -- coincide con el reporte del ERP con diferencia de centavos por redondeo de tramo). **Configuración** -- migración Alembic `0008_comisiones_cobranza`: `comision_tramos_cobranza` (tramos/tasas por perfil externo/interno/jefe_agencia, semilla exacta del cuadro de negocio); `comision_formula`/`comision_formula_componente` -- la ESTRUCTURA de la fórmula deja de estar quemada en código (pedido explícito del usuario), modelada como tubería ordenada de un catálogo CERRADO de componentes con operador sumar/restar/multiplicar por paso -- nunca evaluación de expresiones arbitrarias, superficie de ejecución inaceptable en un módulo que mueve dinero real; se siembran 2 fórmulas, `actual` (activa, reproduce byte a byte el motor legacy) y `cobranza` (inactiva, cobranza + contado de agencia); `comision_config_vendedor` gana el perfil `jefe_agencia` y la columna `agencia`. **Motor** -- `commission_engine.py` gana `calcular_comision_cobranza` (resuelve tramos con desglose, mismo formato que el reporte del ERP), `calcular_contado_agencia`, `evaluar_formula` (tubería declarativa con piso $0) y `calcular_base_lineas_venta` (extrae la lógica de líneas ya existente para reutilizarla desde el motor de fórmula genérico); `CommissionService._calcular_variable` despacha a la ruta legacy intacta si la fórmula activa es `'actual'` (cero riesgo sobre el cálculo ya auditado) o al motor de fórmula genérico (`_calcular_variable_formula`) para cualquier otra -- el bono de cobranza sana se resuelve genéricamente sobre "el acumulado de la tubería justo antes del paso de bonos", generalizando el two-pass que ya existía. **API/Frontend** -- endpoints nuevos bajo `/gerencia/goals/commission-config/tramos-cobranza` y `/formula` (CRUD + activar); `CommissionConfigPanel.tsx` gana pestañas "Comisión sobre cobros" y "Fórmula" (editor de la tubería: orden/componente/operador/activo). Validado extremo a extremo: activación real de la fórmula `cobranza` contra el backend vivo reprodujo `$962,998` de comisión de cobranza para VEN13/feb-2026 (idéntico a la verdad de campo), con `desglose_cobranza`/`traza_formula` poblados con datos reales; revertido a `actual` tras la prueba (activar `cobranza` en producción queda como decisión de gerencia, no tomada aquí); `pytest backend/tests/unit` (215 tests, 214 passed -- 13 nuevos de esta sesión, todos verdes -- 1 falla preexistente ajena y dependiente de la fecha del sistema); `pytest backend/tests/integration -k "commission or metas_actualizacion"` (12 passed, 1 failed -- mismo fallo preexistente ya documentado, vendedor 102 sin suficiente histórico en el EDW real a la fecha de la corrida); suite de integración completa (121 passed, 3 skipped, 9 failed -- los 9 son artefactos ya documentados de este mismo entorno: rate limiter de `/auth/login` agotado por las pruebas manuales en vivo de esta sesión y `ML_MODELS_DIR` no resuelto en este entorno de pruebas, ninguno relacionado con los cambios); `tsc --noEmit`/`oxlint`/`npm run build` de producción limpios. Reglas nuevas RN-CM8..RN-CM13 en `docs/auditoria/02_reglas_negocio_validadas.md` §18. Pendiente (decisión de negocio, no de código): que gerencia decida activar la fórmula `cobranza` cuando corresponda, y si "interno" debe tener tramos propios distintos de "externo" (el cuadro de negocio provisto no los distingue). Actualización previa 2026-07-29 (Hallazgo Bodega-sucursal, `docs/auditoria/42_correcciones_integrales_sistema.md` -- **hallazgo ALTO**, continuación directa de B-3: instrucción explícita del usuario tras B-3 -- "no se debe utilizar la sucursal sino revisa el almacen y los vendedores, pero no en sucursal por que esa no es la regla de negocio" -- llevó a auditar el resto del sistema en busca de otros usos de `sucursal` como RLS forzada. `edw.dim_almacen`/`dim_sucursal` (`SELECT` real): `establ='003'` ("PRINCIPAL: MATRIZ") agrupa 8 de los 15 almacenes reales bajo un solo nombre de sucursal, incluidas camionetas de vendedores individuales (placas como `TBM3626`/`TBL6731`/`TBF-8236`) y bodegas de consignación -- `sucursal` nunca fue la unidad de acceso real de Bodega, que siempre fue `codalm` (`usuario_almacenes`, RN-B10, ya correcto desde la Fase 1/H-1). El módulo Bodega (`backend/app/api/routes/warehouse.py`) forzaba en los 15 endpoints reales una restricción ADICIONAL `sucursal_bodega = resolve_sucursal_filter(allow_override=False)` en AND con la RLS real por almacén, sobre `usuarios.sucursal`. Confirmado en vivo un blackout total (no parcial, a diferencia de B-3): el usuario real `bodega_quito@empresa.com` (`todos_los_almacenes=true`, debía ver TODO el inventario) tiene `usuarios.sucursal='Matriz Quito'`, valor que no existe en ninguna fila de `edw.dim_sucursal.nombre_sucursal` (los 7 valores reales son variantes como `SUC. EL REY`/`PRINCIPAL: MATRIZ`) -- el filtro `su.nombre_sucursal = :sucursal` nunca matcheaba, y este usuario recibía 0 filas en el 100% de sus KPIs/reportes, silenciosamente (sin selector de sucursal en el frontend -- `BodegaFilterBar.tsx` nunca lo expuso -- que permitiera siquiera notar la causa). Corregido: los 15 endpoints reales de Bodega dejan de resolver/propagar `sucursal` (la RLS real ya era `almacenes_permitidos`, inyectada en `WarehouseRepository` desde la Fase 1, y sigue intacta); el endpoint legado `/kpis-inventory` (sin consumidor en el frontend actual, pero endpoint autenticado alcanzable) migró de `sucursal` a la misma RLS real -- `AnalyticsRepository.get_inventory_alerts` ahora filtra `edw.fact_inventario_snapshot` por `al.codalm IN (...)` (join a `dim_almacen`, que ya tiene `almacen_sk` propio) en vez de `dim_sucursal`. Validado extremo a extremo: `pytest backend/tests/unit` (194 passed, 1 falla preexistente ajena); `bi_backend` reconstruido con arranque limpio (6/6 modelos ML); `pytest backend/tests/integration/test_warehouse_actualizacion_bodega.py` dentro del contenedor real (33 passed, incluye los 6 de PDF que solo corren ahí); `test_bodega_rls.py` verde en corrida aislada (el limitador de `/auth/login`, 5/min, se agotó en corridas combinadas de esta misma sesión de validación, mismo artefacto ya documentado antes, no una regresión). Probado en vivo dentro del contenedor comparando el filtro viejo vs. el nuevo sobre el mismo snapshot real: sucursal forzada a `'Matriz Quito'` -> `(0, 0)`; almacén sin restricción (`todos_los_almacenes=True`) -> `(114239, 102891)`; almacén restringido a `['08']` (SALCEDO) -> `(8160, 7285)` -- confirma que la restricción por almacén sigue siendo una intersección real, nunca "todo por defecto". `sucursal` se conserva sin cambios como dimensión voluntaria de exploración para gerencia/administrador (`ai-summary`, forecast de ventas) -- el hallazgo es específico a `sucursal` como RLS forzada sobre roles no privilegiados, no una prohibición total de la columna. Actualización previa 2026-07-29 (Auditoría A-0.3 y decisión B-3 de `docs/features/plan_correcciones_integrales_sistema.md` -- **hallazgo ALTO**: `SELECT` real contra el EDW mostró que de 7 sucursales totales, el 90.9% de los vendedores activos (10 de 11, últimos 12 meses) transaccionan en 4-7 de esas 7 (promedio 5.18; histórico completo: 85.0%, 17/20). `resolve_sucursal_filter(allow_override=False)` forzaba a cada usuario `ventas` a su sucursal fija asignada (`usuarios.sucursal`) en `GET /analytics/ventas/goals` y `/goals/forecast-cierre` -- para un vendedor típico esto le ocultaba la MAYORÍA de sus ventas reales en su propio dashboard; no era "el filtro no discrimina bien", eran datos incompletos activos. **Decisión del usuario: retirar `sucursal`, usar `vendedor`** -- ya es el discriminador correcto y ya usado en el resto de este mismo router (`_codven_restriccion`: `mi-comision`, `meta-sugerida`, `churn-risk`, Venta Cruzada) y en Metas/Comisiones (regla 10: grano `(anio, mes, id_vendedor_origen)`, nunca sucursal). Aplicado: `AnalyticsRepository.get_sales_performance` gana `vendedor` (además de `sucursal`, que se conserva solo para el uso exploratorio voluntario de gerencia en `ai-summary`); `sales.py` cambia `/goals` y `/goals/forecast-cierre` de `resolve_sucursal_filter` a `_codven_restriccion`; `GoalMLService.forecast_cierre` gana `vendedor_nombre` -- `DatasetRepository.get_daily_sales_history` ya soportaba filtrar por `nombre_vendedor` (no `codven`), así que la ruta resuelve `codven -> nombre_vendedor` vía `CatalogRepository.get_vendedor_activo` antes de llamar al servicio ML, para que meta (personal) y proyección (ahora también personal) sean comparables. Validado extremo a extremo: `pytest backend/tests/unit` (194 passed, 1 falla preexistente ajena); `pytest backend/tests/integration -k "sales or ventas or goal"` (24 passed, 1 skipped); `bi_backend` reconstruido con arranque limpio. Probado en vivo: el usuario de prueba estándar (`ventas_gye@empresa.com`) tiene un `id_vendedor_origen` que no existe en el EDW actual (desalineación de datos semilla preexistente, no introducida aquí) -- para confirmar la lógica con datos reales se ejercitaron los servicios directamente dentro del contenedor con el vendedor real `VEN02` (`WILLIAN SANCHEZ`): `meta_mensual=$58.098,83`/`cumplimiento_actual=$54.976,83` coincide exactamente con una consulta SQL de control manual y con la fila de ese vendedor en el ranking de `ai-summary`; `forecast_cierre` muestra ahora `sucursal: 'WILLIAN SANCHEZ'` en el label (antes mezclaba ~6 vendedores/tiendas de toda la sucursal). **Hallazgo colateral, no corregido (fuera de alcance):** `backend/tests/tests/` es una copia duplicada NO versionada (`git status` la marca `??`) y desactualizada de `backend/tests/` -- causa falsos positivos si se corre `pytest tests/` sin acotar a `tests/unit`/`tests/integration`; documentado para que el usuario decida si eliminarla. Con esto se cierra B-3 y la Fase 7 queda desbloqueada. Pendiente del plan completo: Fase 4 §4.3-4.4 (rediseño UX de Ventas) y Fase 7 (dashboard por rol, ya desbloqueada). Actualización previa 2026-07-29 (Fase 2 §2.3 de `docs/features/plan_correcciones_integrales_sistema.md` -- filtros híbridos texto+combobox, **cierre completo de la Fase 2 del plan**. El proyecto ya tenía `Autocomplete<T>` (`components/ui/Autocomplete.tsx`, Venta Cruzada) para "búsqueda parcial contra el backend por tecla" (clientes/productos, catálogos grandes/paginados); faltaba el caso complementario: un combobox tipeable sobre un catálogo YA CARGADO en memoria (`almacenes`/`categorias`/`proveedores` de `GET /analytics/bodega/filtros`), hoy resueltos con `<select>` nativo, incómodo si el catálogo crece. Nuevo `components/ui/ComboboxField.tsx`: filtrado 100% client-side (sin llamada al backend por tecleo), solo permite seleccionar un valor que exista en `options` -- nunca texto libre, porque estos filtros siempre exigen el valor exacto del catálogo (`_filtros_snapshot` compara con `=`, nunca `LIKE`), sin ambigüedad de diseño. Mismo patrón visual/posicionamiento de dropdown que `Autocomplete` (portal a `document.body`, cálculo de espacio arriba/abajo del viewport) para consistencia de UX. Aplicado en `BodegaFilterBar.tsx`: Almacén/Categoría/Proveedor pasan de `<Select>` nativo a `<ComboboxField>`; "Tipo de movimiento" se deja como `<Select>` (catálogo cerrado de 8 valores fijos, sin beneficio real). La dependencia declarativa `categoria → proveedor` de la Fase 2 §2.2 se conserva sin cambios -- solo se reemplazó el control de UI. Validado: `tsc --noEmit`/`oxlint` limpios; `npm run build` de producción sin errores nuevos; verificación visual en navegador real no posible en este entorno Windows sin `chromium-cli` (limitación ya documentada, queda como paso manual). Con esto se cierra por completo la Fase 2 del plan. Pendiente del plan completo: Fase 4 §4.3-4.4 (rediseño UX de Ventas) y Fase 7 (dashboard por rol, bloqueada por B-3). Actualización previa 2026-07-29 (Fase 2 §2.4 de `docs/features/plan_correcciones_integrales_sistema.md` -- auditoría A-0.4: alineación TypeScript↔Pydantic, campo a campo, de los 12 pares tipo/schema del proyecto. **Hallazgo real, ALTO:** `frontend/src/types/ventas.ts` declaraba dos tipos para el MISMO endpoint `GET /analytics/ventas/goals` -- `VentasKPIs` (campos `ventas_actuales`/`cumplimiento_pct`/`clientes_activos`/`churn_promedio`, NINGUNO existe en el backend real) y `VentasGoalsTracking` (el tipo correcto, `meta_mensual`/`cumplimiento_actual`/`meta_proyectada`/`ranking_vendedores`, ya usado por `VendorGoalDashboard.tsx`). `DashboardVentas.tsx` -- la vista PRINCIPAL de Ventas -- usaba el tipo roto: 3 de sus 4 KPI cards ("Ventas Actuales", "Cumplimiento", "Clientes Activos") leían campos `undefined` en el 100% de las cargas, degradando a `NaN`/guiones; un desarrollo previo aparentemente detectó el desajuste y creó `VentasGoalsTracking` para el panel del vendedor, pero nunca corrigió el dashboard original que causó el hallazgo. Corregido: `getSalesGoals` devuelve `VentasGoalsTracking` (no un tipo nuevo); `VentasKPIs` eliminado por completo (tipo muerto); `DashboardVentas.tsx` reescribe las 4 tarjetas con los campos reales -- Meta Mensual, Ventas Actuales (`cumplimiento_actual`, venta real acumulada en $ pese al nombre confuso del campo, no un porcentaje), Cumplimiento % (derivado en frontend, el backend no lo envía calculado) y Meta Proyectada (reemplaza "Clientes Activos", que nunca tuvo datos reales detrás). Confirmado en vivo contra `bi_backend`: la respuesta real trae exactamente los 4 campos del tipo corregido. **Hallazgos MEDIO/BAJO sin bug activo** (corregidos por completitud del contrato, sin consumidor roto): `bodega.ts` `ProductoStockReorden` no declaraba `dias_inventario_baja_confianza` (RN-B11, Fase 6.1) -- sin impacto porque `useStockReorden` no tiene consumidor en el frontend hoy; `ventas.ts` `MetaSugerida` le faltaban 4 campos que el backend sí envía (`componente_estacional`/`componente_tendencia`/`factor_tendencia_aplicado`/`coeficiente_variacion`, ya presentes en el tipo hermano `goals.ts::MetaSugeridaDesglose`); `gerencia.ts` `GerenciaKPIs.ventas_consolidadas?` era código muerto sin contraparte backend ni consumidor -- eliminado. Validado: `tsc --noEmit`/`oxlint` limpios en los 6 archivos tocados; `pytest backend/tests/unit` sin regresiones (193 passed, 1 falla preexistente ajena -- esta fase no tocó backend); confirmado en vivo con `curl`. Pendiente de Fase 2: §2.3 (`ComboboxField` de filtros híbridos texto+combobox), no abordado en esta sesión. Pendiente del plan completo: resto de Fase 4 (rediseño UX de Ventas §4.3-4.4), Fase 7 (dashboard por rol, bloqueada por B-3). Actualización previa 2026-07-29 (Fase 6.5 de `docs/features/plan_correcciones_integrales_sistema.md` -- Módulo Bodega, **cierre completo de la Fase 6 del plan**. H-3: el botón "Imprimir / PDF" llamaba `window.print()`, heredando el layout de pantalla (paginación rota, tablas cortadas, nav/sidebar impresos) -- no era un PDF real. Decisión del usuario entre 3 opciones presentadas: **WeasyPrint** (recomendada, HTML/CSS→PDF con alta fidelidad) sobre ReportLab (más código para igualar el layout de negocio) o xhtml2pdf (soporte CSS más limitado). Nuevo `GET /reportes/{tipo}/pdf` reutiliza el mismo despachador `_generar_reporte` que ya usan `/reportes/{tipo}` y `/reportes/{tipo}/excel` -- una sola fuente de datos para pantalla, Excel y PDF; nuevo `app/services/warehouse_pdf_export.py::reporte_a_pdf` construye HTML/CSS propio desde el contrato tipado `ReporteBodegaResponse` (mismo insumo que `warehouse_export.py`), con el mismo resaltado de prioridad "Alta"/"Crítico"/"Inmovilizado" que el Excel. Mismo `Content-Disposition` con fecha/hora del servidor que la Fase 6.4. Frontend: `descargarReportePdf` (mismo patrón que `descargarReporteExcel`) reemplaza `window.print()`; el botón pasa de "Imprimir / PDF" a "Descargar PDF". Dependencias nuevas: `weasyprint>=62.0,<63.0` + librerías de sistema Pango/Cairo/GDK-Pixbuf en `backend/Dockerfile` (WeasyPrint las carga vía `ctypes`, no son dependencias de pip) -- el nombre de paquete Debian 12 es `libgdk-pixbuf-2.0-0` (con guion), no `libgdk-pixbuf2.0-0` (con punto), corregido antes del primer build exitoso. **Bug real encontrado y corregido en producción, no solo en unit tests:** `weasyprint==62.3` sin pin de su dependencia `pydyf` resolvía `pydyf==0.12.1` vía pip, y TODO intento de generar un PDF fallaba con `AttributeError: 'super' object has no attribute 'transform'` (WeasyPrint llama `pydyf.Stream.transform(...)`, método que pydyf 0.12.x renombró a `set_matrix`) -- el endpoint devolvía 500 en el 100% de los requests pese a un build y arranque limpios; corregido fijando `pydyf==0.11.0` explícito en `requirements.txt`, verificado reconstruyendo la imagen desde cero (no un parche manual en el contenedor). `reporte_a_pdf` importa `weasyprint.HTML` de forma diferida (dentro de la función, no a nivel de módulo) porque WeasyPrint carga Pango/Cairo/GObject vía `ctypes` en el momento del `import` -- un import a nivel de módulo habría roto el arranque de TODO el backend en cualquier entorno sin esas librerías de sistema, incluyendo `pytest` en este mismo entorno de desarrollo Windows (confirmado en vivo: `import weasyprint` falla con `OSError: cannot load library 'gobject-2.0-0'` aquí, que no tiene runtime GTK). Recorte a 200 filas por sección en el PDF (`_MAX_FILAS_PDF`, no aplica al Excel) -- un PDF con miles de filas de `kardex` es ilegible e inmanejable de paginar, declarado explícitamente en el pie de cada tabla. Validado extremo a extremo: `bi_backend` reconstruido desde cero con el pin de `pydyf` ya en `requirements.txt`; `pytest backend/tests/unit` (193 passed, 1 falla preexistente ajena) en host y contenedor; `pytest backend/tests/integration/test_warehouse_actualizacion_bodega.py` corrido **dentro del contenedor real** (única forma de probar WeasyPrint) -- **28 passed**, incluye verificación de `content-type: application/pdf`, nombre de archivo con fecha/hora, firma real `%PDF-`, y los 5 tipos de reporte parametrizados; `tsc`/`oxlint` limpios. Probado en vivo con `curl` contra los 5 tipos: todos `HTTP 200` con firma `%PDF-` real y tamaños de archivo realistas (justificación 390KB, transferencias 221KB, análisis mensual 134KB, sin-venta 61KB, kardex 56KB). Con esto se cierra por completo la Fase 6 del plan. Pendiente: Fase 2 §2.4, resto de Fase 4, Fase 7. Actualización previa 2026-07-29 (Fase 6.7 de `docs/features/plan_correcciones_integrales_sistema.md` -- Módulo Bodega, cierre de la Fase 6 salvo §6.5 (PDF real). `QUERY_BODEGAS` ya estaba implementada (`CatalogRepository.get_almacenes`, `GET /users/almacenes`) -- verificado sin cambios. `QUERY_KARDEX_MOVIMIENTOS` (H-11, MEDIO): la query de Producción tenía un `GROUP BY` sin ninguna función de agregación (un `DISTINCT` disfrazado) mezclando el grano `(bodega, artículo)` con `a.exiact` (`ExistenciaGlobal`), la existencia global del artículo, que `edw.dim_producto` no almacena -- sin equivalente directo en el EDW. Nuevo `tipo=kardex` del contrato tipado ya existente (`ReporteBodegaResponse`, mismo patrón que `sin-venta` de la Fase 6.2): `WarehouseRepository.get_movimientos_kardex` hace un `SELECT` directo sobre `fact_movimientos_inventario` al grano de un movimiento individual (corrige el `GROUP BY` sin agregación con una consulta que no agrupa lo que no debe agregarse); `existencia_global` se deriva sumando el kardex completo (todos los almacenes, todo el histórico) por producto -- misma fórmula de dirección por `es_entrada`/`es_salida` que el resto del módulo (regla 3, corrige H-7 automáticamente) -- etiquetada explícitamente "Existencia global (kardex, todos los almacenes)" en vez de presentarse en silencio como si fuera `exiact` de Producción. **Bug evitado, no heredado:** el resto del módulo traduce `tipo_movimiento` a "artículos que ALGUNA VEZ tuvieron ese tipo de movimiento" (`_filtros_snapshot`, correcto para agregados por artículo); `get_movimientos_kardex` filtra directo por `m.tipo_movimiento` en la fila -- reutilizar la semántica agregada aquí habría devuelto filas `TRA`/`CPA` al filtrar por `FAC`. Sin columna `Cliente` (§6.0). Validado extremo a extremo: `pytest backend/tests/unit` (193 passed, 1 falla preexistente ajena); `pytest backend/tests/integration/test_warehouse_actualizacion_bodega.py` (22 passed, incluye 2 tests nuevos de kardex); `tsc`/`oxlint` limpios; probado en vivo contra `bi_backend` reconstruido -- `GET /reportes/kardex?fecha_desde=2026-01-01&fecha_hasta=2026-01-15` devuelve 1.000 movimientos reales (tope del límite, 2.058 unidades de entrada, 1.548 de salida); con `tipo_movimiento=TRA` el 100% de las filas son `TRA` (confirma filtro por fila, no por artículo) con `existencia_global` poblada con datos reales. Pendiente de Fase 6: solo §6.5 (PDF real en backend, requiere decisión de dependencia nueva); y Fase 2 §2.4, resto de Fase 4, Fase 7. Actualización previa 2026-07-29 (Fases 6.3 y 6.6 de `docs/features/plan_correcciones_integrales_sistema.md` -- Módulo Bodega. **Fase 6.3 (UI/UX de los 4 reportes de abastecimiento):** verificada sin hallazgos -- las tarjetas de selección de `BodegaReportes.tsx` (`REPORTES`, incluida `sin-venta` agregada en la Fase 6.2 de esta misma sesión) ya declaran `titulo`/`pregunta`/`descripcion` por reporte, dentro del contrato tipado que estableció la Fase 5 de `plan_actualizacion_modulo_bodega.md`; el objetivo del plan ya estaba satisfecho por trabajo de fases anteriores, sin cambios de código. **Fase 6.6 (motivos de transferencia más accionables):** `/transferencias-sugeridas` ya calculaba `justificacion`/`confianza`/`beneficio_neto_estimado` (RN-B9) pero el texto `motivo` solo enumeraba "días de stock" y "uds/día" en origen y destino sin traducirlo en una acción concreta. Nuevo `WarehouseService._motivo_transferencia` (mismos datos ya calculados en `_justificacion_transferencia`, sin consulta nueva al EDW) arma el texto en 3 partes siguiendo el formato del plan: origen (días sin movimiento o días de stock + valor monetario inmovilizado, `stock_actual × costo_unitario`), destino (demanda mensual observada `demanda_media_destino × 30` + meses de historial + días de cobertura, con degradación explícita si no hay demanda medible) y la acción (`cantidad_transferir / demanda_mensual_destino` meses de cobertura que cubre la transferencia). Se omite deliberadamente "Cliente asociado" del ejemplo original del requerimiento (decisión ya tomada en §6.0: los reportes de Bodega no llevan identidad de cliente, aunque `fact_movimientos_inventario.cliente_sk` esté poblada para `FAC`). Validado extremo a extremo: `pytest backend/tests/unit` (193 passed, 1 falla preexistente ajena); `pytest backend/tests/integration/test_warehouse_actualizacion_bodega.py` (22 passed) y `test_bodega_rls.py` (todas las pruebas verdes en ejecución aislada -- 3 errores vistos en una corrida combinada fueron el limitador de tasa de `/auth/login`, 5/min, agotado por pruebas manuales en vivo de esta misma fase, no una regresión); `tsc`/`oxlint` no aplicaban (sin cambios de frontend en esta fase); probado en vivo contra `bi_backend` reconstruido -- `GET /transferencias-sugeridas` devuelve, por ejemplo: "Sin salidas en los últimos 180 días en PELILEO (4 uds, $624 inmovilizados). ATAHUALPA vendió 33 uds/mes en los últimos 7 meses y tiene 10 días de cobertura. Transferir 4 uds cubre 0.1 meses de su demanda." -- el formato exacto que pedía el plan, con datos 100% reales del EDW. Pendiente de Fase 6: §6.5 (PDF real en backend, requiere decisión de dependencia nueva), §6.7 (`QUERY_KARDEX_MOVIMIENTOS`); y Fase 2 §2.4, resto de Fase 4, Fase 7. Actualización previa 2026-07-29 (Fases 6.2 y 6.4 de `docs/features/plan_correcciones_integrales_sistema.md` -- Módulo Bodega. **Fase 6.2 (H-9):** reporte único parametrizado `GET /reportes/sin-venta?solo_con_stock=true|false&busqueda=...` reemplaza `QUERY_PRODUCTOS_SIN_VENTA`/`QUERY_ARTICULOS_ESTANCADOS` (eran la misma query salvo un `HAVING`). Nuevo `WarehouseRepository.get_articulos_sin_venta`: universo `(producto, almacén)` con movimiento histórico, stock calculado agregando el kardex completo (no `fact_inventario_snapshot`, solo poblado "hacia adelante"), última venta histórica vía `ROW_NUMBER() OVER (PARTITION BY almacen_sk, producto_sk ORDER BY fecha DESC, num_documento DESC)`, `NOT EXISTS` de ventas FAC en el rango. **Limitación heredada documentada, no introducida aquí (H-8):** `fact_movimientos_inventario` no tiene columna de estado de documento -- confirmado en `etl/extractors/kardex_extractor.sql`, que no aplica ningún token `{ESTADO}` a diferencia de otros extractores -- una factura anulada podría aparecer como "última venta"; corregirlo requiere cambio de ETL/re-extracción desde SAP, fuera de alcance de este reporte puntual. `WarehouseService.get_reporte_sin_venta` devuelve el contrato `ReporteBodegaResponse` ya existente (Excel/tabla gratis, sin código de presentación nuevo); sin columna `Cliente` (decisión del usuario). Frontend: tarjeta nueva en `BodegaReportes.tsx` con toggle "solo con stock" + buscador. **Fase 6.4 (H-4):** `GET /reportes/{tipo}/excel` ya generaba el nombre con fecha/hora en el backend, pero **el frontend lo neutralizaba**: `descargarReporteExcel` fijaba `link.download` con un nombre estático sin fecha, y ese atributo `download` es lo que el navegador usa (no la cabecera `Content-Disposition` original) cuando se crea un `<a>` sobre un blob vía `URL.createObjectURL` -- bug real encontrado y corregido leyendo el nombre real del header en la respuesta. Validado extremo a extremo: 4 tests de integración nuevos, `pytest backend/tests/unit` (193 passed, 1 falla preexistente ajena), `pytest` de Bodega/RLS (39 passed, 1 skipped, sin regresiones), `tsc`/`oxlint` limpios; probado en vivo (`GET /reportes/sin-venta` con 500 estancados reales y $59.544,87 en valor inmovilizado; búsqueda "HANKOOK" con 73 resultados reales; header `Content-Disposition` confirmado con fecha/hora real del servidor). Pendiente de Fase 6: §6.3 (pulido UI/UX, ya cubierto en gran parte), §6.5 (PDF real en backend, requiere dependencia nueva), §6.6 (motivos de transferencia más accionables), §6.7 (QUERY_KARDEX_MOVIMIENTOS); y Fase 2 §2.4, resto de Fase 4, Fase 7. Actualización previa 2026-07-29 (Fase 6.1 de `docs/features/plan_correcciones_integrales_sistema.md` -- Módulo Bodega, cierre de **H-2 (ALTO), el hallazgo de datos más grave del plan completo**: un artículo con stock > 0 y CERO salidas caía en `"Seguro"` porque `_dias_inventario` devuelve `None` sin salidas y la rama de "Exceso" (`dias_inv > BODEGA_DIAS_EXCESO`) es matemáticamente inalcanzable con `None` -- el reporte "Productos en exceso de stock" filtraba por ese estado, así que el peor caso posible de sobre-stock (inmovilizado total) era el único que **nunca aparecía** (caso reportado "Consignación Verónica Sánchez: stock pero 0.0 uds/día"). Corregido con un estado nuevo `"Inmovilizado"` (stock > 0, cero salidas en una ventana de 90 días -- `BODEGA_DIAS_VENTANA_INMOVILIZADO`, nuevo, deliberadamente más amplia que los 30 días de `salida_diaria` para no confundir estacionalidad con estancamiento real), aplicado con prioridad en `WarehouseService._enriquecer_producto`/`_inventario_matriz_completo`, alimentado por una columna nueva `salidas_ventana_inmovilizado` en `WarehouseRepository.get_inventario_productos`/`get_stock_por_almacen` (mismo patrón `FILTER` ya usado para el período anterior). También `dias_inventario_baja_confianza` (nuevo, caso "El Rey": `salida_diaria` bajo `BODEGA_MIN_SALIDA_CONFIABLE` marca "0 días de stock" como urgencia de baja confianza). El reporte de exceso ahora es `Exceso ∪ Inmovilizado`, ordenado por capital inmovilizado (`stock × costo`) descendente. **Bug real encontrado y corregido durante la validación en vivo** (no solo en unit tests): el truncado a 50 filas se aplicaba ANTES de separar por estado -- con 149 críticos reales en el EDW, el corte nunca llegaba a Exceso/Inmovilizado, dejando el reporte vacío de nuevo pese a la clasificación ya corregida; se truncó cada categoría por separado, después de filtrar. Validado extremo a extremo: 6 tests unitarios nuevos (`test_warehouse_estado_stock.py`), `pytest backend/tests/unit` (193 passed, 1 falla preexistente ajena), integración de Bodega (28 passed, 1 skipped) y RLS (11 passed) sin regresiones, `tsc`/`oxlint` limpios; probado en vivo contra `bi_backend` reconstruido -- `GET /analytics/bodega/reportes/analisis-mensual` pasó de **0 filas** en "exceso de stock" a **50 filas reales (41 Inmovilizado + 9 Exceso)**. Pendiente del resto de Fase 6: §6.2 (reporte unificado de artículos sin movimiento), §6.4 (nombres de archivo con fecha/hora), §6.5 (PDF real en backend), §6.6 (motivos de transferencia más accionables), §6.7 (QUERY_KARDEX_MOVIMIENTOS); y Fase 2 §2.4, resto de Fase 4, Fase 7. Actualización previa 2026-07-29 (Fase 5 de `docs/features/plan_correcciones_integrales_sistema.md` -- Módulo Administración, `docs/auditoria/42_correcciones_integrales_sistema.md` §Fase 5. §5.1: `GET /users` migrado a `Page[UserOut]` (paginación real, mismo patrón que Bodega/Notificaciones) -- bug real encontrado y corregido en el camino: anotar `Page[User]` (User = modelo ORM) en el servicio rompe la generación de schema de Pydantic (`PydanticSchemaGenerationError`) porque `User` no es serializable como parámetro genérico; el servicio devuelve `tuple[list[User], int]` y el router arma `Page[UserOut]` con `UserOut.model_validate(...)`, igual que ya hacía `audit-logs`. §5.2 (toggle activo/inactivo) ya estaba completamente implementado de una fase anterior, verificado sin cambios. §5.3: `UserRepository.delete()` (borrado duro) ya existía sin endpoint -- nuevo `DELETE /users/{id}/permanente` con las mismas restricciones del plan (no auto-eliminarse, no eliminar al último admin activo); viable sin migraciones nuevas porque **todas** las FK hacia `public.usuarios` ya declaran `ondelete` explícito (`SET NULL`/`CASCADE`, ninguna `RESTRICT`); frontend con doble confirmación real (checkbox + email exacto) vía un `confirmDisabled` nuevo y reutilizable en `ConfirmDialog.tsx`. §5.4: `/users` pasa a `/admin/usuarios` (`RouteKey 'admin.usuarios'`) -- el Sidebar ya soporta sub-nav 100% genérico por convención de key con punto, sin lógica nueva. §5.5: nuevo `GET /analytics/admin/resumen` (usuarios activos/inactivos reales, vendedores/bodegas vigentes del EDW) con 4 KPI cards nuevas en `DashboardAdmin.tsx`; la actividad reciente no se duplicó, ya la cubre el log de auditoría existente. Validado extremo a extremo: `bi_backend` reconstruido (primer intento falló por el bug de `Page[User]`, corregido y validado en el segundo build con arranque limpio); `pytest backend/tests/unit` (187 passed -- 186 + 1 test nuevo -- 1 falla preexistente ajena; 2 tests de `test_system_service.py` actualizados por el cambio de firma del constructor); `pytest backend/tests/integration/test_admin_actualizacion.py` (20 passed, 4 skipped, 4 tests nuevos); `tsc`/`oxlint` limpios; probado en vivo (`GET /users/?page=1&page_size=5`, `GET /analytics/admin/resumen` con datos reales: 11 activos, 36 inactivos -- acumulación conocida de usuarios de prueba, no un bug de esta fase). Pendiente: Fase 2 §2.4 (auditoría A-0.4), resto de Fase 4 (rediseño UX de Ventas), Fases 6-7. Actualización previa 2026-07-29 (Fase 4 (parcial) de `docs/features/plan_correcciones_integrales_sistema.md` -- Módulo Ventas, `docs/auditoria/42_correcciones_integrales_sistema.md` §Fase 4. §4.1 (RLS/agregación de cada gráfico) verificado sin hallazgos nuevos -- ya cerrado en la auditoría 34 (RN-V4/RN-V5) de una fase anterior. §4.2 aplicado: `Cartera360Repository.get_lista_trabajo` ya calculaba `ultima_compra` pero no la exponía; nuevo `Cartera360Service._estado_cartera(dias_sin_comprar)` deriva `activo`/`potencial`/`inactivo` con umbrales configurables (`CARTERA360_DIAS_ACTIVO=60`, `CARTERA360_DIAS_INACTIVO=180`, `config.py`); `ClienteListaTrabajo` (schema + tipo TS) gana ambos campos; `VentasCartera360.tsx` los muestra en la tabla y en el drawer (badge de color, patrón `Badge` ya existente). §4.3/§4.4 (rediseño completo del dashboard de Ventas) se evaluaron y se dejaron pendientes de una sesión dedicada -- `DashboardVentas.tsx` ya tiene estados vacíos/error reales y el proyecto ya cuenta con una herramienta más completa de una fase anterior ("Mi Ruta Inteligente de Ventas", `VentasRuta.tsx`, activable con `CARTERA360_RUTA_INTELIGENTE_ENABLED`) que cubre gran parte de ese objetivo; comprimir un rediseño de UX completo en esta pasada se descartó a favor de cerrar el hallazgo real de datos (última compra/estado de cartera). Validado: `bi_backend` reconstruido con arranque limpio; `pytest backend/tests/unit` (186 passed, 1 falla preexistente ajena); `pytest backend/tests/integration/test_cartera360_ruta_inteligente.py` (6 passed, 6 skipped); `tsc`/`oxlint` limpios; probado en vivo (`GET /analytics/ventas/cartera360/lista-trabajo` 200 con el contrato nuevo). Pendiente: resto de Fase 4 (rediseño UX), Fase 2 §2.4 (auditoría A-0.4), Fases 5-7. Actualización previa 2026-07-29 (Fase 3 y parte de la Fase 2 de `docs/features/plan_correcciones_integrales_sistema.md`, continuando el mismo plan de la actualización anterior del mismo día. **Fase 2 (parcial, §2.1-2.2):** nuevo `DateField` (`frontend/src/components/ui/DateField.tsx`, icono de calendario explícito vía `lucide-react` en vez de depender del pseudo-elemento no estándar `::-webkit-calendar-picker-indicator`, ausente en Firefox/Safari) aplicado en los 4 puntos con `<input type="date">` sueltos (`BodegaFilterBar.tsx`, `QuickLogForm.tsx`, `DashboardAdmin.tsx`, `DashboardGerencia.tsx`); filtro inteligente categoría→proveedor en Bodega (`WarehouseRepository.get_proveedores(categoria)` restringe a proveedores reales de esa categoría vía `EXISTS` contra `edw.fact_compras`, `GET /analytics/bodega/filtros?categoria=...`, `BodegaFilterBar.tsx` limpia el proveedor al cambiar de categoría y deshabilita el selector con una leyenda explícita si no hay proveedores -- `helper` nuevo en `FilterField`). Quedan pendientes §2.3 (`ComboboxField`) y §2.4 (auditoría A-0.4 de alineación TS↔Pydantic). **Fase 3 completa (Módulo Gerencia, `docs/auditoria/42_correcciones_integrales_sistema.md` §Fase 3):** §3.1 -- `GET /gerencia/goals/commissions` y `GET /gerencia/goals/tracking` ganan `vendedor` opcional (filtra por `edw.dim_vendedor.nombre_vendedor` en `goal_repository.py`, mismo criterio que el selector ya existente de `DashboardGerencia.tsx`); sin selección, comportamiento previo intacto (vista agregada de todos los vendedores); selector nuevo en `CommissionTracker.tsx`/`GoalsConsole.tsx` reutilizando `useVendedores`. §3.2, §3.3 y §3.4 se auditaron y se encontraron **ya resueltos de fases previas no documentadas explícitamente como tales**, sin requerir cambios de código: los gráficos "Histórico y Predicción" (`sales_rf`, en $) y "Predicción de Compras" (`demand_rf`, en unidades) ya separan escalas sin porcentajes cruzados, con tooltips reales y ambos contratos en `status: "active"` (bloqueo automático de `plausible_range` ya vigente); el flujo Aprobar/Rechazar/Modificar de metas ya está cableado en `GoalsConsole.tsx` con simulación previa en un panel dedicado (`CommissionSimulationPanel.tsx`, meses editables 3/6); los paneles "Vendedores en riesgo"/"Alta probabilidad"/"Recomendaciones por categoría" (`GoalsAISummaryPanel.tsx`) ya calculan todo desde el EDW real (`GoalMLService.classify_vendor_risk`/`get_category_recommendations`, este último reutilizando el modelo `association`), sin ningún valor estático. Validado extremo a extremo: `bi_backend` reconstruido, arranque limpio (6/6 modelos cargados); `pytest backend/tests/unit` (186 passed, 1 falla preexistente ajena, `classify_vendor_risk`, dependiente de la fecha del sistema); subconjunto de integración de metas/comisiones (20 passed, 3 skipped, 1 failed -- `test_meta_sugerida_vendedor_expone_trazabilidad_del_motor`, preexistente y ajeno: depende de que el vendedor `102` siga teniendo suficiente histórico en el EDW real a la fecha de la corrida, sin relación con ningún cambio de esta sesión); `tsc --noEmit`/`oxlint` limpios en los 9 archivos frontend tocados entre ambas fases. Pendiente: resto de Fase 2 (§2.3-2.4) y Fases 4-7 del plan (Ventas, Administración, Bodega, Dashboard por rol). Actualización previa 2026-07-29 (Fase 1 de `docs/features/plan_correcciones_integrales_sistema.md` -- cierre del hallazgo de seguridad más grave del plan (H-1, docs/auditoria/42_correcciones_integrales_sistema.md): el módulo Bodega no tenía segregación de datos por almacén -- cualquier usuario con rol `bodega` podía leer/exportar KPIs, stock, rotación, transferencias y el Excel completo de **cualquier** bodega cambiando `?almacen=<otro>` en la URL, sin relación con la que el admin le hubiera asignado. Corregido con RLS real: migración Alembic `0006_usuario_almacenes` reemplaza el modelo 1:1 `usuarios.codalm` (que solo distinguía "una" de "todas") por una relación N:N (`public.usuario_almacenes` + columna real `usuarios.todos_los_almacenes`) -- decisión del usuario, corrigiendo una respuesta previa: "un usuario puede ver varias bodegas y otro solo la suya, depende de cómo lo creó el admin". Nueva dependencia `resolve_almacenes_filter` (`app/api/dependencies.py`) resuelve la restricción desde el usuario autenticado (no desde el JWT, que nunca llevó `codalm`); se inyecta en el **constructor** de `WarehouseRepository` (no como parámetro threaded por cada función) y `_filtros_snapshot` -- ya el único choke point de filtros del repositorio -- pasa de `@staticmethod` a método de instancia que aplica `al.codalm IN (...)` en AND con el filtro `almacen` que el usuario elija (intersección real, nunca ampliación; lista vacía = no ve nada, nunca "todo" por defecto), cubriendo las ~14 funciones públicas del repositorio y los 13 endpoints de `warehouse.py` sin tocarlos uno por uno. `notification_service.py` migrado de leer `user.codalm` (columna eliminada) a delegar en la RLS ya inyectada. `UserCreate`/`UserUpdate`/`UserOut`/`UserMe` cambian `codalm: str` por `codalms: list[str]`; frontend con selector múltiple en `UsersManagement.tsx`. Regla nueva RN-B10 en `docs/auditoria/02_reglas_negocio_validadas.md` §16. Validado extremo a extremo: migración aplicada contra `bi_postgres_edw` real, `bi_backend` reconstruido; `pytest backend/tests/unit` (182 passed, 1 falla preexistente no relacionada); `pytest backend/tests/ -m integration` contra el EDW real (114 passed, 4 skipped, 1 falla preexistente no relacionada en el módulo de Metas, ajena a esta fase); test de integración nuevo `test_bodega_rls.py` (6 casos: una bodega pidiendo otra, varias bodegas, `todos_los_almacenes`, alta sin asignación rechazada, RLS también en el export Excel -- el vector de fuga de mayor volumen -- todos verdes); `tsc`/`oxlint` limpios en los archivos nuevos del frontend. **Ampliación el mismo día (petición explícita del usuario):** "el filtro de bodega no debe mostrar otras bodegas si el usuario solo se creó específicamente para una bodega, pero si el usuario se creó para todas las bodegas sí puede ver ese filtro con todas las demás" -- `GET /analytics/bodega/filtros` ahora inyecta la misma `resolve_almacenes_filter` de la RLS y traduce los `codalm` permitidos a `nombre_almacen` vía `CatalogRepository.list_almacenes()` antes de devolver el catálogo, cerrando por completo el gap de UX que había quedado documentado como pendiente (ya no hace falta tocar `BodegaFilterBar.tsx`, que ya consumía la lista tal cual venía del backend). `test_bodega_rls.py` se reestructuró con fixtures `scope="module"` (un usuario de prueba por perfil, no uno por test) para no agotar el rate limit real de `/auth/login` al sumar la séptima prueba. Suite de integración completa re-ejecutada tras el cambio: 115 passed, 4 skipped, 1 failed (mismo único fallo preexistente y ajeno del módulo de Metas). Pendiente el resto del plan (Fases 2-7: filtros inteligentes, Gerencia, Ventas, Admin, resto de Bodega, Dashboard por rol) y B-3 (sucursal vs. bodega para el rol `ventas`, pendiente de la auditoría A-0.3). Actualización previa 2026-07-28 (Fases 3, 4, 5 y 6 de `docs/features/plan_refactor_venta_cruzada_ia.md` -- cierre del refactor de Venta Cruzada tras el resultado negativo de la Fase 2 (el ranker `cross_sell_ranker` no se promovió), aplicando la opción recomendada del plan (§Consecuencia de la Fase 2: "el módulo conserva el motor actual y las Fases 1, 3-6 siguen aportando valor por sí solas") bajo una restricción explícita del usuario: **ningún campo del response de ningún endpoint nuevo puede ser simulado, inventado o rellenado con un placeholder -- todo dato debe salir de una consulta real al EDW o de un modelo ya entrenado**. Detalle completo por fase en `docs/auditoria/40_refactor_venta_cruzada.md` §Fase 3/4/5/6 aplicada; reglas nuevas RN-CS7..RN-CS10 en `docs/auditoria/02_reglas_negocio_validadas.md` §17. **Fase 3 (simulador de canasta):** `POST /cross-selling/simular` (`CrossSellEngineService.simular_venta`) devuelve `ticket_estimado` (suma real de precios), `margen_estimado` (suma real de márgenes, `None` si falta el costo de cualquier producto -- nunca una suma parcial engañosa), `incremento_vs_ticket_promedio_cliente` y `probabilidad_recompra` (reutiliza `100 - probabilidad_abandono` de `churn_rf`, ya calculado en Fase 1), con una `explicacion` de plantilla determinista (sin LLM, decisión ya tomada al inicio del plan); no expone "probabilidad de cierre de venta" ni "probabilidad de compra por producto" (ambos dependían del ranker no promovido). RLS igual que el resto del módulo. Bug real corregido: una rama `if not items: raise ValidationError` muerta en el servicio (Pydantic ya rechaza con 422 antes de llegar ahí) -- retirada junto con el import no usado. **Fase 4 (combos inteligentes):** `GET /cross-selling/combos` con 4 estrategias 100% reales -- Oferta Estrella (coocurrencia real de facturas), Mayor Rentabilidad (nuevo `CatalogRepository.get_top_margen_relativo`, top-1 por categoría con costo y venta reales), Cliente Frecuente (solo con `cliente_id`, `popularidad` = % real del gasto histórico del cliente) y Protección Total; "Ideal para Flotas" queda fuera (sin definición de "flota/corporativo" derivable del EDW, ya decidido). Los campos `confianza`/`incremento_esperado` del diseño original del plan se OMITEN del contrato en vez de rellenarse sin base real. Hallazgo de calidad de datos no corregido en esta fase (queda documentado): algunos productos muestran `margen_unitario`≈99.998% del precio porque `dim_producto.costo_promedio` está casi en $0 para esos artículos -- dato real del EDW, no un bug de este código. **Fase 5 (decomposición del score):** `SugerenciaProducto.factor_margen` (real, `1.0` neutro) expuesto junto a `score` en `/cross-selling/sugerencias`; como el ranking final resulta de MULTIPLICAR `score × factor_margen` (no sumarlos), el frontend (`ScoreDecompositionBar.tsx`) renderiza dos medidores independientes normalizados en vez de una barra apilada aditiva, que habría sido una representación visual falsa de la composición real. **Fase 6 (explicabilidad real vía SHAP):** `GET /cross-selling/clientes/{id}/explicacion-churn` (`PredictionService.get_churn_explanation`, `shap.TreeExplainer` sobre `churn_rf`, con manejo explícito de la forma 2D/3D de `shap_values` según el algoritmo ganador de la competencia -- XGBoost/LightGBM vs. RandomForest/CatBoost) expone la contribución real de cada feature RFM al riesgo de abandono; el panel del frontend (`WhyExplanationPanel.tsx`) se etiqueta explícitamente "Explicación del modelo (SHAP)", nunca "IA", cumpliendo R-3 del plan (no fingir IA generativa donde hay un cálculo determinista real); consulta bajo demanda (solo al expandir el panel), con caché en memoria por cliente. Nueva dependencia `shap>=0.46.0,<0.47.0` en `backend/requirements.txt` (instalada en vivo en el contenedor para iterar; pendiente de confirmar con una reconstrucción completa de la imagen). Validado extremo a extremo: suite completa de `backend/tests/` (282 passed, 3 skipped, 8 failed -- los mismos 8 fallos preexistentes ya documentados desde antes de esta sesión, sin relación con los cambios: 4 por `ML_MODELS_DIR` no resuelto en el entorno de pruebas, 3 de metas/`goal_ml_service`, 1 de `classify_vendor_risk`; 7 tests más que la corrida anterior por los nuevos de integración de Fase 3/4); `tsc`/`oxlint` limpios en todos los archivos nuevos del frontend; probado en vivo contra `bi_backend`/`bi_frontend` reales (simulador con canasta real, los 4 combos con y sin cliente, decomposición del score visible, panel SHAP expandido con contribuciones coherentes). Pendiente, correctamente diferido por el propio plan: Fase 7 (ciclo de retroalimentación con telemetría de aceptación) requiere ≥2.000 eventos en `public.recomendaciones_eventos` y solo hay 79 (A0-1 de la Fase 0) -- no implementada. Gaps conocidos, no bloqueantes: sin test de integración dedicado para `factor_margen` (Fase 5) ni para el endpoint de explicación SHAP (Fase 6), solo verificación manual en vivo; verificación visual en navegador real sigue sin ser posible en este entorno Windows (sin `chromium-cli`). Actualización previa 2026-07-28 (Fase 2 de `docs/features/plan_refactor_venta_cruzada_ia.md` -- entrenamiento real del 7º modelo `cross_sell_ranker` (ranking supervisado sobre los candidatos de `association`), con resultado NEGATIVO documentado completo (no promovido, no es un fallo: es el criterio de decisión del plan funcionando -- ver docs/auditoria/40_refactor_venta_cruzada.md §Fase 2 aplicada y `ml/REPORTE_MEJORA_MODELOS.md`). Contrato nuevo `ml/contracts/models/cross_sell_ranker.json` queda en `status: draft` (no se activó). Dataset con cortes temporales mensuales (`ml/src/features/cross_sell_ranker_features.py::construir_dataset_ranking`, 12 cortes, 261.105 filas, 26.87% positivos) sobre `fetch_cross_sell_transacciones` (nuevo en `make_dataset.py`): positivos = producto comprado por primera vez en `(T,T+60d]`, negativos = candidatos que `recommendation.pkl` (item-item) habría propuesto en `T` + respaldo de popularidad, ratio 3:1; `p_abandono`/`segmento_rfm` calculados aplicando `churn_rf`/`segmentation` YA ENTRENADOS sobre el RFM del cliente al corte (features de entrada, no solo servicios aparte). Entrenamiento con la competencia RF/XGBoost/LightGBM/CatBoost existente (`ml/src/training/train_cross_sell_ranker.py`), ganador LightGBMClassifier, split cronológico 9/12 cortes (nunca aleatorio). Backtest de ranking reutilizando el arnés de la auditoría 25 (`ml/src/training/backtest_recommendation.py`, nuevas `construir_canastas_con_contexto`/`evaluar_estrategia_reranked`): línea base item-item fresca Precision@5=0.0782 (consistente con el 0.0769 histórico), ranker=0.0369 -- **peor que la línea base**. Hipótesis con evidencia (no solo "no funcionó", ver auditoría 40): sesgo de distribución train/serving en el muestreo de negativos -- en entrenamiento el modelo distingue el positivo de ~4 candidatos (ratio fijo 3:1), en el backtest debe ordenar hasta 50 candidatos reales, una tarea de ranking mucho más difícil que nunca vio en entrenamiento; consistente con `hit_rate_5` cayendo de 0.36 a 0.18 y el ranker sobre-priorizando productos de mayor precio/margen. Deliberadamente NO se reintentó con otros hiperparámetros/ratios para forzar una promoción (evitar p-hacking); el cambio de diseño que la evidencia sí justifica (si se retoma) es muestrear negativos del pool completo de ~50 candidatos de producción, no un ratio fijo. Consecuencia: el módulo de Venta Cruzada sigue sirviendo el motor item-item actual sin cambios (`registry.json` no se tocó, sin cambios de serving en el backend). Actualización previa 2026-07-28 (Fase 0-1 de `docs/features/plan_refactor_venta_cruzada_ia.md` -- inicio del refactor del módulo de Venta Cruzada hacia un motor de recomendación con ranking aprendido (7º modelo ML del proyecto, aún no entrenado). Auditoría previa obligatoria `docs/auditoria/40_refactor_venta_cruzada.md`: confirmó viabilidad de datos para el ranker de la Fase 2 (A0-0: ~4.765 pares positivos `(cliente, producto)` por corte mensual sobre 95 cortes posibles, sin riesgo de volumen) y cuantificó con evidencia propia el techo estructural de personalización que el contrato de `recommendation.pkl` ya documentaba (A0-5: intersección=0/20 entre el top-20 de coocurrencia de las 2 sucursales de mayor volumen, que juntas son 67% del negocio) -- obliga a que `sucursal` entre como feature explícita del ranker en la Fase 2, no solo implícita vía categoría. También cuantificó el arranque en frío real de la cartera (A0-3: 57.8% de clientes con una sola compra, 77.1% con menos de 3 meses de historial) y descartó por bajo volumen (A0-1: 79 eventos, 2 aceptadas) usar la telemetría de aceptación como feature del ranker hasta acumular ≥2.000 eventos. Fase 1 implementada (CAMBIO 1 del plan, decisiones de negocio: CLV histórico no predictivo, sin reemplazo del KPI "probabilidad de cierre de venta", perfiles "corporativo"/"flota" omitidos por no ser derivables del EDW): `CatalogRepository.search_clientes` enriquecido con `ciudad`/`ultima_compra`/`frecuencia_12m`/`n_compras`; `Cartera360Repository.get_perfil_cliente`/`get_productos_favoritos_cliente` nuevos (CLV histórico = `SUM(subtotal_neto)` de toda la historia del cliente, sin filtrar por vendedor, a diferencia de `get_lista_trabajo` que sí filtra por cartera de un `codven`); servicio nuevo `app/services/cross_sell_engine_service.py` (decisión de arquitectura del plan: no se engordó `PredictionService`, se compone por inyección igual que `Cartera360Service`); endpoint nuevo `GET /analytics/ventas/cross-selling/clientes/{cliente_id}/perfil` con RLS obligatoria (mismo `_verificar_pertenencia_cartera` de la auditoría 34 H-V2) y `probabilidad_recompra` derivada de `churn_rf` (`100 - probabilidad_abandono`, no un modelo aparte); estado vacío real (`tiene_historial=false`, campos `null`) para clientes sin ventas, nunca ceros inventados (RN-CS5). Frontend: `store/crossSellStore.ts` (Zustand, cliente como estado raíz de la página de Venta Cruzada, sin `persist` porque trae PII real) y `components/crossSelling/ClientProfileCard.tsx` (skeleton por sección, estado vacío real). Reglas nuevas RN-CS4/RN-CS5 en `docs/auditoria/02_reglas_negocio_validadas.md` §17. Validado extremo a extremo: `pytest` nuevo de integración (4 tests: 403 cliente ajeno, 404 cliente inexistente, estado vacío real) + suite completa de backend (275 passed, 3 skipped, 8 failed preexistentes no relacionados -- 4 por `ML_MODELS_DIR` no resuelto en este entorno de pruebas, 3 de metas/`goal_ml_service`, 1 de `classify_vendor_risk`, ninguno toca código de esta fase); `tsc`/`oxlint` limpios sobre los archivos nuevos; probado contra `bi_backend`/`bi_frontend` reales corriendo (login real, autocompletar y perfil probados con datos reales del EDW, RLS 403 confirmado en vivo). Pendiente: Fases 2-7 del plan (el ranker es la Fase 2, el cambio central), verificación visual en navegador (sin `chromium-cli` disponible en este entorno Windows) y medición de latencia base (A0-6). Actualización previa 2026-07-20 (Fase 4 de `docs/features/plan_mejora_pipeline_ml.md` -- revisión de los 6 modelos con buenas prácticas de entrenamiento: limpieza/split/CV, búsqueda de hiperparámetros donde faltaba, baseline estadístico `statsmodels`, métricas completas por tipo, y detalle en `docs/auditoria/38_mejora_pipeline_ml.md` §8. Nuevo `ml/src/utils/stats_baseline.py::fit_ols_baseline` (`smf.ols(...).fit()` sobre el split de entrenamiento, nunca el holdout) aplicado a ventas y demanda: el `summary()` completo se guarda en `ml/models/diagnostics/<clave>_ols_baseline.txt` y un resumen (R² ajustado, F-test, VIF, p-values significativos) en el `.meta.json` -- hallazgo real: en ventas, el OLS (R²_ajustado=0.3126) **iguala o supera levemente** al RandomForest ganador (R²=0.2907), la señal de alerta que el propio plan anticipaba, documentada para la model card (Fase 5) sin cambiar el algoritmo ganador en esta pasada (eso requeriría una nueva competencia formal, ver auditoría 22). Métrica WAPE añadida a `evaluate_reg` (`model_selector.py`) para ventas/demanda. **Churn (D-7, petición explícita del usuario):** se agregó `recency` como 4ª feature (ya se calculaba en el dataset sin fuga -- H-05 -- pero nunca se entrenaba con ella, pese a ser la señal RFM típicamente más predictiva de abandono); `evaluate_churn_classifier` ahora devuelve matriz de confusión, precision/recall/F1/PR-AUC y una calibración de umbral (F1 óptimo, solo diagnóstico -- el serving real sigue usando `CHURN_UMBRAL_RIESGO_ALTO`, un umbral de negocio distinto). Contrato `churn.json` v0.1.0→v0.2.0; sincronizado el serving (`ChurnFeatures`/`get_churn_features`/`get_churn_features_batch` en `prediction_repository.py`, `prediction_service.py`) con la feature nueva. **Segmentación:** nuevo `evaluar_estabilidad_k` (barrido K=2..8 con inertia/silhouette/Davies-Bouldin, guardado en el sidecar) documenta que K=4 no es arbitrario (mejor Davies-Bouldin de K>=3); se agregan `davies_bouldin` y `perfil_clusters` (centroides RFM reales por segmento) al `.meta.json`. **Anomalías:** `contamination` deja de ser un `0.01` fijo sin evidencia -- nuevo `estimar_contamination_iqr` (regla IQR sobre `margen`, mismo criterio que `IQRGoalCalculationEngine`, acotado a `[0.005, 0.05]`); corrida real: outliers IQR=5.71% -> `contamination=0.05`. **Asociación/Recomendación -- hallazgo real, no solo "formalizar":** `train_recommendations` (`ml/main.py`) entrenaba co-ocurrencia (`min_support=0.005`, sin ventana), un algoritmo DISTINTO al ganador real ya publicado (item-item, ventana 2 años, vía un script aparte `ml/notebooks/publicar_ganador_cross_selling.py`); como esa función es la que invoca el reentrenamiento automático (`ml/retrain_all.py`, Fase 3), un reentrenamiento real habría intentado sobrescribir el artefacto ganador con uno peor -- neutralizado hasta ahora solo porque `metrics` nunca traía `precision_at_5` y el gating rechaza por seguridad cualquier candidato sin la métrica del `metric_gate` (nunca promovía, pero tampoco medía nada real). Corregido: ahora entrena item-item (mismo algoritmo, ventana y backtest temporal que el ganador documentado) y puebla `precision_at_5`/`recall_at_5`/`hit_rate_5`/`cobertura` reales para que el gating compare de verdad. Validado extremo a extremo: imagen `ml` reconstruida (agrega `statsmodels` a `ml/requirements.txt`), `python retrain_all.py --model all` -- **6/6 modelos promovidos**, ninguno rechazado (asociación: precision_at_5=0.0767 nuevo vs. 0.0769 campeón anterior, dentro del `min_delta`); `pytest` verde en `ml/tests/` (16, el test de contrato de churn fallaba antes de reentrenar porque el `.pkl` en disco aún no tenía `recency` -- esperado en un flujo contrato-primero) y `python -m src.contracts.contract_validator` limpio (6/6). Backend reconstruido: `GET /health` → `modelos_ml_listos: true`; `GET /analytics/ventas/churn-risk` probado con 3 `cliente_id` reales del EDW (inferencia con `recency` funcionando end-to-end, una probabilidad cruza el umbral de riesgo alto). `pytest` en `backend/tests/` (169 passed, 1 falla preexistente no relacionada, dependiente de la fecha del sistema). Pendiente: Fase 3b (panel MLOps del frontend), Vía B (documentación de tarea programada de Windows), Fase 5 (`ml/model_cards/*.md` + generador + índice). Actualización previa 2026-07-20 (Fase 3 de `docs/features/plan_mejora_pipeline_ml.md` -- gating/promoción automática de campeón único y disparo real de reentrenamiento desde el panel de administración. Módulo nuevo `ml/src/training/promotion.py::evaluar_y_promover`: se invoca DESPUÉS de que la función `train_*` ya entrenó y guardó (Fase 1 ya sobrescribe el archivo estable + archiva una versión) -- compara candidato vs. campeón anterior con el `metric_gate` de `registry.json` (unifica maximize/minimize/target con un cálculo de "mejora" con signo) y, si rechaza, REVIERTE el archivo estable a la versión anterior copiando desde `versions/<clave>/`; si no hay campeón previo comparable, promueve por defecto. Deja traza en `public.ml_model_runs` (tabla nueva, migración Alembic `0004_ml_model_runs`, append-only, mismo patrón que `comision_config_auditoria`) y en `ml/REPORTE_MEJORA_MODELOS.md`. Nuevo `ml/retrain_all.py` (CLI `--model <clave|all>`) es el entrypoint real de reentrenamiento -- reemplaza el flujo previo de `TrainingService`, que estaba roto en la práctica: ejecutaba `python <script>.py` sobre cada archivo de `ml/src/training/`, pero esos módulos solo definen funciones (ningún `__main__`), así que "reentrenar" nunca entrenaba nada y siempre reportaba éxito falso -- bug encontrado durante esta fase, no documentado antes. Nuevo `ml/promote.py` (CLI `--model <clave> --to <version>`) para promoción manual/rollback sin reentrenar. Backend: `TrainingService` reescrito para disparar `docker run --rm -v "<HOST_PROJECT_DIR>\ml:/app" --network proyect_bi_default -e PG_... proyect_bi-ml python retrain_all.py ...` -- decisión validada empíricamente en este entorno (Docker Desktop/Windows): `docker compose run` NO funciona invocado desde dentro de un contenedor porque Compose necesita LEER localmente tanto el YAML como el `.env` que auto-carga para interpolación (100% client-side), y este contenedor no tiene una ruta local que corresponda a `HOST_PROJECT_DIR` (con backslashes de Windows) -- fallaba con `env file ... not found`. `docker run -v` en cambio solo necesita que el DAEMON (que sí corre en el host real) resuelva el string del bind-mount, y Docker Desktop acepta rutas estilo Windows ahí sin que el cliente las vea localmente -- comprobado con una corrida real end-to-end. Endpoints nuevos en `admin_ml.py`: `POST /admin/modelos/retrain` (ahora acepta `{"clave": "<modelo>"}`, default `"all"`, y de verdad entrena+aplica gating), `POST /admin/modelos/promote`, `POST /admin/modelos/rollback`, `GET /admin/modelos/runs` (paginado, `public.ml_model_runs`), `GET /admin/modelos/{clave}/versions` (versiones archivadas disponibles). Bug adicional encontrado y corregido durante la validación (Fase 1, no exclusivo de Fase 3): la retención de versiones en `save_artifact`/`_save_version_snapshot` (`ml/src/utils/model_export.py`) calculaba el "stem" de cada archivo con `os.path.splitext`, que para `<version>.meta.json` da `<version>.meta` (dos extensiones) en vez de `<version>` -- duplicaba el conteo de versiones y podía borrar un lado del par `.pkl`/`.meta.json` dejando el otro huérfano (reproducido en vivo). Validado extremo a extremo con una corrida real disparada vía la API HTTP real (no simulada): reentrenamiento de `segmentation` con gating, promoción automática, y verificado con `GET /admin/modelos/runs` (6 filas: promociones, un rechazo forzado con `min_delta` imposible que restauró el `.pkl` anterior byte a byte -- hash MD5 idéntico antes/después del rechazo -- y una promoción manual/rollback vía `ml/promote.py`) y `GET /admin/modelos/{clave}/versions`. `pytest` verde en `ml/tests/` (16) y `backend/tests/` (170, misma falla preexistente no relacionada de siempre). Pendiente: Vía B (tarea programada Windows, solo documentación por decisión ya tomada), panel MLOps del **frontend** (hoy no existe ningún panel visible -- se confirmó que el hook `hooks/admin.ts` existe pero ningún page/component lo consume, igual que se encontró con `/demand-forecasting` en Fase 2), Fase 4 (revisión de los 5 modelos restantes con `statsmodels`) y Fase 5 (model cards). Actualización previa 2026-07-20 (Fase 2 de `docs/features/plan_mejora_pipeline_ml.md` -- rediseño del modelo de demanda (`demand_rf`) de serie global apilada por producto a grano `(fecha, codart, almacén)`, decisión de negocio "la reposición se decide por bodega". Detalle completo en `docs/auditoria/38_mejora_pipeline_ml.md` §6. Resumen: nuevo dataset `ml/src/data/make_dataset.py::fetch_demand_by_product_warehouse` sobre `fact_movimientos_inventario` (salidas físicas, no `fact_ventas_detalle`), excluye la clase `Z-999` (chatarra -- el SKU `Z-9001` "BATERIAS CHATARRAS" era el de mayor error absoluto en el backtest, no es un artículo de reposición); umbral mínimo de historia `ML_DEMANDA_MIN_MESES_VENTA` (default 6 de 36 meses) filtra las combinaciones demasiado ralas (75% de las 12.653 combinaciones detectadas) hacia el pronóstico estadístico ya existente en vez del modelo; `almacen_sk` nueva feature, lags/rolling/expanding agrupados por `(producto, almacen)` en `ml/src/features/build_features.py`; `hyperparameter_search=True` (antes demanda era el único regresor sin búsqueda); contrato `ml/contracts/models/demand.json` v0.1.0→v0.2.0. Backtest comparativo (mismo método por decil de volumen que la auditoría): el decil de mayor volumen -- antes el punto débil -- mejoró de MAE=40.15/RMSE=561.14 a MAE=11.98/RMSE=40.46; el R2 global cae de 0.876 a 0.133, cambio esperado (antes inflado por SKU de volumen enorme como el ahora excluido Z-9001), no una regresión. Serving sincronizado: `dataset_repository.py::get_product_sales_history` gana `almacen` opcional; `backend/app/ml/preprocessing.py` agrupa igual que el entrenamiento; se corrigió un bug real en `backend/app/ml/forecasting.py::walk_forward_forecast` (ponía TODA la fila simulada en 0.0, lo que habría borrado `almacen_sk`/`producto` en cada paso del walk-forward -- ahora conserva la identidad y solo resetea el target); `WarehouseService._forecast_ml_producto` y sus callers (`get_salidas_forecast`, `_prediccion_articulo`) ya pasan el `almacen` que el dashboard de Bodega conoce. El endpoint legado `/demand-forecasting` (sin almacén en su contrato, confirmado sin consumidores reales en el frontend -- el hook existe pero ningún page/component lo usa) degrada a `0.0` en vez de dar una predicción ML, trade-off documentado en `known_serving_mismatch` del contrato, no un descuido. Validado extremo a extremo: `contract_validator` limpio, `pytest` verde en `ml/tests/` (16) y `backend/tests/` (170, 1 falla preexistente no relacionada y dependiente de la fecha del sistema), y probado en vivo contra `bi_backend` reconstruido -- `GET /analytics/bodega/salidas-forecast?producto_cod=...&almacen=...` responde `metodo: ml_demand_rf` con predicciones en rango plausible. Fases 3-5 del plan (gating/promoción automática, panel MLOps del frontend, model cards) quedan pendientes. Actualización previa 2026-07-20 (Fases 0-1 de `docs/features/plan_mejora_pipeline_ml.md` — mejora del pipeline de ML: campeón único por proceso, base para reentrenamiento automático. Auditoría previa `docs/auditoria/38_mejora_pipeline_ml.md`: backtest reproducible (nuevo `ml/scripts/backtest_demand_por_sku.py`) del modelo de demanda (`demand.pkl`) por SKU confirmó cuantitativamente D-1 (el modelo entrena como serie global apilada, sin distinguir producto) — el decil de mayor volumen tiene MAE 40x y RMSE 150x peor que los deciles bajos, con un solo SKU atípico (`Z-9001`) concentrando la mayor parte del error; conteo de filas por `(producto, almacén)` confirmó con evidencia dura el riesgo de series ralas de la Fase 2 (66.5% de combinaciones con actividad en ≤3 de 36 meses); verificación de paridad de filtros entrenamiento↔serving para demanda sin hallazgos. Fase 1 implementada: `ml/models/registry.json` (nuevo) es ahora la fuente de verdad de qué `.pkl` es el campeón vigente por proceso; `save_artifact` (`ml/src/utils/model_export.py`) gana `registry_key` opcional y archiva cada entrenamiento en `ml/models/versions/<clave>/<version>.{pkl,meta.json}` con retención configurable (`ML_RETENCION_VERSIONES`, default 5) — el entrenamiento sigue escribiendo el archivo estable de siempre, la promoción real a campeón (mover el puntero de `registry.json`) queda para la Fase 3 (gating), no implementada aún; `backend/app/ml/model_loader.py` resuelve el archivo de cada modelo leyendo `registry.json` con fallback automático a los nombres fijos `_MODEL_FILES` si el registro no existe (cambio 100% aditivo); los 9 artefactos legacy sin contrato (`*_rf_model.pkl`, `*_best_model.pkl`, `association_rules.pkl`, `isolation_forest_model.pkl`, `kmeans_rfm_model.pkl`) se movieron a `ml/models/versions/_legacy/`; nuevo test de guardia `ml/tests/test_registry.py` (falla si un `.pkl` en la raíz de `ml/models/` no está referenciado por el registro). Validado extremo a extremo reconstruyendo `bi_backend`: los 6 modelos cargan vía el registro sin WARNING/ERROR, `GET /health` → `modelos_ml_listos: true`; suite `ml/tests/` (16 tests) y subset de `backend/tests/unit` de ML en verde. Además, precondición de infraestructura para la Vía A de reentrenamiento (§5.2.1 del plan, decisión de negocio confirmada 2026-07-20 de exponer el socket de Docker ahora en vez de solo documentarlo): `docker-compose.yml` monta `/var/run/docker.sock` en el backend; `backend/Dockerfile` instala `docker-ce-cli`+`docker-compose-plugin`+`gosu` y ya NO fija `USER appuser` en build (el contenedor arranca como root); `backend/entrypoint.sh` ajusta el socket montado (`chmod 666`, necesario porque en este entorno Docker Desktop lo expone `root:root`, no `root:docker` con un GID predecible — verificado empíricamente, no asumido) y usa `gosu appuser` para que el proceso real de uvicorn siga corriendo sin privilegios de root (confirmado vía `/proc/<pid>/status`: Uid 1000); validado que `appuser` (no root) puede ejecutar `docker ps` contra el daemon del host desde dentro del contenedor. El código que dispara `docker compose run ml ...` (`TrainingService`, endpoints `promote`/`rollback`, tabla `ml_model_runs`, panel MLOps del frontend) es Fase 3, pendiente. Riesgo de seguridad de este cambio explícitamente aceptado por el usuario, no un descuido — ver §11 del plan. Actualización previa 2026-07-16: Revisión de planes en `docs/features/` pendientes de cerrar: la mayoría ya estaba implementada (verificado cruzando cada plan contra `docs/auditoria/*.md` y el código real); se cerraron 5 brechas genuinas encontradas -- **Ventas**: filtro "solo riesgo alto" en `VentasCartera360.tsx` (el backend ya calculaba `riesgo_alto` por cliente pero la lista de trabajo no lo exponía; se agregó al schema/tipo y al filtro de la tabla) y `CrossSellKpiPanel` (ya existía y se usaba en Ventas) montado también en `DashboardGerencia.tsx` (el endpoint `/cross-selling/kpis` ya estaba abierto a gerencia por RBAC pero nunca se había montado ahí). **Metas y Comisiones** (Fase 2 de `plan_actualizacion_modulo_metas_comisiones.md`, antes con 3 ítems sin cerrar): (1) el endpoint `/gerencia/goals/meta-sugerida` con el desglose IQR completo ya existía pero no estaba wireado a ningún lado del frontend -- se agregó un drawer de detalle en `GoalsConsole.tsx` (clic en el vendedor) que lo consume, para lo cual `GoalCommissionReportItem`/`get_commission_report` ganaron `vendedor_origen` (antes solo tenían el nombre, no el código SAP necesario para pedir el desglose); (2) bitácora de cambios de configuración de comisiones -- tabla nueva `public.comision_config_auditoria` (migración Alembic `0003_comision_config_auditoria`, append-only) que registra usuario/tabla/acción/detalle en cada upsert de matriz de categorías, reemplazo de factores de crédito o cambio de tipo de vendedor (los dos últimos endpoints no capturaban `current_user` antes de esto), con una pestaña "Bitácora de cambios" nueva en `CommissionConfigPanel.tsx`; (3) alerta de divergencia plano vs. variable del piloto en sombra -- nuevo generador calculado `NotificationService._generar_divergencia_comisiones` (activo solo con `COMISION_MODO=sombra`, umbral `NOTIF_DIVERGENCIA_COMISION_PCT`), reutiliza `CommissionSimulationService.simular` sobre el último mes cerrado, sin cálculo nuevo. Fuera de estos 5 puntos, no se encontró código faltante -- lo demás pendiente en `plan_correcciones_pendientes.md` §4-5 y `plan_mejoras_proyecto.md` (M-13 a M-24) es deuda de infraestructura/EDW/CI conocida, ya catalogada, sin acción de código pendiente en esta sesión. Bug encontrado y corregido durante la validación: una nueva dependencia FastAPI (`get_commission_simulation_service`, inyectada en `get_notification_service`) se había definido DESPUÉS de su primer uso en `app/api/dependencies.py` -- Python 3.14 (entorno local) no lo marca por PEP 649 (anotaciones diferidas), pero Python 3.11 (imagen Docker real) sí falla con `NameError` al arrancar; reordenado antes de su uso, validado reconstruyendo el contenedor real. Validado extremo a extremo reconstruyendo `bi_backend`: migración 0003 aplicada, tabla creada, endpoint de auditoría probado con un cambio real (creó y limpió una regla de prueba). Actualización previa 2026-07-16: Migraciones versionadas del esquema `public` con Alembic — `docs/features/plan_migraciones_esquema_public.md`, auditoría `docs/auditoria/37_migraciones_esquema_public.md`: resuelve D-1..D-5 del plan, las tres fuentes de esquema que competían (`edw/07` solo en initdb de volumen nuevo, `Base.metadata.create_all` que nunca altera tablas existentes, y un `ALTER TABLE ... codalm` hardcodeado en el lifespan). Alembic (`backend/alembic/`) es ahora el único dueño del esquema `public`: `0001_baseline_public` crea las 12 tablas modeladas con DDL congelado (deliberadamente NO delega en `Base.metadata.create_all` en tiempo de ejecución -- se probó esa vía y permitía que un cambio de modelo apareciera en instalaciones nuevas sin ninguna migración, mientras una BD ya migrada nunca lo recibía) más `public.cliente_lookup` (sin modelo SQLAlchemy) y el trigger `set_updated_at`; `0002_seed_roles` siembra los 4 roles + admin inicial de forma idempotente, con la contraseña desde `ADMIN_INITIAL_PASSWORD` (ya no el hash bcrypt fijo de `edw/08`, una puerta trasera conocida en producción). `backend/entrypoint.sh` + `backend/scripts/apply_migrations.py` aplican las migraciones automáticamente al arrancar el contenedor del backend (ENTRYPOINT del Dockerfile, antes de que uvicorn levante): detecta si la BD es pre-Alembic (existe `public.usuarios`, no existe `public.alembic_version` -- el caso de la BD de desarrollo actual y de cualquier producción inicializada con `edw/07`) y la sella con `alembic stamp 0001_baseline_public` sin re-ejecutar DDL, o crea todo desde cero si la BD está vacía. `edw/07`/`edw/08` se conservan activos en `docker-entrypoint-initdb.d` (cabecera "REFERENCIA" agregada) porque `edw/09_vistas_ml.sql` depende de `public.cliente_lookup` en el mismo `initdb` -- Alembic solo corre después, al arrancar el contenedor del backend, así que retirarlos del todo (opción recomendada del plan) habría roto la vista en un volumen nuevo. `app/main.py` ya no ejecuta `Base.metadata.create_all` ni el `ALTER TABLE` hardcodeado. Nuevo test de guardia `backend/tests/integration/test_alembic_schema_sync.py`: falla si un modelo cambia sin migración correspondiente (corre contra una BD desechable vía `ALEMBIC_TEST_DATABASE_URL`, no contra la BD compartida de desarrollo, que conserva nomenclatura heredada de `edw/07` y nunca calzaría 1:1 con `Base.metadata` aunque no haya drift real -- ver auditoría 37 H-1/H-2 para el detalle de por qué). Validado extremo a extremo con contenedores Postgres desechables: volumen vacío → `upgrade head` crea todo; BD inicializada con `edw/01..09` completo (como producción) → adopción vía `stamp`+`upgrade` preserva los usuarios semilla existentes. Actualización previa 2026-07-15 (Actualización del módulo Ventas — Fases 0-1 de `docs/features/plan_actualizacion_modulo_ventas.md`: auditoría `docs/auditoria/34_actualizacion_modulo_ventas.md` confirmó una **fuga real de RLS** (RN-V4): `churn-risk`/`recommendations`/`clientes/{id}/segmento`/`cartera360/clientes/{id}/detalle` aceptaban cualquier `cliente_id` sin verificar que perteneciera a la cartera del vendedor autenticado — cualquier `ventas` podía consultar el riesgo de fuga, las recomendaciones y el segmento de cualquier cliente del sistema; ahora `PredictionService` valida pertenencia vía `CatalogRepository.cliente_pertenece_a_vendedor` (403 si no pertenece, sin restricción para gerencia/administrador). Además, verificando el fix del selector de período (RN-V5) se descubrió que **`GET /analytics/ventas/goals` — el KPI principal del dashboard de Ventas — devolvía 500 en el 100% de los requests de cualquier usuario `ventas` real**: filtraba por una columna `m.sucursal` que no existe en `metas_comerciales_operativas` (grano `(anio, mes, id_vendedor_origen)`, regla 10), y `resolve_sucursal_filter(allow_override=False)` siempre fuerza la sucursal propia para ese rol — corregido restringiendo las metas por vendedores activos en esa sucursal en vez de por una columna inexistente. También: selector de sucursal muerto (`GlobalBranchSelector`, actualizaba estado que ningún hook consumía, y el backend nunca honra un override en este router) retirado; selector de período agregado (`GET /gerencia/goals/periods` con RBAC ampliado a `ventas`); deduplicación de doble-click en `POST cartera360/gestion`; umbral de churn "riesgo alto" movido a `settings.CHURN_UMBRAL_RIESGO_ALTO` (antes hardcodeado). Ver `docs/auditoria/02_reglas_negocio_validadas.md` §19 (RN-V4/V5). Actualización previa 2026-07-15: módulo Metas y Comisiones — Fases 0-1 de `docs/features/plan_actualizacion_modulo_metas_comisiones.md`: auditoría `docs/auditoria/35_actualizacion_modulo_metas.md` encontró y corrigió dos bugs críticos de dinero real en el esquema de Comisiones Variables (piloto `COMISION_MODO=sombra`/`variable`). **RN-CM5:** el cálculo real de comisión (`CommissionService._calcular_variable`, usado por `/commissions` y `/mi-comision`) resolvía la matriz de categorías/factores de crédito vigentes "hoy" sin importar qué período se consultaba (el fix de vigencia-por-período de la auditoría 34 H-8 solo se había aplicado a la simulación, nunca al cálculo real) — ahora ambos usan `commission_engine.fecha_referencia_periodo(anio, mes)`. **RN-CM6:** los snapshots de `public.comision_liquidaciones` con `modo='oficial'` (dinero real) se sobrescribían (`UPSERT`) en cada vista de un período cerrado si la configuración había cambiado, pese a que el modelo se documenta como "snapshot congelado" (salvaguarda 6) — ahora son genuinamente inmutables: una vez creado el snapshot oficial, se devuelve tal cual sin recalcular (el modo `sombra`/piloto sigue refrescándose a propósito). **RN-CM7:** el panel de "Simulación de Comisiones" (`POST /commission-simulation`, el argumento decisivo para que gerencia active el esquema variable) nunca calculaba los 3 bonos complementarios (venta cruzada, cliente nuevo, cobranza sana) — siempre pasaba `bonos_total=0.0`, subestimando el costo real del esquema frente a lo que efectivamente se paga; se extrajo `calcular_bonos_periodo` a `app/services/commission_bonus.py` para que simulación y liquidación real compartan exactamente la misma lógica. El resto de hipótesis del plan (grano de metas sin sucursal, manejo defensivo de campos opcionales en frontend, advertencia de plazos de crédito sin datos, clasificación por código no por `nombre_clase`, RBAC) se verificaron sin hallazgos. Ver `docs/auditoria/02_reglas_negocio_validadas.md` §18 (RN-CM5..7). Actualización previa 2026-07-15: módulo Gerencia — Fases 0-1 de `docs/features/plan_actualizacion_modulo_gerencia.md`: auditoría `docs/auditoria/33_actualizacion_modulo_gerencia.md` confirmó que el forecast de ventas (`sales_rf`) nunca respetó `categoria`/fechas porque el dataset de entrenamiento no tiene esa columna y el modelo usa una ventana continua fija de 730 días (no un rango arbitrario) — se corrigió con un aviso explícito en la UI en vez de fingir que esos filtros aplican, RN-G1; `GPKPIGerencia.ingresos_totales` ahora lo calcula el backend en SQL (antes el servicio lo descartaba y el frontend lo reconstruía sumando sucursales, una fuente que excluye sucursales con neto exactamente 0), RN-G2; nuevo endpoint `GET /system/provenance` (cualquier usuario autenticado, sin restricción de rol) reemplaza el mock estático `PROVENANCE_FACTS` que `ProvenanceRail` mostraba como estado real del sistema en cada página autenticada de los 4 roles, RN-G3. La matriz de filtros de `/gerencia/kpis`/`revenue-by-category` se verificó completa, sin gaps. Ver `docs/auditoria/02_reglas_negocio_validadas.md` §22. Actualización previa 2026-07-15: módulo Administrador — Fases 0-1 de `docs/features/plan_actualizacion_modulo_admin.md`: auditoría de caza de bugs (`docs/auditoria/36_actualizacion_modulo_admin.md`) refutó dos hipótesis del plan — `/analytics/admin/anomalies` es consulta puntual por transacción, no un listado con ventana, y `es_activo` ya se verificaba en cada request — y confirmó cuatro reales: `GET /analytics/admin/audit-logs` gana filtros de fecha/usuario/módulo + paginación `Page[T]` con ventana por defecto `ADMIN_AUDIT_LOGS_VENTANA_DIAS`; política de contraseña unificada en backend (`PASSWORD_MIN_LENGTH`/`PASSWORD_REGEX`, antes solo existía como `pattern` HTML evadible); `UserService.update()` valida colisión de email/`id_vendedor_origen` contra otro usuario (antes solo `create()` lo hacía, editar duplicado producía un 500); `POST /admin/modelos/retrain` valida `ML_SOURCE_DIR` de forma síncrona antes de encolar el reentrenamiento (antes respondía "iniciado" con éxito falso en entornos sin `ml/` montado); el formulario "Nuevo usuario" ya no preselecciona el rol `administrador` por id fijo, selecciona por nombre del catálogo real. Fase 2 (triage de anomalías, notificación de score alto, panel de salud) y el retiro de `docs/credenciales_sistema.md`/hardening de CORS quedan diferidos por decisión del usuario — no corregidos en esta sesión. Actualización previa 2026-07-15: módulo Bodega — Fases 1-5 completas de `docs/features/plan_actualizacion_modulo_bodega.md`: filtros de reportes/Excel corregidos de punta a punta, validación cerrada de `tipo_movimiento`, montos monetarios condicionados al tipo de movimiento — RN-B8 —, reorganización del dashboard, justificación estadística de transferencias — RN-B9 —, y rediseño de reportes con contrato tipado (resumen ejecutivo interpretado + secciones con columnas de negocio) coordinado backend+frontend + Excel con formato de negocio; ver `docs/auditoria/32_actualizacion_modulo_bodega.md`. Pendiente solo la sesión de validación con usuario final, no automatizable). Actualización previa 2026-07-14: Notificaciones Inteligentes Segmentadas por Rol (`docs/auditoria/31_modulo_notificaciones.md`), Comisiones Variables (`docs/auditoria/30_comisiones_variables.md`).

## Descripción del proyecto

Plataforma de Business Intelligence de punta a punta: extrae datos del ERP transaccional (SAP SQL Anywhere 17), los carga en un Enterprise Data Warehouse (EDW) dimensional en PostgreSQL 16, entrena 6 modelos de Machine Learning sobre el EDW y los sirve mediante una API FastAPI con autenticación JWT + RBAC, consumida por una SPA React con dashboards por rol (Gerencia, Ventas, Bodega, Administrador). El módulo de Metas y Comisiones NO usa ningún modelo ML (el modelo `goals_rf` fue decomisionado, ver `docs/auditoria/20_decomision_goals_rf.md`): la meta se calcula con estadística pura (IQR + tendencia reciente) sobre el histórico de Venta Neta del EDW.

El DW es la **fuente oficial** para:

- Predicción de ventas
- Predicción de reposición (demanda)
- Dashboard Gerencial
- Dashboard de Bodega
- Venta Cruzada (recomendaciones)
- Segmentación de Clientes (RFM)
- Metas y Comisiones
- Detección de anomalías (auditoría/fraude)

## Arquitectura

```
SAP SQL Anywhere 17 (ERP producción, SOLO LECTURA)
        │  SELECT vía pyodbc (etl/connectors/sqlany_connector.py)
        ▼
ETL Python (etl/orchestrator.py)
  - extractors/*.sql con tokens {CODEMP}, {ESTADO}, {FECHA_DESDE}
  - transformers/ (dims SCD2, hechos, dim_tiempo algorítmica)
  - loaders/ (dimensiones, hechos append-only con idempotencia)
  - control de cargas en edw.etl_control (incremental por última corrida OK)
        ▼
PostgreSQL 16 "postgres_edw" (Docker, puerto host 5433) — UN solo servidor, 3 esquemas:
  - edw.*    → DW multiestrella: 11 dimensiones + 11 tablas de hechos
  - public.* → app (roles, usuarios RBAC, cliente_lookup con PII real,
               metas_comerciales_operativas)
  - ml.*     → vistas para notebooks/entrenamiento (ml.v_ventas_cruzadas_desanonima)
        ▼                                    ▼
Backend FastAPI (backend/)            Pipeline ML (ml/)
  - JWT + RBAC (4 roles)                - ml/main.py entrena 6 modelos desde el EDW
  - KPIs y analytics por rol            - exporta .pkl a ml/models/
  - inferencia de los .pkl              - publish_models.py reinicia el backend
  - reentrenamiento (admin)               (volumen Docker ./ml/models:/app/ml_models:ro)
        ▼
Frontend React 19 + Vite + TS (frontend/, puerto 5173)
  - Dashboards por rol, Zustand + TanStack Query + Recharts
```

- **Origen de datos:** SAP SQL Anywhere 17 (`codemp='01'`, ~500 tablas OLTP; inventario en [docs/identificacion_bd.md](docs/identificacion_bd.md)).
- **Staging:** no hay capa de staging separada; los extractores SQL leen del ERP y los transformers en pandas producen directamente las tablas del EDW.
- **Data Marts:** no existen como capa separada; los "marts" son las vistas por rol servidas por la API.
- **Orquestación:** Docker Compose (`postgres_edw`, `backend`, `frontend`, y perfiles opcionales `etl` y `ml` que se ejecutan manualmente con `docker compose run`). No hay Airflow/dbt/SSIS.
- **Despliegue:** los DDL de `edw/` se ejecutan automáticamente al inicializar el contenedor de PostgreSQL (`/docker-entrypoint-initdb.d`) — **solo en volumen nuevo**.

## Tecnologías

- **Base de datos:** PostgreSQL 16 (EDW + app), SAP SQL Anywhere 17 (origen, solo lectura).
- **ETL:** Python 3 (pandas, SQLAlchemy 2, pyodbc, psycopg2, python-dotenv).
- **Backend:** FastAPI, Pydantic v2 / pydantic-settings, SQLAlchemy 2, python-jose (JWT), passlib+bcrypt, uvicorn.
- **ML:** scikit-learn, XGBoost, LightGBM, CatBoost, Optuna, mlxtend (Apriori), joblib, JupyterLab.
- **Frontend:** React 19, TypeScript, Vite 8, Tailwind CSS 4, Zustand, TanStack React Query, React Router 7, Recharts, Axios, oxlint.
- **Infraestructura:** Docker + Docker Compose (con `docker-compose.override.yml` para dev: hot-reload y mount de `ml/`).
- **Pruebas:** pytest (backend: `backend/tests/unit` y `backend/tests/integration`; etl: pytest declarado en requirements).

## Estructura del proyecto

| Ruta | Propósito |
|---|---|
| `etl/` | Pipeline ETL SAP → EDW. `orchestrator.py` (entrypoint), `extractors/*.sql` (24 extractores tokenizados), `transformers/`, `loaders/`, `connectors/`, `config/settings.py`, `tasks/generar_metas_operativas.py`. |
| `edw/` | DDL del DW en orden de ejecución: `01_schema.sql` … `09_vistas_ml.sql` (esquema, dims, hechos, índices, `etl_control`, verificación, tablas `public.*`, seed de roles/usuarios, vistas ML). |
| `backend/` | API FastAPI. `app/api/routes/` (routers), `app/services/` (lógica de negocio), `app/repositories/` (acceso a datos), `app/ml/` (carga e inferencia de .pkl), `app/models/` + `app/schemas/`, `app/core/` (config, seguridad, excepciones), `tests/`. |
| `ml/` | Entrenamiento. `main.py` (orquestador de los 6 modelos), `src/` (data/features/training/prediction/utils), `notebooks/` (EDA y experimentos), `models/` (.pkl publicados), `publish_models.py`, `REPORTE_MEJORA_MODELOS.md`. |
| `frontend/` | SPA React. `src/pages/` (dashboards por rol), `src/services/` (API + interceptores JWT), `src/store/` (Zustand), `src/hooks/`, `src/components/`, `src/router/AppRouter.tsx`. |
| `docs/` | Documentación: `arquitectura_dw.md` (diseño completo del EDW), `auditoria/` (reportes de auditoría), `features/`, `tesis/`, `matriz_trazabilidad.md`, `identificacion_bd.md`, `hoja_de_ruta_ejecucion.md`. |
| `.agent/` | Skills y workflows del agente (`workflows/ejecutar-etl.md`, `workflows/configurar-entorno.md`). |
| `docker-compose.yml` / `.env(.example)` | Orquestación y variables de entorno. |

## Reglas de negocio

Validadas contra Producción vía SELECT; detalle completo en [docs/auditoria/02_reglas_negocio_validadas.md](docs/auditoria/02_reglas_negocio_validadas.md):

1. **Estado de documento:** `estado = 'P'` (Procesada) es el único estado válido; `'A'` = anulada. Parametrizado como `ESTADO_VALIDO` (token `{ESTADO}`).
2. **Empresa:** toda la operación es `codemp = '01'`. Parametrizado como `CODEMP` (token `{CODEMP}`) para multi-empresa futura.
3. **Dirección de movimientos de Kardex:** `cantot` siempre es positivo (magnitud). La dirección la da `tipdoc`: entrada = `('EN','AC')`, salida = `('SA','AD')`. Nunca usar el signo de la cantidad.
4. **Transferencias (`tiporg='TRA'`):** cada ítem genera exactamente 2 filas (SA=bodega origen, EN=bodega destino) pareadas por `(codemp, numdoc, numren, codart)`. El kardex no expone cantidad solicitada ni estado de la transferencia (limitación del ERP).
5. **Descarga de inventario:** el costo de inventario solo aplica cuando `renglonesfacturas.desinv = 'S'` (líneas `'N'` son servicios/no inventariables).
6. **Stock:** la fuente de existencias por bodega es la vista `vi_mv_existencias` del ERP; el costo se toma de `articulos.ultcos`.
7. **Historial:** `Dim_Producto` y `Dim_Cliente` son SCD Tipo 2 (`es_vigente`, `fecha_inicio/fin_vigencia`).
8. **Anonimización PII:** los clientes se anonimizan con hash + salt (`PII_SALT`); la única tabla con PII real es `public.cliente_lookup`, aislada fuera del esquema `edw` a propósito. El ETL aborta si `PII_SALT` falta o usa el valor inseguro heredado.
9. **RBAC:** 4 roles fijos de negocio: `gerencia`, `administrador`, `ventas`, `bodega` (catálogo cerrado en `public.roles`; seed en `edw/08_seed_roles_usuarios.sql`). Ventas/bodega ven datos filtrados por su sucursal / código SAP.
10. **Metas y Comisiones (grano vendedor, 100% estadística, sin ML):** las metas operativas viven en `public.metas_comerciales_operativas` con grano `(anio, mes, id_vendedor_origen)` -- NO por sucursal, porque `edw.dim_vendedor` no tiene sucursal propia y un vendedor transacciona en múltiples sucursales (docs/auditoria/19_...md). Se generan vía `POST /gerencia/goals/generate` → `GoalMLService.generate_proposals`, que usa `IQRGoalCalculationEngine` (24 meses de Venta Neta, recorte de picos por IQR, tendencia rodante de los últimos meses, techo/piso de sanidad contra la tendencia reciente); el monto base se ajusta por tipo de vendedor si hay configuración en `public.comision_config_vendedor` (ver regla 13). El modelo `goals_rf` fue decomisionado (docs/auditoria/20_...md): no queda ningún modelo ML en este módulo. `edw.fact_metas_comerciales` existe pero está vacía.
11. **Ventana de entrenamiento de ventas (inferida, justificada en `ml/main.py`):** el modelo de ventas entrena solo con los últimos 3 años (`VENTANA_ENTRENAMIENTO_VENTAS_ANIOS`) por quiebre estructural del negocio (~31% de crecimiento 2018→2026); mejoró el R² de −0.03 a +0.21 en backtest.
12. **Registros centinela:** toda dimensión tiene una fila `-1` ("desconocido") usada cuando una llave foránea no resuelve; prohibido el fallback a filas arbitrarias (`LIMIT 1`), eliminado en auditoría 04.
13. **Comisiones Variables por margen/categoría (RN-CM1..CM4, docs/features/plan_integracion_comisiones_variables.md, docs/auditoria/30_...md):** esquema de comisión alternativo al plano (regla 10) que comisiona sobre `margen_bruto` de la línea (ponderado por categoría A/B/C/S/X, crédito y tipo de vendedor externo/interno) en vez de venta bruta. Convive con el esquema plano vía `COMISION_MODO` (`plana` default, `sombra` calcula ambos, `variable` activa el nuevo como oficial) en `backend/app/core/config.py` -- **el rollback es cambiar esa env var**. Configuración en `public.comision_matriz_categorias` / `comision_factores_credito` / `comision_config_vendedor` (CRUD bajo `/gerencia/goals/commission-config/*`), motor puro en `commission_engine.calcular_comision_variable` (no reemplaza `calcular_comision`), snapshots de liquidación congelados en `public.comision_liquidaciones` (transparencia total, salvaguarda 6). El código clasifica productos por `dim_producto.clase`/`subclase`, nunca por `nombre_clase` (100% vacío en el catálogo, auditoría 30 H2); el ajuste por plazo de crédito solo tiene datos reales para 0 y 30 días en el EDW actual (auditoría 30 H4).

## Restricciones

- **La base de datos de Producción (SAP) es de SOLO LECTURA.**
  - Nunca ejecutar `INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`, `ALTER` o `MERGE` sobre Producción.
  - Toda validación contra Producción debe hacerse exclusivamente con `SELECT`.
- No asumir que el SQL existente es correcto (los extractores ya tuvieron bugs críticos corregidos; ver `docs/auditoria/03_cambios_aplicados.md`).
- Detectar y eliminar hardcodes; los valores de negocio van en `etl/config/settings.py` / variables de entorno, y en los extractores como tokens (`{CODEMP}`, `{ESTADO}`, `{FECHA_DESDE}` — cada rama de un `UNION ALL` lleva su propio token).
- Toda regla de negocio debe estar documentada (en `docs/auditoria/02_reglas_negocio_validadas.md` o en la sección correspondiente).
- Los DDL de `edw/` (esquemas `edw.*`/`ml.*`, territorio del ETL) solo se ejecutan al crear el volumen de Docker; cambios de esquema en una BD existente requieren aplicación manual. **Ya no aplica al esquema `public`:** desde `docs/features/plan_migraciones_esquema_public.md`, Alembic (`backend/alembic/`) aplica automáticamente cualquier cambio de `public.*` al arrancar el contenedor del backend, tanto en un volumen nuevo como en una BD ya existente.
- Fail-fast de seguridad: el ETL aborta sin `PII_SALT` válido; el backend con `ENV=production` aborta si `JWT_SECRET` o `POSTGRES_PASSWORD` conservan el valor por defecto inseguro.
- El backend en producción NO tiene acceso al código de `ml/` (solo a los `.pkl` vía volumen de solo lectura); el reentrenamiento (`POST /admin/modelos/retrain`) solo funciona en dev con `docker-compose.override.yml`.
- Las librerías xgboost/catboost/lightgbm son dependencias de **runtime** del backend (los `.pkl` de joblib requieren la misma librería que los serializó); no removerlas aunque parezca deuda.
- `etl/truncate_edw.py` es destructivo sobre el EDW — usar solo de forma deliberada en desarrollo.

## Convenciones de desarrollo

- **Idioma:** código, comentarios, docs y nombres de negocio en español; snake_case en SQL y Python.
- **EDW:** dimensiones `dim_*` con surrogate keys `*_sk` (SERIAL), hechos `fact_*` (BIGSERIAL) con FKs a dims; SCD2 con `es_vigente`/vigencias; toda tabla lleva `fecha_carga`.
- **Extractores:** un archivo `etl/extractors/<entidad>_extractor.sql` por entidad, tokenizado; se registran en `PIPELINE_CONFIG` de `orchestrator.py` (SQL no referenciado ahí es código muerto).
- **Backend en capas:** `routes` → `services` → `repositories`. Los servicios lanzan excepciones de dominio (`app/core/exceptions.py`), nunca `HTTPException`; los handlers globales en `main.py` las traducen a HTTP. Sin lógica de negocio en `main.py` ni en los routers.
- **Frontend:** servicios por dominio en `src/services/`, tipos en `src/types/`, hooks de datos por rol en `src/hooks/`, permisos en `src/constants/permissions.ts`.
- **Configuración:** todo por variables de entorno con `.env` (plantilla en `.env.example`); nunca commitear secretos reales.
- **Comentarios:** los comentarios explican *por qué* (regla de negocio, hallazgo de auditoría), con referencia al reporte que lo valida.

## Flujo de trabajo esperado (para agentes de IA)

Antes de modificar código:

1. **Analizar contexto:** leer este documento, la documentación en `docs/` y el código afectado.
2. **Detectar dependencias:** qué extractores, transformers, tablas, servicios o dashboards dependen del componente.
3. **Evaluar impacto:** especialmente sobre datos ya cargados (idempotencia, SCD2) y sobre los contratos API/frontend.
4. **Generar auditoría:** crear un reporte en `docs/auditoria/` ANTES de modificar código (formato de los reportes existentes: fecha, alcance, método, hallazgos con severidad, acción).
5. **Proponer cambios:** documentar el cambio y su justificación (regla de negocio validada, no supuesta).
6. **Implementar:** respetando las restricciones (Producción solo lectura, sin hardcodes).
7. **Validar:** contra Producción solo con `SELECT`; contra el EDW con las verificaciones de `edw/06_verificacion.sql` y los tests (`pytest` en `backend/`); `py_compile` para el código Python del ETL.
8. **Documentar:** actualizar el reporte de auditoría con lo aplicado y las reglas de negocio nuevas en `docs/auditoria/02_reglas_negocio_validadas.md`.

## Auditoría

- Todos los reportes se guardan en `docs/auditoria/`. Existentes:
  - `00_planificacion.md` — inventario y arquitectura del proyecto.
  - `01_auditoria_extractores.md` — hallazgos en los extractores SQL.
  - `02_reglas_negocio_validadas.md` — reglas validadas contra SAP vía SELECT.
  - `03_cambios_aplicados.md` — correcciones aplicadas (idempotencia C1, tokens C2, dirección de kardex, etc.).
  - `04_auditoria_pipeline_python.md` — auditoría del núcleo Python del ETL.
  - `05_auditoria_ml_calidad_datos.md` — auditoría del pipeline ML y calidad de datos del EDW.
- La trazabilidad operativa de cargas vive en `edw.etl_control` (tabla + `edw/05_etl_control.sql`).
- `edw.fact_logs_auditoria` consolida modificaciones críticas del ERP para el detector de anomalías.
- Los reportes 00–05 referencian prompts por fase en una carpeta `prompts/` que ya no existe en el repo (ver Observaciones).

## Calidad de datos

Validaciones existentes:

- **Idempotencia de hechos:** antes de cargar, se borra por fecha real vía `dim_fecha` (`DELETE ... WHERE fecha_sk IN (SELECT fecha_sk FROM edw.dim_fecha WHERE fecha_completa >= :desde)`) y se hace append; el control incremental usa `edw.etl_control` (`estado='SUCCESS'`).
- **Integridad referencial:** FKs de todas las facts hacia las dims; llaves no resueltas van al registro centinela `-1` y se loguea `WARNING` con conteo de filas afectadas por columna.
- **SCD2 seguro:** `load_dim_scd2` verifica explícitamente la existencia de la tabla (no `except Exception` genérico) para no duplicar historial vigente.
- **Verificación post-carga:** `edw/06_verificacion.sql`.
- **Reconciliación con producción:** las reglas y volúmenes se validaron contra SAP con SELECT (auditorías 02 y 05); los 24 extractores se verificaron ejecutándose contra SAP (24/24 OK).
- **Hallazgos abiertos** (auditoría 05, no corregidos): `dim_geografia` vacía (0 filas), `edw.fact_metas_comerciales` vacía (metas solo en `public.metas_comerciales_operativas`), `dim_fecha.es_feriado` nunca poblado (workaround hardcodeado en código ML), `fact_inventario_snapshot` solo poblada "hacia adelante" (<1% histórico pre-2026).

## Objetos importantes

- **Dimensiones (11):** `dim_fecha` (conformada, generada algorítmicamente 2010–2030 parametrizable), `dim_producto` (SCD2), `dim_cliente` (SCD2, anonimizada), `dim_sucursal`, `dim_almacen`, `dim_proveedor`, `dim_vendedor`, `dim_empleado`, `dim_usuario`, `dim_formapago`, `dim_geografia` (vacía).
- **Hechos (11):** `fact_ventas_detalle` (~539k filas, principal), `fact_movimientos_inventario` (~948k), `fact_movimientos_caja`, `fact_cobros_cxc`, `fact_compras`, `fact_inventario_snapshot`, `fact_pagos_cxp`, `fact_devoluciones`, `fact_nomina`, `fact_metas_comerciales` (vacía), `fact_logs_auditoria`.
- **Tablas `public.*`:** `roles`, `usuarios` (auth de la plataforma), `cliente_lookup` (única tabla con PII real), `metas_comerciales_operativas`, `recomendaciones_eventos` (telemetría de Venta Cruzada, docs/auditoria/25_modulo_cross_selling.md), `comision_matriz_categorias` / `comision_factores_credito` / `comision_config_vendedor` / `comision_liquidaciones` (Comisiones Variables, docs/features/plan_integracion_comisiones_variables.md, regla 13), `notificaciones` (Notificaciones Inteligentes Segmentadas por Rol -- solo notificaciones PERSISTIDAS con estado de lectura; las calculadas al vuelo nunca se escriben ahí, docs/auditoria/31_modulo_notificaciones.md, regla 20).
- **Vistas:** `ml.v_ventas_cruzadas_desanonima` (une ventas con identidad real vía lookup, para notebooks); origen ERP: `vi_mv_existencias`.
- **Control:** `edw.etl_control` (idempotencia/auditoría del ETL).
- **Pipelines / entrypoints:** `etl/orchestrator.py` (ETL completo), `ml/main.py` (entrena los 4 modelos), `ml/publish_models.py` (recarga el backend). Las metas comerciales se generan desde el backend (`GoalMLService.generate_proposals`, `POST /gerencia/goals/generate`), no desde un task del ETL -- `docs/matriz_trazabilidad.md`/versiones previas de este documento referenciaban un `etl/tasks/generar_metas_operativas.py` que no existe en el repositorio (inconsistencia ya corregida aquí, ver `docs/auditoria/20_...md`).
- **Modelos ML (4, en `ml/models/*.pkl`, servidos por `backend/app/ml/`):** `demand_rf_model` / `demand_best_model` (demanda — Bodega), `kmeans_rfm_model` (segmentación RFM, K=4 — Ventas), `churn_classifier` / `churn_best_classifier` (riesgo de fuga, un solo algoritmo fijo -- CatBoost, docs/auditoria/51_...md), `association` / `recommendation.pkl` (venta cruzada — filtrado colaborativo item-item por similitud coseno, contrato v0.2.0, ganador de la competencia documentada en `ml/REPORTE_MEJORA_MODELOS.md` y `docs/auditoria/25_modulo_cross_selling.md`; expone `score` genérico, NO `lift`). El modelo de metas (`goals_rf`) fue decomisionado (docs/auditoria/20_...md): Metas y Comisiones usa solo estadística pura. El modelo de predicción de ventas (`sales_rf`) fue decomisionado (docs/auditoria/49_...md): el dashboard de Gerencia muestra la evolución mensual real (Venta Neta, sin ningún modelo) en vez de un forecast. El modelo de anomalías (`anomaly`, Isolation Forest) fue decomisionado (docs/auditoria/51_...md): el panel de Administrador pierde el detector transaccional y su cola de triage, sin reemplazo.
- **API (prefijo `/api/v1`):** `/auth`, `/users`, `/roles`, `/analytics` (Gerencia), `/analytics/bodega`, `/analytics/ventas`, `/analytics/admin`, `/admin/modelos` (MLOps), `/gerencia/goals`, `/notificaciones`. Salud en `/health`; Swagger en `/docs`. El módulo Bodega (docs/auditoria/23_modulo_bodega.md, extensión en docs/auditoria/24_prediccion_categoria_paginacion.md) expone bajo `/analytics/bodega`: `/filtros`, `/kpis`, `/salidas-forecast`, `/prediccion-compras-mes`, `/rotacion-matriz`, `/top-productos`, `/salidas-categoria`, `/stock-reorden`, `/necesidad-compra`, `/inventario-matriz`, `/transferencias-sugeridas`, `/reportes/{tipo}[/excel]` — todo estadística sobre el EDW salvo el forecast por producto (`/salidas-forecast?producto_cod=...`) y `/prediccion-compras-mes` (predicción de compras del mes siguiente por categoría, con drill-down a los top artículos), que reutilizan `demand_rf` (sin modelos ML nuevos); umbrales configurables `BODEGA_*` en `backend/app/core/config.py` (reglas RN-B1..B7 en `docs/auditoria/02_reglas_negocio_validadas.md` §16). Actualización 2026-07-15 (`docs/features/plan_actualizacion_modulo_bodega.md`, `docs/auditoria/32_actualizacion_modulo_bodega.md`, RN-B8/RN-B9 en §16): `/reportes/{tipo}[/excel]` aceptan y propagan los 6 filtros globales (antes solo `almacen/categoria/proveedor`); `tipo_movimiento` se valida contra el catálogo cerrado (`ValidationError` → 400) en el choke point `WarehouseRepository._filtros_snapshot`; `/top-productos` y `/salidas-categoria` ganan `monto_ventas` opcional (RN-B8, solo poblado con `tipo_movimiento=FAC` o `CPA`, reutilizando `valor_venta`/`costo_total` ya presentes en `fact_movimientos_inventario`, sin JOIN nuevo); `/transferencias-sugeridas` gana `justificacion`/`confianza`/`beneficio_neto_estimado` opcionales (RN-B9, umbral de emisión `beneficio_neto_estimado > 0` y `meses_con_venta_destino ≥ BODEGA_MIN_MESES_VENTA`); en el frontend, la tabla "Estado de Stock vs Punto de Reorden" se eliminó de `DashboardBodega.tsx` (el endpoint `/stock-reorden` se conserva, lo sigue consumiendo Notificaciones) y "Predicción de Necesidad de Compra" se movió a `BodegaAlmacenes.tsx`. Fase 5 (rediseño de reportes) cambia el contrato de `GET /reportes/{tipo}[/excel]`: `ReporteBodegaResponse` pasa de `{generado_en, contenido: dict libre}` a `{tipo, titulo, generado_en, filtros_aplicados, resumen_ejecutivo: KpiResumenEjecutivo[], interpretacion, secciones: SeccionReporte[]}` (columnas de negocio declaradas por sección en vez de claves crudas de dict); el Excel (`warehouse_export.py`) genera una hoja "Resumen" (KPIs + filtros aplicados) y una hoja por sección con formato de moneda/porcentaje, autofiltro y resaltado de prioridad "Alta"/"Crítico"; `BodegaReportes.tsx` se reescribió para renderizar el contrato tipado (tarjetas con la pregunta de negocio que responde cada reporte, banda de KPIs, interpretación en una frase, chip-bar de filtros aplicados). Pendiente solo la sesión de validación con usuario final (criterio de aceptación no automatizable). El endpoint `/analytics/bodega/notificaciones` (calculadas al vuelo) fue reemplazado por el router unificado de Notificaciones (ver más abajo) -- el generador de Bodega se conserva intacto en `WarehouseService.get_notificaciones`, solo cambió quién lo expone. Paginación genérica reutilizable (`Page[T]`/`PaginationParams`, `backend/app/schemas/pagination.py`, espejo en `frontend/src/types/pagination.ts` + `components/ui/Pagination.tsx` + `hooks/usePagination.ts`) aplicada a `/stock-reorden`, `/necesidad-compra` (solo `recomendados`), `/inventario-matriz` y `/transferencias-sugeridas`. El módulo de Venta Cruzada (docs/auditoria/25_modulo_cross_selling.md) expone bajo `/analytics/ventas/cross-selling`: `POST /sugerencias` (asistente de canasta simulada), `POST /eventos` (telemetría mostrada/aceptada/rechazada, RN-CS2), `GET /kpis` (tasa de conversión) y `GET /productos` (autocompletar); reutiliza el modelo `association` (sin modelo nuevo); umbrales `CROSS_SELL_*` en `backend/app/core/config.py` (reglas RN-CS1/RN-CS2 en `docs/auditoria/02_reglas_negocio_validadas.md` §17). El endpoint original por-cliente (`GET /analytics/ventas/recommendations`) se conserva sin cambios de contrato, solo actualizado internamente para leer `score` en vez de `lift`. En el frontend, el Asistente de Venta Cruzada vive en una página propia (`/ventas/cross-selling`, `VentasCrossSelling.tsx`, nav propio en el Sidebar) -- mismo patrón que Metas y Comisiones (`/ventas/metas`), no una sección embebida en `DashboardVentas.tsx`. El sistema de Comisiones Variables (docs/features/plan_integracion_comisiones_variables.md, regla 13) extiende `/gerencia/goals` de forma aditiva: `GET/POST /commission-config/matriz`, `GET/PUT /commission-config/credito`, `GET/PUT /commission-config/vendedores/{vendedor_origen}`, `POST /commission-simulation` (simulación retroactiva plano vs. variable), `GET /commission-analysis/categorias` (perfil de margen por categoría) y `GET /lineas-sin-costo` (salvaguarda 2); el endpoint existente `GET /gerencia/goals/commissions` y `GET /analytics/ventas/goals/mi-comision` ganan los campos opcionales `comision_variable`/`nivel_variable` (y `desglose_variable` en mi-comision), poblados solo cuando `COMISION_MODO` ≠ `plana` -- sin romper el contrato para instalaciones que sigan en el esquema plano. El módulo de Notificaciones Inteligentes Segmentadas por Rol (docs/features/plan_modulo_notificaciones.md, docs/auditoria/31_modulo_notificaciones.md, regla 20) generaliza el patrón de alertas de Bodega a los 4 roles, expone bajo `/notificaciones`: `GET ""` (calculadas al vuelo por rol -- Bodega, salud de modelos de Admin, divergencia plano-vs-variable de comisiones de Gerencia, churn alto RLS de Ventas -- + persistidas no leídas del rol/usuario del token), `POST /{id}/leer`, `POST /leer-todas`, `GET /historial` (paginado, `Page[T]`); sin modelos ML nuevos (reutiliza `demand_rf` vía `WarehouseService`, `churn_rf` vía `Cartera360Service.get_lista_trabajo`). (La notificación calculada "Desvío del forecast semanal" de Gerencia, que reutilizaba `sales_rf`, se retiró junto con el modelo -- docs/auditoria/49_...md.) Solo las notificaciones **persistidas** (eventos puntuales: `metas_generadas` emitida al final de `GoalMLService.generate_proposals`) se escriben en `public.notificaciones`, con deduplicación de 24h (`NOTIF_DEDUPE_HORAS`) y estado de lectura por usuario (`leida_por` JSONB); umbrales `NOTIF_*` en `backend/app/core/config.py`. Polling de 60s en el frontend (sin WebSockets en v1); campana global en `components/layout/NotificationBell.tsx` (antes solo visible para Bodega). El módulo de Reabastecimiento Inteligente (docs/features/plan_reabastecimiento_inteligente.md, auditoría 50, reglas RN-RB1..RN-RB10 en `docs/auditoria/02_reglas_negocio_validadas.md` §25) expone bajo `/analytics/bodega/reabastecimiento`: `GET /lista` (paginado, `Page[T]`, priorizado por riesgo real de quiebre), `GET /lista/{codart}/explicacion` (detalle explicable de un artículo, misma clase ABC que la lista completa) y `GET /resumen` (KPIs de decisión); `GET /alertas` (cambio brusco de demanda/tendencia decreciente, también integradas a la campana de Notificaciones); `POST /simular` (what-if de política, solo lectura); `POST /propuestas` · `GET /propuestas[/{id}]` · `POST /propuestas/{id}/{aprobar,rechazar}` (propuestas de compra persistidas, snapshot congelado); `GET/PUT /politica/{clase_abc}` y `GET/PUT/DELETE /lead-times` (solo gerencia/administrador) para la configuración editable de nivel de servicio y lead time. Convive con `/necesidad-compra` (RN-B1/RN-B4) sin reemplazarlo -- prioriza por stock de seguridad/punto de reorden, no por volumen de ventas.

## Dependencias

- **Externas:** SAP SQL Anywhere 17 (ERP; en Docker se conecta con FreeTDS/TDS 5.0 instalado en la imagen del ETL, en el host puede usarse el driver ODBC nativo — ver `docs/auditoria/06_auditoria_driver_sap_docker.md`), Docker/Docker Compose. Paquetes en `etl/requirements.txt`, `backend/requirements.txt`, `ml/requirements.txt`, `frontend/package.json`.
- **Internas (orden de dependencia):** EDW ← ETL ← ERP; ML entrena desde el EDW y publica `.pkl` que el backend consume vía volumen; el frontend depende de los contratos de la API; los DDL `edw/01..09` deben ejecutarse en orden.
- **Acoplamientos a tener en cuenta:** los `.pkl` acoplan versiones de librerías ML entre `ml/` y `backend/`; `publish_models.py` depende de Docker Compose en el host; el esquema `public` lo crea/actualiza Alembic (`backend/alembic/`, aplicado por `entrypoint.sh` antes de que uvicorn arranque) — `edw/07`/`edw/08` se conservan activos en `docker-entrypoint-initdb.d` solo por el bootstrap de un volumen nuevo (`edw/09_vistas_ml.sql` depende de `public.cliente_lookup` en el mismo initdb), no como fuente de verdad del esquema.

## Riesgos técnicos (observados, no corregidos)

- ~~`etl/loaders/` está borrado del working tree~~ — **resuelto 2026-07-15** (`docs/features/plan_correcciones_pendientes.md` P0-2): `dim_loader.py`/`fact_loader.py` existen en el working tree y están rastreados por git (`git ls-files etl/loaders/`); `python -m py_compile` de ambos módulos + `orchestrator.py`, y `import orchestrator`, terminan sin errores. El riesgo nunca se materializó o fue un estado transitorio ya corregido. Pendiente no automatizable: una corrida real del ETL contra SAP en dev para confirmar extremo a extremo (requiere conectividad en vivo al ERP).
- Scripts ad-hoc de diagnóstico en `etl/` (`query_diag_db.py`, `query_nc.py`, `query_notas_credito.py`, `test_sap.py` duplicado en `etl/` y `etl/connectors/`, `inspect_formapago.py`): código exploratorio mezclado con el pipeline.
- `etl/truncate_edw.py`: script destructivo sin salvaguardas, junto al pipeline.
- ~~Duplicación de artefactos ML~~ — **mitigado 2026-07-16** (M-13, `docs/features/plan_mejoras_proyecto.md`): `models/` (raíz) ya no existe; `backend/ml_models/`/`ml/models/` nunca estuvieron versionados (`*.pkl` en `.gitignore`, son copias locales de runtime, no duplicados del repo). Lo único versionado por error era `ml/catboost_info/` (residuos de log de entrenamiento) -- agregado a `.gitignore` y retirado del índice.
- `docs/credenciales_sistema.md` versionado en el repo: riesgo de exposición de credenciales.
- ~~CORS por defecto `"*"` en el backend~~ — **mitigado 2026-07-15** (S-2, `docs/features/plan_correcciones_pendientes.md`): el default de código sigue en `"*"` para no romper dev, pero `validar_configuracion()` ahora aborta el arranque con `ENV=production` si `CORS_ORIGINS` sigue en `"*"` (mismo patrón fail-fast que `JWT_SECRET`/`POSTGRES_PASSWORD`). Restringir explícitamente vía `CORS_ORIGINS` (string separado por comas, no JSON) sigue siendo responsabilidad del entorno de despliegue.
- Defaults inseguros conocidos (JWT, password de BD) tolerados fuera de `ENV=production`.
- Frontend con `src/services/mocks/`: verificar que ningún dashboard consuma mocks en producción.
- Deuda documentada: `es_feriado` con workaround hardcodeado en ML, `dim_geografia` y `fact_metas_comerciales` vacías, cabeceras de extractor huérfanas ya eliminadas en auditoría 04.

## Observaciones (inconsistencias código ↔ documentación)

- ~~**Crítico:** `docs/auditoria/04_auditoria_pipeline_python.md` describe correcciones aplicadas a `etl/loaders/*`, pero esos archivos están actualmente borrados del working tree~~ — **obsoleto, ver §Riesgos técnicos** (verificado 2026-07-15, P0-2 de `docs/features/plan_correcciones_pendientes.md`): los archivos existen y compilan; la auditoría 04 y el estado en disco están alineados.
- `docs/auditoria/00_planificacion.md` referencia rutas antiguas del backend (`backend/app/api/v1/endpoints/`); la estructura actual es `backend/app/api/routes/` (los endpoints v1 antiguos figuran como eliminados en git).
- Los reportes de auditoría mencionan una carpeta `prompts/` (prompts de fases 00–08) que no existe en el repositorio.
- `.agent/workflows/ejecutar-etl.md` referencia la ruta `c:\Tesis` como raíz de trabajo; la raíz real es `c:\Proyect_BI`.
- `docs/matriz_trazabilidad.md` lista rutas de endpoints (`/api/v1/kpis/gerencia/forecast`, etc.) que no coinciden con los prefijos reales de `backend/app/api/routes/api.py` (`/analytics/...`, `/gerencia/goals`); tomar el código como fuente de verdad.
- ~~Dos convenciones de variables de conexión al ERP (`SQLANY_*` vs `DB_*`)~~ — resuelto 2026-07-08: `.env.example` ahora usa las variables `DB_*` que realmente lee `etl/config/settings.py` (auditoría 06).
- `docs/auditoria/00_planificacion.md` indica "No existe README raíz": sigue siendo cierto; este CLAUDE.md actúa como documento principal de contexto.
- Pendiente de documentar: ventanas de carga/calendarización del ETL (hoy la ejecución es manual; el crontab está planificado en la Fase 6 de `docs/hoja_de_ruta_ejecucion.md`).
