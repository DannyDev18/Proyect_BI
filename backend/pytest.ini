[pytest]
markers =
    integration: requiere Postgres real (postgres_edw) corriendo; excluido del run rápido por defecto.
    model_artifacts: requiere los .pkl reales en ml/models/; se salta si no existen.
addopts = -m "not integration and not model_artifacts"
testpaths = tests
