fastapi>=0.100.0,<1.0.0
uvicorn>=0.23.0,<1.0.0
sqlalchemy>=2.0.19,<2.1.0
# Migraciones versionadas del esquema public (docs/features/plan_migraciones_esquema_public.md)
alembic>=1.13.0,<2.0.0
pydantic>=2.1.1,<3.0.0
pydantic-settings>=2.0.2,<3.0.0
psycopg2-binary>=2.9.7,<3.0.0
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
bcrypt>=3.0.0,<4.0.0
python-multipart>=0.0.6,<1.0.0
email-validator>=2.0.0,<3.0.0
pydantic[email]>=2.1.1,<3.0.0
pandas>=2.0.0,<3.0.0
# H-20 / M-01 (docs/auditoria/24_.../plan_mejoras_proyecto.md): versiones EXACTAS e
# idénticas a ml/requirements.txt -- joblib.load() de los .pkl no garantiza compatibilidad
# entre versiones de sklearn/xgboost/lightgbm/catboost (InconsistentVersionWarning puede
# esconder predicciones silenciosamente distintas, no solo un crash). Actualizar ambos
# archivos a la vez y reentrenar+republicar si se sube alguna de estas versiones.
scikit-learn==1.9.0
joblib==1.5.3
# xgboost/catboost/lightgbm: necesarias en runtime (no solo en entrenamiento) porque
# joblib.load() de los .pkl requiere la MISMA librería que los serializó -- no se
# pueden quitar del backend aunque parezca deuda de "ML no debería estar en el API".
xgboost==2.1.4
catboost==1.2.10
lightgbm==4.6.0
# Export XLSX de reportes del módulo Bodega (docs/auditoria/23_modulo_bodega.md)
openpyxl>=3.1.0,<4.0.0
# Rate limiting de /auth/login (S-3, docs/features/plan_correcciones_pendientes.md)
slowapi>=0.1.9,<0.2.0
# Explicabilidad real de churn_rf (Fase 6, docs/features/plan_refactor_venta_cruzada_ia.md
# §2.1 Opción A): shap.TreeExplainer sobre 4 features -- churn_rf es siempre un ensamble de
# árboles (RF/XGBoost/LightGBM/CatBoost, competencia de model_selector.py), TreeExplainer
# aplica a los 4 sin distinción de cuál ganó.
shap>=0.46.0,<0.47.0
# Auditoría 41 (refactor Cartera 360, hallazgo real): `shap==0.46.0` importa incondicionalmente
# su módulo de gráficos (`shap/plots/colors/_colorconv.py`, código heredado de skimage) desde
# `shap/__init__.py`, aunque nunca se usa ningún gráfico aquí (solo `TreeExplainer`). Ese módulo
# llama `np.dtype(np.floating)` con un tipo abstracto -- numpy>=2.3 lo rechaza
# (`TypeError: Converting np.inexact or np.floating to a dtype not allowed`), rompiendo el
# `import shap` completo, no solo el plotting. Sin pin explícito, pip resolvía numpy 2.4.x
# (compatible con pandas/scikit-learn/xgboost, pero no con shap 0.46). Reproducido en vivo:
# `import shap` fallaba en el contenedor real tras reconstruir la imagen.
numpy<2.3
